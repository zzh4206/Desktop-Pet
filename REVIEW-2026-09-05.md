# 桌宠仓库全量 Review 记录（2026-09-05）

> 审查起点 HEAD `1d8e64a`（v0.14.45，REVIEW-2026-09-04 批次 A-H 清偿完毕）
> 范围：app.py + pet/ 全部模块 + rig/ + ui/ + tools/ 10 脚本 + spikes/ 测试与 _qa + README/config.example/requirements；三路并行深查（核心运行时 / 安全敏感集成面 / 传感器·训练线·测试缺口）+ 全部 P1/P2 与主要 P3 逐条第一手复核（复核中剔除 2 条误报）。
> 上游：`REVIEW-2026-09-04.md`（其未清项见 §八，不重复开票）。
> 本轮重点：上轮 9 轮修复后的新鲜眼全仓重读——安全面（open_app 黑名单绕过类）、健壮性（线程收尾/文件编码/掉电一致）、ML 产线分布一致性。

---

## 一、P1 安全（4 项，批次 A 清偿）

**P1-1 🔴 open_app 黑名单被 Win32 尾点归一化绕过**
- `pet/tools_win.py`（第一手复核）：`"cmd."`/`"cmd.."` 过 `_APP_NAME`（仅禁纯点串）且不在精确名单；`os.startfile` 经 ShellExecute 名字解析剥尾点照常启动 cmd.exe。LLM（提示注入可及，含记忆通道存储型注入）一键绕过 L12 全部门。修：`_app_denylisted` 归一化命中（尾点剥除 + splitext 兜 "cmd.com" 变体），集合改存裸名。

**P1-2 🔴 黑名单缺交互 shell/系统工具**
- 同文件：`wsl`/`bash`/`conhost`/`python`/`py`/`wmic`/`mmc`/`certutil`/`bitsadmin`/`curl`/`msbuild`/`installutil`/`msiexec`/`winget`/`ssh`/`scp`/`ftp`/`telnet` 等 20 项全放行——正是 L12 注释本要防的"包装器绕过"类。修：扩充名单。

**P1-3 🔴 mac open_app 无系统程序黑名单（与 win 不对称）**
- `pet/tools_mac.py`：`open -a Terminal/Script Editor/Automator` 免确认启动脚本环境。修：移植 `_APP_DENYLIST`（Terminal/iTerm/Script Editor/Automator/Console/活动监视器/磁盘工具/快捷指令）。**留实机复核。**

**P1-4 🔴 非 dict 工具参数崩整轮聊天**
- `pet/llm.py` + `pet/tools_schema.py`（第一手复核）：畸形模型输出 json.loads 出 str/list 时，INFO 日志 `(args or {}).items()` 与 open_app `dangerous_when(a.get(...))` 双处 AttributeError 逃出 dispatch 保护段 → 整轮降级 FALLBACK（模型无从纠正）。修：解析层归一空参 + dispatch 入口防御 + dangerous_when 判定异常按危险处理（fail-closed）。

## 二、P2 功能/隐私/健壮（11 项，批次 B 清偿）

- **P2-1** 坏字节编码不崩：config/memory/pet_state 三处 load 的 `except (OSError, JSONDecodeError)` 漏接 UnicodeDecodeError（ValueError 子类）——config.json 存成 GBK 每次启动崩到删档、memory.json 损坏永久禁聊天且 .bak 兜底被跳过。补 ValueError。
- **P2-2** 权限页剪贴板自检零内容访问：旧版每次唤出真跑 `Get-Clipboard`（工具层同操作 dangerous=True"常含密码/token"）。改 GetClipboardSequenceNumber + OpenClipboard 开合探针。
- **P2-3** 全屏误判：自动隐藏任务栏下 work==monitor，任意最大化前台窗口矩形覆盖=假全屏（宠物无限期隐藏、WANDER 暂停）；前台窗无 cloak 检查。加 WS_CAPTION/WS_THICKFRAME 样式过滤 + 前台 cloak 排除。
- **P2-4** 全屏期间气泡照发：H2 只挡进入瞬间 hide，久坐/早晚安/聊天情绪新气泡照常 show+raise 盖演示。app 增 `_auto_bubble` 统一入口（全屏丢弃留痕），用户主动路径不受限。
- **P2-5** 系统 DND 未接线：`dnd_fn=None`（README 却承诺"非勿扰"）；`on_dnd_active` 仅 eat_mouse 入口一处调用，会话中途开启勿扰不打断。eat_mouse_tick 周期复查 → 中途吐出；README 措辞对齐。
- **P2-6** 主动关怀状态不持久化：重启重发早晚安/节日、follow-up 静默丢失、唤醒链重排。落 proactive_state.json（tmp+replace 原子写 + 启动重水化；过期 follow-up 不复活、12h 内过期唤醒保留到点即触发）。
- **P2-7** 多屏 bounding-box 死区：不等高/纵向错位多屏下 `_new_target` 可选中无屏区（宠物走进真空整轮隐形，看门狗查 isVisible 救不了）；`_bottom()` 用合集底在高屏侧坠落穿死区。Sensors 增 `screen_rects`（双端同实现），FSM 按屏采样目标/取屏底；无 screen_rects 退旧行为。
- **P2-8** v2 定时推理分布外：encoder 只训过单句，旧版把最近 3 句 `"\n".join` 成单串喂入（tokenize 空白折叠=未见形态），每日 22:00 路径恒跑偏。改逐句 encode、概率平均；单句即时路径不变。
- **P2-9** CPED Utterance_ID 字典序排序：≥10 句对话上下文乱序（"1","10","11","2"）。改 int 数值序 + 非数字退字典序；**数据重建随 GPU 重训并轨**（本机无 CPED 数据）。
- **P2-10** 危险确认懒初始化竞态 + 无超时：无锁双构造泄漏 QObject；主线程被模态占住时 ChatWorker 永久阻塞。加锁 + 60s 超时视为拒绝（fail-closed）。
- **P2-11** 留存披露：chat_emotion 空档同步移除 .bak；设置窗注明 48h 保留 + 开关即时生效（顺修"重启后完全生效"过时文案）；README 补 memory.json/chat_emotion.json 本地明文说明。

## 三、P3（27 项，批次 C 清偿）

| # | 位置 · 问题 · 修法 |
|---|---|
| 1 | hotkey_win stop 二次调用向已回收线程 id 投 WM_QUIT + _loop 收尾读共享 _keys 可注错新热键 → join 后清 id/引用；线程体入口局部快照 |
| 2 | mouse_lock_win _release 消费后置零 _hook_thread_id（新会话启动窗口期 force_spit 投死 id） |
| 3 | play_frames 无条件熄旧计时器（1 帧序列不 start 不 stop，land 520ms 名存实亡）+ 空帧防御停帧 |
| 4 | _mtime_cached isfile→getmtime TOCTOU 包 try（150ms 帧槽反复炸） |
| 5 | winreg OpenKey 异常路径泄句柄 → with 语法 |
| 6 | mac LaunchAgent plist 原子写 tmp+os.replace（**留实机复核**） |
| 7 | llm 取消后幽灵降级回复掐掉 + 流式累计 1MB 上限（超限丢弃+标注，回显同步停） |
| 8 | _apply_chat_emotion 成功后才置 active（失败下轮 poll 可重试） |
| 9 | pet_state 清 dirty 进锁内复核 _last_update；memory save 逐条浅拷贝防撕裂、forget_expired 衰减锚定 _last_decay_at（不再重复衰减/无变化置脏） |
| 10 | 死旋钮与文案：user_name 入 example+schema；pet_height_px 出 example/safe-defaults（留启动兜底注释）；potplayer.exe→potplayermini64.exe；"DS key"→"API key" |
| 11 | win 子进程 CREATE_NO_WINDOW×4（工具调用闪控制台）+ 音量 COM 对象 finally Release |
| 12 | sensor_win 枚举回调体包 try（ctypes 回调异常=静默截断枚举入缓存）+ 补 logger + 空监视器结果同 TTL 缓存（降级态 20Hz 重枚举） |
| 13 | DRAG 态跳过 y 防御钳制（拖过上下任务栏 20Hz 拔河/落点弹回消除） |
| 14 | wake 链兜底：_fire_wake 异常重排、worker 占用顺延 10min 不再断链、done/failed 恰入安静时段气泡顺延 |
| 15 | 拖放可执行文件二次确认（win 11 类扩展名 / mac 8 类，双端） |
| 16 | README 补 file_search 结果送 LLM 明示 |
| 17 | obvious_emotion 元数据版本参数化（默认 MODEL_VERSION 兼容，app 传 engine.version） |
| 18 | 训练护栏：encoder 训练侧复验 train/acceptance 不相交（结构性保证不只靠构建侧）；v1 空验证集 SystemExit 对齐 v2 |
| 19 | gen_fill 掩码必须真 alpha + 全图覆盖拒绝落盘（"构造性零漂移"不再静默失效） |
| 20 | 资产工具杂项：gen_walk_mids --only 缺值/200 非 JSON、import_layers 缺参用法、extend_part id 不存在、qa_rig 豁免无预算改 FAIL + 零像素图兜底、postprocess_assets global 前置（**compileall tools 此前不可过**） |
| 21 | requirements-tools.txt（Pillow+scipy，此前 7 个 tools 脚本依赖无处声明） |
| 22 | 版本单一源 0.14.36→0.14.48（落后 9 版架空其用途） |
| 23 | solid_at ref=None 平台语义差异注释固化（win 2s 缓存 vs mac 任意实体窗，保守方向） |
| 24 | proactive 陈旧注释对齐（win 实际已注入 mouse_lock_win） |
| 25 | follow_toggle 分支注明仅测试引用（生产走 motion_mode:） |
| 26 | rig play_frames 文件帧播前重显 _quick（rig+emoji+文件帧组合动画画进隐藏场景） |
| 27 | app.py 表达式内异常空白清理 |

## 四、误报剔除（复核不成立，留档防复报）

- ~~`_poll_chat_emotion` 坏标签炸时段循环~~：`active_label()` 已滤非 LABELS 标签返 None。
- ~~config.example 热键 `cmd+option+p` 在 win 失效~~：parse_hotkey `cmd→MOD_CONTROL` 别名映射在位。

## 五、核对无问题区域（抽查在位）

subprocess 全 argv 无 shell=True/eval/exec；_ps_run 单引号转义+双拦；key 全程凭据库不进日志（工具参数日志 24 字符截断、工具结果 4000 截断）；TLS 默认校验+无重试；危险确认跨线程 BlockingQueued 派发 + fail-closed；memory_tools 无按路径删改面；atomic 写链（tmp+fsync+replace）全仓一致；QTimer/QThread 生命周期（parent+finished→deleteLater）在上轮抽查后无回退。

## 六、执行中实测新发现（随批处理）

- **confirm 超时 fail-open（P2-10 执行中发现，比记录更重）**：超时路径回读 `_inner.result` 拿到上一轮确认的陈旧值——上轮通过+本轮超时=放行危险操作。改按次记录结果（v04 P2-10b 用例锁定）。
- **tools/postprocess_assets.py 语法级不可运行**：`global SRC, DST` 在使用之后声明（compileall 扩到 tools 才暴露）。
- **v06 首轮全量并行跑一次 35/1**（复跑×4 全绿 36-39/39）：判 T25 异步泵计时偶发，非本轮改动回归；后续观察。

## 七、清偿执行记录

| 批次 | 提交 | 内容 | 实跑 |
|---|---|---|---|
| A | v0.14.46 | P1-1~P1-4 安全四连 | v08 63/63（+28 用例）·v04 20/20·v02 14/14·v08perm 5/5·冒烟 9/9 |
| B | v0.14.47 | P2-1~P2-11 功能/隐私/健壮十一项 | 新增 26 用例：v06 36·v15 +3·v09 42·v04 22（超时两项）·v08perm 7·v03fsm 80·v03s 15·v05/v02/v07/v07win/v11 回归全绿 |
| C | v0.14.48 | P3-1~P3-27 杂项二十七项（P3-6 mac plist 原子写补落后 amend） | 16 套件 + 冒烟全绿·compileall app/pet/tools 全过 |
| D | v0.14.49 | E1/E3/E4/E5 测试缺口补齐 + 到达复查 fail-closed 顺手修（与 L11 语义对齐）+ 本 REVIEW 留档 | **全量 17 项 552 通过 0 失败**（v02 14·v03 80·v03s 21·v04 22·v05 23·v06 39·v07 25·v07win 33·v08 63·v08perm 7·v09 42·v10 35·v11 20·v13 48·v14 35·v15 36·冒烟 9），rig 门禁 healthy/neglected 双 figure PASS |

基线 v0.14.45 = 484 → 本轮 +68 用例（P1 +28、P2 +26、批次 D +14）。

## 八、本轮未清项（移交下轮）

- **v2/v1 模型重训**（待 GPU，并轨上轮）：P2-8（逐句聚合）与 P2-9（CPED 数值序）已落推理/构建侧，重训后以新 report 为准；本机无 onnxruntime/tokenizers/CPED 数据。
- **mac 实机复核**（累计：上轮批次G 两项 + 本轮 P1-3 黑名单、P3-6 plist 原子写）。
- **E2 v2 运行路径合成 ONNX fixture**：P2-8 用假 tokenizer/session 覆盖聚合/阈值/softmax 链，`_load_v2` 真加载链仍待有 ort 环境补。
- 维持留档：rL7 尾件、eat_mouse 12 帧零调用、F28 pytest+CI、F22 打包依赖、非流式慢滴流续轮、T5/T6/T7 测试债、encoder.int8.onnx 23MB 仓库膨胀。
- 思考项（本轮评估后暂不动作）：`llm.additionalProperties=True` 允许 config 改 `base_url`（凭据将发往任意主机——同权限攻击面，建议未来对 base_url 变更加日志留痕）；聊天情绪档案 DPAPI/Keychain 加密（可选）；solid_at ref=None 双端语义对齐（当前注释固化差异）。
