# 桌宠 工作表

> 每个小版本的双端任务清单。**mac端/win端任务各自负责；共享任务不预先指派，认领实现**，该模块主笔（见 `平台适配与分工.md` §三）负责 review。认领后在对应行标 `@端`。
> 配套：`版本规划.md`（验收标准 Must/Should）、`设计思路.md`（接口）、`平台适配与分工.md`（平台实现）。

---

## v0.1.0 上屏（透明浮窗 + emoji 立绘 + 底边行走）

- **mac端**：透明置顶浮窗(NSWindow+PySide6) / sensor_mac(鼠标·工作区 `NSScreen.visibleFrame`) / 托盘 / 路径平台化 / 单实例锁
- **win端**：**W1 Spike→**透明穿透窗 ✅结论：v0.1 窗口即宠物尺寸无穿透问题(全屏 mask 方案留 v0.3) / sensor_win ✅`pet/sensor_win.py`(ctypes 鼠标/空闲 + Qt 工作区，实测通过) / window_win ✅(薄继承 WindowBase) / 托盘 ✅(共享 TrayManager 实测) / 路径平台化 ✅(`%LOCALAPPDATA%\Desktop-Pet`) / 单实例锁 ✅(msvcrt+EnumWindows 唤醒，第二实例 exit 0 实测)
- **共享(认领)**：AssetProvider 双方法接口冻结 / `SpriteRef` / ActionType 定义 / BehaviorFSM 骨架(idle+底边走) / EmojiProvider / BubbleWidget 骨架 / config·logging_setup 地基 / platform.py 工厂 / app.py 入口+shutdown()+单实例锁 / mousePressEvent 等 hook 预留

---

## v0.2.0 能长大基础（养成 state + 情绪切换立绘）

- **mac端**：点击交互入口(单击摸头/双击喂食/右键菜单) / 养成通知气泡接入
- **win端**：点击交互入口(同手势消解) ✅@win端(共享 WindowBase 手势消解：单击延迟消歧/双击/右键菜单/拖拽候选 hook，QtTest 7/7 通过 `spikes/test_v02_interaction.py`) / 养成通知气泡接入 ✅@win端(app._interact: gain 改数值+气泡反馈+emoji 切换)
- **共享(认领)**：PetState+PetStateStore(含 on_change) / 数值自然衰减 / save/load(原子写+version migrate+.bak) / EmojiProvider 按 mood ✅@win端(fullness<20→HUNGRY 优先，含在测试脚本) / BehaviorFSM 受数值调制 / config 默认值(decay/gain/score 权重阈值)+jsonschema 校验 🔶@win端(先加 interaction_gain 默认值，decay/score/校验留 mac 主笔) / 单元测试 PetStateStore

---

## v0.3.0 桌面物理（拖拽抛掷 + 重力 + 跟随鼠标 + 随机游走）

- **mac端**：sensor_mac 补窗口枚举缓存(AXUIElement，事件驱动) / 全屏检测(NSWorkspace+CGWindowList)
- **win端**：sensor_win 补 EnumWindows 缓存 ✅@win端(TTL 2s+DWM cloaked 排除，QtTest 8/8 `spikes/test_v03_sensor_win.py`) / 高 DPI 多屏工作区 ✅@win端(窗口框物理→逻辑坐标按屏 DPR 换算；工作区沿用 Qt 逻辑系；跨屏不同缩放实现已备，多缩放率专项实测待补) / 全屏检测 ✅@win端(foreground_fullscreen()：前台窗口覆盖所在显示器即全屏+进程名供演示白名单；FSM 接线待共享层)
- **共享(认领)**：get_frames 实现 ✅@win端(行走2帧/动作3帧，emoji 占位) / BehaviorFSM 扩展 ✅@win端(五态+CLIMB 攀爬，v0.3.5-6：扫掠边线检测/图层双检查 Sensors.solid_at 增量字段/支撑校验/落地弹跳0.35×≤2次/走到窗壁即爬/全屏空中收敛地面，`test_v03_fsm.py` 33/33) / WANDER 算法 ✅@win端 / 拖拽抛物+触底停止 ✅@win端 / 窗口枚举不每帧(性能) ✅@win端(sensor TTL2s 缓存) / 边界测试(不穿屏不飞出) ✅@win端 / 全屏检测 FSM 接线 ✅@win端(adapter.is_fullscreen_active→隐藏+1s轮询；mac 端检测实现待补) / 已知限制：窗口被拖动时宠物不随窗移动(支撑消失即落)，真跟随需窗口身份追踪

---

## v0.4.0 能聊（DS + function calling + 1 工具）

- **mac端**：keyring(Keychain) 存 DS key / tools_mac `open_app`(`open -a`) 样板 / 危险确认 NSAlert
- **win端**：keyring(凭据管理器) ✅@win端(读回一致实测) / tools_win `open_app`(`os.startfile`) ✅@win端(参数校验单测) / 危险确认 Qt 对话框(文案对齐) ✅@win端(QMessageBox 继续/取消,v0.4 open_app 不触发)
- **共享(认领)**：DS 客户端+function calling+usage / 聊天面板 QML(流式打字机/markdown) / ToolRegistry+ToolContext+schema / system prompt(身份/性格/规则/注入防护) / 错误降级+离线模式 / 首次启动 key 引导输入 / DS 工具调用集成测试

---

## v0.5.0 进化（年龄→阶段→分支）

- **mac端**：进化事件可视化 ✅(v0.5.0 共享 app._on_evolve 气泡"我长大了")
- **win端**：进化事件可视化 ✅(同上，共享实现平台中立，win 实机冒烟通过)
- **共享(认领)**：年龄随时间增长(`age_speed_multiplier`+fast-mode) / 进化阈值事件 / 阶段切换 YOUNG→ADULT→FINAL / 加权养护分判定分支(HEALTHY/NEGLECTED) / EmojiProvider 按 (stage,branch,mood) / 进化分支持久化 / 重置机制(托盘→二次确认→清档重启) / 进化分支集成测试

---

## v0.6.0 主动关怀（链式唤醒 + 提醒 + 节日）

- **mac端**：空闲传感器(NSEvent/CGEvent) ✅@mac端(v0.6.1 sensor_mac.idle_seconds CGEventSourceSecondsSinceLastEventType 免权限 O(1))
- **win端**：空闲传感器(`GetLastInputInfo`) ✅@win端(v0.1 即实现 sensor_win.get_idle_seconds 并入 build_sensors，实测见 test_v03_sensor_win)
- **共享(认领)**：ProactiveScheduler(链式唤醒 10–360min+空闲/深夜检测+隔离上下文) ✅@win端主笔(`pet/proactive.py`：LLM隔离决策chat_once独立history/本地罐头兜底/唤醒钳制) / follow-up ✅@win端(30min 定时回访+聊天"去吃饭"启发式 on_user_message 钩子) / 提醒(久坐/喝水/护眼/吃饭) ✅@win端(空闲≥45min 轮换+30min 冷却) / 节日/生日/早安晚安 ✅@win端(内置节日表+config可扩展+早晚窗口每日一次) / 气泡 UI 接入 ✅@win端(app 30s 轮询+锚宠物气泡) / 应用未运行不触发(已知限制,v0.11 解)——测试 `spikes/test_v06_proactive.py` 20/20

---

## v0.7.0 吃鼠标（鼠标抑制 + 安全铁律 + 最小权限引导）

- **mac端**：**S1 Spike→** CGEventTap 抑制+看门狗解锁 / mouse_lock_mac / 强制吐出热键 `Cmd+Option+T` / Accessibility 最小引导(检测+深链系统设置)
- **win端**：**W2 Spike→** ✅@win端(钩子+钉光标+看门狗，SendInput 注入验证真抑制) / mouse_lock_win ✅@win端(`pet/mouse_lock_win.py`：专用线程 LL 钩子+消息循环/看门狗先启 0.25s 轮询钳 [0.3,15]/_release 幂等/启动栅栏防竞态/GetModuleHandle 修全局钩子) / 强制吐出热键 `Ctrl+Alt+T` ✅@win端(RegisterHotKey 钩子线程注册,WM_HOTKEY→force_spit,占用降级 warning) / UIPI 降级(管理员窗口只气泡) ✅@win端(_foreground_elevated: OpenProcess 被拒→start 返 False 走气泡) / 权限运行时自检 ✅@win端(is_accessibility_trusted 恒真+prompt no-op,win 无特权需求)；测试 `test_v07_eat_mouse_win.py` 11/11
- **共享(认领)**：EatMouseSession(start/force_spit/on_dnd_active) / 看门狗线程(5s 释放，平台只换释放实现) / BehaviorFSM 加 EAT_MOUSE 状态(ProactiveScheduler 发事件切换) / 安全六铁律 / idle gate(idle<5min 只气泡) / 活跃内容检测(视频白名单) / DND 白名单 / 养成回血(+饱食+心情) / 安全解锁集成测试

---

## v0.8.0 系统工具完整 + 危险操作拦截 + 权限向导完善

- **mac端**：tools_mac 补全(clipboard/mdfind/volume/process/lock/sleep) / 权限状态页(QML)+深链+聚焦刷新 / 权限永久拒绝处理
- **win端**：tools_win 补全 ✅@win端(v0.8.1：clipboard=clip/Get-Clipboard、file_search=Get-ChildItem 限用户目录防全盘扫、volume=ctypes 直调 CoreAudio IAudioEndpointVolume(免PS嵌C#,set/get/mute,S_FALSE容错)、process=taskkill(危险)、sleep=SetSuspendState(危险)；实机闭环验证 剪贴板回读/音量30%设读回/搜索/非法参数拒) / 权限运行时自检页(QML) ✅@win端(pet/ui/perm.qml+perm_bridge：LL钩子/热键占用/剪贴板/音量COM/数据目录/DS key 六项,右键设置唤出+聚焦刷新,六项实检全绿)
- **共享(认领)**：ToolRegistry 危险拦截+参数黑名单(禁 `/`·`~`根·`rm -rf`)+文件数上限 / 危险确认对话框双端一致(按钮文案+显示内容) / 单元测试 ToolRegistry

---

## v0.9.0 长期记忆（+ 快捷启动器）

- **mac端**：拖拽文件/文件夹给它打开(Qt 拖放)
- **win端**：拖拽文件/文件夹给它打开(Qt 拖放) ✅@win端(WindowBase dragEnter/dropEvent+fileDropped signal→adapter.open_path(os.startfile)；修 mac/win open_path 插反；QtTest 拖放事件 3/3)
- **共享(认领)**：memory.py ✅@win端主笔(`pet/memory.py`：冻结四方法+load/save 原子写.bak+重置保留记忆；TF-IDF(中文2-gram)×0.5+importance×0.3+近期×0.2 打分；0.995^天衰减+召回回血+<0.1 删+500 上限挤占) / 注入 prompt ✅@win端(memory_tools：DS 工具 memory_save/search+set_memory_context 每消息前刷新) / 滚屏摘要 ✅@win端(chat_bridge >20轮→DS 压最老10轮,失败保留) / 记忆管理 UI(QML：查看/删除/清空) ✅@win端(mem.qml+mem_bridge,托盘'记忆管理'唤出,删/清即落盘) / 单元测试 memory.py ✅@win端(`test_v09_memory.py` 27/27)

---

## v0.10.0 AI 立绘 provider（正式接入）

- **mac端**：无
- **win端**：无
- **共享(认领)**：基于 S0 Spike 选型 ✅(ComfyUI+Animagine XL 3.1,交接文档§三主锚定层级派生) / AIArtProvider get_static ✅@win端(`assets/ai/{stage}_{branch}_{mood}.png` 30张,miss_cache 防重复 stat,降级 EmojiProvider 不崩；skin 非 default 加后缀) / get_frames 降级返回 emoji 帧 ✅@win端 / config 切 provider(emoji/ai/commission) ✅@win端(app._make_provider 工厂,idle_fn/sleepy 透传) / 养成·物理·动画零改动验证 ✅@win端(git diff behavior.py 0 行差异,emoji/ai 双模式冒烟通过)

---

## v0.11.0 全局热键 + 开机自启

- **mac端**：pyobjc 全局热键(`Cmd+Option+P` 唤聊天) / 开机自启(LaunchAgents plist)
- **win端**：`RegisterHotKey` 全局热键(`Ctrl+Alt+P` 唤聊天 / `Ctrl+Alt+T` 吐出) / 开机自启(注册表 Run)
- **共享(认领)**：hotkey 统一管理+热键冲突检测 ✅@win端(platform adapter start/stop_hotkeys 统一接口;config hotkeys 段可自定义;冲突→WARNING气泡) / 自启逻辑 ✅@win端(adapter set/is_autostart_enabled,托盘切换) / 解 v0.6 主动关怀常驻限制 ✅(自启开启后宠物常驻→主动关怀完整)

---

## v0.12.0 打包

- **mac端**：py2app `.app` / bundle 内 assets+QML 路径 / 签名公证(可选) / 安装卸载+首次权限文档
- **win端**：PyInstaller `.exe` / bundle 内 assets+QML 路径 / 签名或报毒说明 / 安装卸载文档
- **共享(认领)**：**S2 Spike 结论落地** / 打包脚本 / 资源路径处理统一 / 权限申请流程实测

---

## v1.0.0 发布

整合 v0.1–v0.12，全量回归 + 性能基准复核 + 24h 浸泡测试最终通过，打 tag `v1.0.0`。

---

## v0.13.0 分层绑骨渲染器（丝滑展示层）

- **mac端**：待适配（polish 与回归；win 先行交付）
- **win端**：P0 渲染载体 spike ✅@win端(`spikes/spike_rig_qtquick.py`：offscreen 软件渲染 4/4 + 实机 D3D11 合成目检；坑留档——QQuickWidget 默认 SizeViewToRootObject 遇无尺寸根 Item 缩成 0×0) / 拆件工具与资产 ✅@win端(`tools/split_parts.py` α 连通域+屏障确定性切割，final/neglected 尾件+派生核心图入库；healthy 姿态裙摆宽幅屏障失效记已知限制) / RigWindow+场景+装配 ✅@win端(降级铁律三级收敛 build_rig_window；presentation 配置键；is_playing 收口私有读)
- **共享(认领)**：AssetProvider/FSM 接口零破坏 ✅(behavior 增公开 velocity 只读、window 增 is_playing/set_motion_params no-op 缺省，v02-v11 十三套 286 检查全绿佐证) / 测试 `spikes/test_v13_rig.py` ✅@win端(28/28：spec 降级矩阵、装配回退、淡化序列簿记、facing/motion 钩子、emoji 降级可见性)。已知限制：桌面截图管线在本机出现冻结假象（时钟不跳），实机观感请用户以 `python spikes/spike_rig_live_demo.py` 自行复核

## v0.13.1 healthy 尾件补绘拆切

- **win端**：gpt-image-2 产线闭环 ✅@win端(`tools/gen_fill.py` probe/fill/patch 三模式,密钥只走 env) / healthy 尾件交付 ✅@win端(edits 掩码移除尾→差分足迹闭运算→区域回贴出派生核心"构造性区域外=原图";edits 无掩码抽尾→删除足迹对位重摆(s=0.3203)→parts+manifest;静置合成 IoU=0.9616) / 关键发现 ✅留痕(**quanzil 中转不遵守掩码保真**——带外仍有 9.4% 像素被重渲染,整图采用=v0.10.19 漂移形态,必须区域回贴) / 几何算法两课留痕(stem 判据须"先宽后薄的长段"防鳍尖误触;闭运算勿与膨胀自相交,先重膨胀再填洞) / QA 全套 `spikes/_qa/`(补绘zoom/抽尾件/对齐脚本/三联对照)

## v0.13.2 摆尾连带裙角修复

- **win端**：部件冗余遮蔽裁除 ✅@win端(`spikes/_qa/trim_part_overlap.py`：部件画布位姿上凡落在"核心α>0膨胀1px"内的像素清零——静置时本被核心覆盖不可见、旋转时滑出成"裙角/蕾丝随尾摆"幽灵;healthy 裁 5020px(白蕾丝角距转轴160-265px 为元凶)/neglected 裁 6794px(裙料条+金绣+发丝)) / bbox 边框 1px 清零 ✅@win端(裁切矩形边缘残留渲染成悬浮亮框) / 参数依据 ✅留痕(遮蔽膨胀余量 3px→1px:过大加剧旋离侧凹口;±5° 超幅模拟两分支六姿态 QA 全净 `sway_sim2.png`,旋离侧细缝为"尾离开露重建裙沿"物理正确形态) / 核心重建流程幂等化 ✅(split_parts/align_tail 可重复跑,manifest 按 id 去重)

## v0.13.3 rig 模式聊天面板载入失败修复（引擎次序）

- **win端**：defer_quick ✅@win端(实机日志复现：rig 开启即 `QML 聊天面板载入失败`——RigWindow 的 QQuickWidget 在窗口创建期抢建全进程**首个 QML 引擎**，违反 app.py:349 既有约束"mem/perm/chat singleton 必须注册于首个引擎前(PySide6 6.10)") / 修法 ✅@win端(`build_rig_window(defer_quick=True)`：QQuickWidget 延至事件循环首拍创建——`_setup_chat` 在 `__init__` 内同步完成三个 singleton 注册与聊天引擎建立,次序恢复"注册在引擎前";延迟失败仍自动降级基类,`_rig_pending` 构造期待就绪标记) / 回归锁 ✅@win端(test_v13 T10a/b/c：pending→首拍就绪→直显翻译照常;31/31) / 实机验证 ✅@win端(rig 模式启动日志:`rig 后端就绪:2 figures / 2 parts` 且聊天面板零错误)

## v0.13.4 实机物理交互失效修复（QQuickWidget 吞原生鼠标）

- **win端**：定位 ✅@win端(用户报抛掷/反弹/站小窗全灭。排除法:pet.log 零异常+stderr 空+Win32 三采样窗口自行位移 ⇒ FSM/集成层活;离屏完整 PetApp 复现 rig 模式游走/拖拽钉住/全正常 ⇒ 逻辑层活;锁定实机输入链) / 根因 ✅留痕(**QQuickWidget 在实机 D3D11 下吞原生 WM_LBUTTONDOWN**,盖满全窗 ⇒ WindowBase 手势/拖拽/右键全收不到 ⇒ 抓不住=抛掷/站窗/反弹全不可测;低 mood(mood 8.8 钩膝瘫坐)叠加"物理死了"观感) / **mock 盲区教训 ✅留痕**(离屏 QTest"通过"是假象——QTest 把事件直接投给目标控件,绕过 OS 命中分发,恰好测不出此类 bug;真实验证必须 SendInput 打实机窗口) / 修法 ✅@win端(`WA_TransparentForMouseEvents` 于 QQuickWidget:场景无交互 QML,鼠标全数让给 WindowBase 手势层) / 实机验证 ✅@win端(SendInput 真实拖拽:Y 轴精确跟手(603=光标-160)+抛掷弧 383→682→699 重力落地;第二轮向左拖 X 跟随并撞左边界钳制;期间宠物自行横穿屏幕 1387→152 佐证自主行为) / 排查副产物 ✅留痕(**DPI 虚拟化坑**: unaware 进程 GetWindowRect/SetCursorPos 均为逻辑系=物理÷1.5,跨进程采样须换算,否则"窗口不动/拖不中"全是假象)

## v0.13.5 行走步态连续性（中间步姿 + 硬切策略）

- **win端**：播放策略 ✅@win端(大位移动作 walk/chew/eat_mouse **硬切**、表情/小动作保持淡化——步姿两帧间腿部位移大,淡化=双影糊("看不出迈步"的用户实测根因);判定按帧名 `_walk_/_chew_/_eat_mouse_` 子串,首版 startswith 被阶段前缀骗过被 T5b-4 当场抓获) / 中间步姿产线 ✅@win端(`tools/gen_walk_mids.py`:gpt-image-2 edits 双参考生成 walk_0b(0→1 过渡)/walk_1b(1→0 过渡),等比钳幅±20%+底对齐+水平居中门禁防走动垂直跳动;四帧步态环 [w0,0b,w1,1b] QA 条带 `walk_cycle_*.png` 风格/基线一致) / 帧规格扩展 ✅@win端(_FRAME_SPECS walk 收录可选 _b 帧,frames_for 缺文件自动跳过 ⇒ 未产帧阶段维持 2 帧行为;frame_interval 增 stage 参数按实际帧数均分 400ms 步态周期,4 帧=100ms/帧) / 回归 ✅@win端(test_v13 33/33 含 T5a-2 步态环顺序锁/T5b-4 硬切策略锁;test_v10 25/25 不变,young 无 _b 帧不受扰)

## v0.13.6 "多手多脚"残影修复（硬切单槽直换）+ 步速放缓

- **win端**：根因 ✅留痕(用户实测:闪太快+"多手多脚"。硬切虽然 fade=0,但仍走双槽叠放结构——A 前景槽**恒不透明**且 B 以 mix=1 叠上,新帧迈步扫开的透明腿档把旧帧肢体透出来=残影;100ms/帧放大频闪) / 修法 ✅@win端(`_transition_to` fade_ms<=0 分支改为**单槽直换**:_canonicalize 后直接写 figASrc、B 槽保持空、mix=0,任意时刻场景只有一帧像素) / 步速 ✅@win端(walk 4 帧 100ms→**140ms/帧**(560ms 周期),100ms 大位差频闪观感差) / 回归锁 ✅@win端(T5c-2:硬切后必须 BSrc 空且 mix=0 的单槽语义锁;34/34) / 步姿角度评价 ✅留痕(腿部放大对照 legs_zoom.png:四帧腿位本身合理,"角度不合适"观感主判为频闪+残影支配,修复后待用户复验再定是否重生成)

## v0.13.7 八帧步态环（用户复验:4 帧仍卡顿/角度不均匀）

- **win端**：细分产线 ✅@win端(gen_walk_mids.py `--subdivide`:对现有环每区间取中点——m1(0↔0b)/m2(0b↔1)/m3(1↔1b)/m4(1b↔0),提示词加"四肢角度严格取两参考帧中间值+仅一手脚"约束;对齐门禁升级 align_pair(高度=两参考均值钳幅±15%,底对齐取双参考低点,水平居中取均值)) / 生成事故与防线 ✅留痕(m1 首版把"透明背景"画成实体棋盘格——脚本新增透明占比体检(α0<15% 判背景实体化,自动重试至 3 次)+`--only` 单帧重生成;重造后干净) / 帧规格 ✅@win端(_FRAME_SPECS walk 8 名单;frame_interval 表驱动 {2:200,4:140,8:90}) / 回归锁 ✅@win端(T5a-2 升级八帧顺序锁;T5c 改确定性断言"前景已离开 walk_0"(90ms 帧距下旧 240ms 窗口存在推进数竞态);34/34) / QA ✅@win端(walk_cycle_8f.png 八帧全环+w0 收口,风格/基线/腿摆细腻连贯)
