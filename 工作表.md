# 桌宠 工作表

> 每个小版本的双端任务清单。**mac端/win端任务各自负责；共享任务不预先指派，认领实现**，该模块主笔（见 `平台适配与分工.md` §三）负责 review。认领后在对应行标 `@端`。
> 配套：`版本规划.md`（验收标准 Must/Should）、`设计思路.md`（接口）、`平台适配与分工.md`（平台实现）。

---

## v0.1.0 上屏（透明浮窗 + emoji 立绘 + 底边行走）

- **mac端**：透明置顶浮窗(NSWindow+PySide6) / sensor_mac(鼠标·工作区 `NSScreen.visibleFrame`) / 托盘 / 路径平台化 / 单实例锁
- **win端**：**W1 Spike→**透明穿透窗 ✅结论：v0.1 窗口即宠物尺寸无穿透问题(全屏 mask 方案留 v0.3) / sensor_win ✅`pet/sensor_win.py`(ctypes 鼠标/空闲 + Qt 工作区，实测通过) / window_win ✅(薄继承 WindowBase) / 托盘 ✅(共享 TrayManager 实测) / 路径平台化 ✅(`%LOCALAPPDATA%\Desktop-Pet`) / 单实例锁 ✅(msvcrt+EnumWindows 唤醒，第二实例 exit 0 实测)
- **共享(认领)**：AssetProvider 双方法接口冻结 / `SpriteRef` / ActionType 定义 / BehaviorFSM 骨架(idle+底边走) / EmojiProvider / BubbleWidget 骨架 / config·logging_setup 地基 / platform.py 工厂 / app.py 入口+shutdown()+单实例锁 / mousePressEvent 等 hook 预留

---

## v0.2.0 能长大基础（养成 state + 情绪切换立绘）

- **mac端**：点击交互入口(单击摸头/双击喂食/右键菜单) / 养成通知气泡接入
- **win端**：点击交互入口(同手势消解) ✅@win端(共享 WindowBase 手势消解：单击延迟消歧/双击/右键菜单/拖拽候选 hook，QtTest 7/7 通过 `spikes/test_v02_interaction.py`) / 养成通知气泡接入 ✅@win端(app._interact: gain 改数值+气泡反馈+emoji 切换)
- **共享(认领)**：PetState+PetStateStore(含 on_change) / 数值自然衰减 / save/load(原子写+version migrate+.bak) / EmojiProvider 按 mood ✅@win端(fullness<20→HUNGRY 优先，含在测试脚本) / BehaviorFSM 受数值调制 / config 默认值(decay/gain/score 权重阈值)+jsonschema 校验 🔶@win端(先加 interaction_gain 默认值，decay/score/校验留 mac 主笔) / 单元测试 PetStateStore

---

## v0.3.0 桌面物理（拖拽抛掷 + 重力 + 跟随鼠标 + 随机游走）

- **mac端**：sensor_mac 补窗口枚举缓存(AXUIElement，事件驱动) / 全屏检测(NSWorkspace+CGWindowList)
- **win端**：sensor_win 补 EnumWindows 缓存 ✅@win端(TTL 2s+DWM cloaked 排除，QtTest 8/8 `spikes/test_v03_sensor_win.py`) / 高 DPI 多屏工作区 ✅@win端(窗口框物理→逻辑坐标按屏 DPR 换算；工作区沿用 Qt 逻辑系；跨屏不同缩放实现已备，多缩放率专项实测待补) / 全屏检测 ✅@win端(foreground_fullscreen()：前台窗口覆盖所在显示器即全屏+进程名供演示白名单；FSM 接线待共享层)
- **共享(认领)**：get_frames 实现 ✅@win端(行走2帧/动作3帧，emoji 占位) / BehaviorFSM 扩展 ✅@win端(五态+CLIMB 攀爬，v0.3.5-6：扫掠边线检测/图层双检查 Sensors.solid_at 增量字段/支撑校验/落地弹跳0.35×≤2次/走到窗壁即爬/全屏空中收敛地面，`test_v03_fsm.py` 33/33) / WANDER 算法 ✅@win端 / 拖拽抛物+触底停止 ✅@win端 / 窗口枚举不每帧(性能) ✅@win端(sensor TTL2s 缓存) / 边界测试(不穿屏不飞出) ✅@win端 / 全屏检测 FSM 接线 ✅@win端(adapter.is_fullscreen_active→隐藏+1s轮询；mac 端检测实现待补) / 已知限制：窗口被拖动时宠物不随窗移动(支撑消失即落)，真跟随需窗口身份追踪

---

## v0.4.0 能聊（DS + function calling + 1 工具）

- **mac端**：keyring(Keychain) 存 DS key / tools_mac `open_app`(`open -a`) 样板 / 危险确认 NSAlert
- **win端**：keyring(凭据管理器) ✅@win端(读回一致实测) / tools_win `open_app`(`os.startfile`) ✅@win端(参数校验单测) / 危险确认 Qt 对话框(文案对齐) ✅@win端(QMessageBox 继续/取消,v0.4 open_app 不触发)
- **共享(认领)**：DS 客户端+function calling+usage / 聊天面板 QML(流式打字机/markdown) / ToolRegistry+ToolContext+schema / system prompt(身份/性格/规则/注入防护) / 错误降级+离线模式 / 首次启动 key 引导输入 / DS 工具调用集成测试

---

## v0.5.0 进化（年龄→阶段→分支）

- **mac端**：进化事件可视化
- **win端**：进化事件可视化
- **共享(认领)**：年龄随时间增长(`age_speed_multiplier`+fast-mode) / 进化阈值事件 / 阶段切换 YOUNG→ADULT→FINAL / 加权养护分判定分支(HEALTHY/NEGLECTED) / EmojiProvider 按 (stage,branch,mood) / 进化分支持久化 / 重置机制(托盘→二次确认→清档重启) / 进化分支集成测试

---

## v0.6.0 主动关怀（链式唤醒 + 提醒 + 节日）

- **mac端**：空闲传感器(NSEvent/CGEvent)
- **win端**：空闲传感器(`GetLastInputInfo`)
- **共享(认领)**：ProactiveScheduler(链式唤醒 10–360min+空闲/深夜检测+隔离上下文) / follow-up / 提醒(久坐/喝水/护眼/吃饭) / 节日/生日/早安晚安 / 气泡 UI 接入 / 应用未运行不触发(已知限制,v0.11 解)

---

## v0.7.0 吃鼠标（鼠标抑制 + 安全铁律 + 最小权限引导）

- **mac端**：**S1 Spike→** CGEventTap 抑制+看门狗解锁 / mouse_lock_mac / 强制吐出热键 `Cmd+Option+T` / Accessibility 最小引导(检测+深链系统设置)
- **win端**：**W2 Spike→** `WH_MOUSE_LL` 钩子+钉光标+看门狗 / mouse_lock_win / 强制吐出热键 `Ctrl+Alt+T` / UIPI 降级(管理员窗口只气泡) / 权限运行时自检
- **共享(认领)**：EatMouseSession(start/force_spit/on_dnd_active) / 看门狗线程(5s 释放，平台只换释放实现) / BehaviorFSM 加 EAT_MOUSE 状态(ProactiveScheduler 发事件切换) / 安全六铁律 / idle gate(idle<5min 只气泡) / 活跃内容检测(视频白名单) / DND 白名单 / 养成回血(+饱食+心情) / 安全解锁集成测试

---

## v0.8.0 系统工具完整 + 危险操作拦截 + 权限向导完善

- **mac端**：tools_mac 补全(clipboard/mdfind/volume/process/lock/sleep) / 权限状态页(QML)+深链+聚焦刷新 / 权限永久拒绝处理
- **win端**：tools_win 补全(clip/Windows搜索·Everything/PowerShell音量/taskkill/睡眠) / 权限运行时自检页(QML)
- **共享(认领)**：ToolRegistry 危险拦截+参数黑名单(禁 `/`·`~`根·`rm -rf`)+文件数上限 / 危险确认对话框双端一致(按钮文案+显示内容) / 单元测试 ToolRegistry

---

## v0.9.0 长期记忆（+ 快捷启动器）

- **mac端**：拖拽文件/文件夹给它打开(Qt 拖放)
- **win端**：拖拽文件/文件夹给它打开(Qt 拖放)
- **共享(认领)**：memory.py(memorize/recall/forget+重要性+遗忘+滚屏摘要+注入 prompt) / 记忆管理 UI(QML：查看/删除/清空) / 单元测试 memory.py

---

## v0.10.0 AI 立绘 provider（正式接入）

- **mac端**：无
- **win端**：无
- **共享(认领)**：基于 S0 Spike 选型 / AIArtProvider get_static(512×512+固定prompt+固定seed+rembg 去背+缓存) / get_frames 降级返回 emoji 帧 / config 切 provider(emoji/ai/commission) / 养成·物理·动画零改动验证

---

## v0.11.0 全局热键 + 开机自启

- **mac端**：pyobjc 全局热键(`Cmd+Option+P` 唤聊天) / 开机自启(LaunchAgents plist)
- **win端**：`RegisterHotKey` 全局热键(`Ctrl+Alt+P` 唤聊天 / `Ctrl+Alt+T` 吐出) / 开机自启(注册表 Run)
- **共享(认领)**：hotkey 统一管理+热键冲突检测(注册失败→气泡提示改键) / 自启逻辑 / 解 v0.6 主动关怀常驻限制

---

## v0.12.0 打包

- **mac端**：py2app `.app` / bundle 内 assets+QML 路径 / 签名公证(可选) / 安装卸载+首次权限文档
- **win端**：PyInstaller `.exe` / bundle 内 assets+QML 路径 / 签名或报毒说明 / 安装卸载文档
- **共享(认领)**：**S2 Spike 结论落地** / 打包脚本 / 资源路径处理统一 / 权限申请流程实测

---

## v1.0.0 发布

整合 v0.1–v0.12，全量回归 + 性能基准复核 + 24h 浸泡测试最终通过，打 tag `v1.0.0`。
