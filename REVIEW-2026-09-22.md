# v0.16.0 视觉实现审查与修复

审查提交：`35e9f0f30b4fd59df179c94e236c4d33b14d5fd9`。

本次修改已落在工作区，未创建新提交。运行时采用 20 个图层、47 根骨骼，网格为 3,145 顶点、5,156 三角形。原始生成图、reference spec 和历史备份保留；运行时规范为 `assets/rig_young/spec.json`。

## 问题与修复

| 问题 | 根因与修复 |
| --- | --- |
| 蒙皮权重没有实际混合 | 解析器只填权重，骨骼索引全部停在绑定骨；改为按骨名建立列映射，支持顶点间影响骨排序不同、未知骨回退，并按拓扑序计算 FK。 |
| 动态拖影、错误的软件后端支持判断 | `int(Flag)` 失败被误当作脏标记失效，旧树延迟 80ms 释放造成多帧叠挂。改为持久 `QSGGeometryNode` 和标准脏标记。显式持有 AttributeSet，避免其回收导致原生崩溃；场景节点资源只在渲染线程释放。 |
| 成年/最终体仍显示幼年体 | 默认蒙皮资产探测未限制阶段；现在仅 young 探测幼年资产。 |
| 表情和动作被蒙皮遮住 | 蒙皮只接管 healthy_neutral 静态立绘，表情、动作序列和软件后端恢复原分层/帧路径；坏资产也回退。 |
| 接缝漏底、发梢被裁 | 稀疏轮廓采样和按三角形重心筛选会漏掉有效像素。改为覆盖 alpha 的网格单元，保留抗锯齿边缘。使用已有补全额头的面部底图，锁定刘海上部权重；清除刘海层里的两块呆毛残片。 |
| 腮红颤动、眨眼不闭合 | 眼睑层混入腮红和嘴角，且与脸部不同步。换用干净睫毛；脸部绑定 head；眼白、虹膜和睫毛共享闭合变形，保留闭眼笔画，时间读取 spec。 |
| 瞳孔移出眼白 | 在固定眼白椭圆内移动瞳孔 UV，闭眼时隐藏虹膜，保持椭圆限幅。 |
| 尾巴断口、色差与头发残片 | 重生成连续整尾，仍由四节骨链驱动，移除分段交界。 |
| 领结/腰带被遮、右脚偏高 | 校正脸、衣身、裙摆、手臂的绘制顺序；右脚及关节一起移动 (-10,+32) 源图像素。 |
| bodyY 不生效 | `anchors.fill` 与 y 偏移冲突，bob 容器改为显式宽高。 |

Qt 依据：[QSGGeometry 属性生命周期](https://doc.qt.io/qtforpython-6/PySide6/QtQuick/QSGGeometry.html)、[官方自定义几何示例](https://doc.qt.io/qtforpython-6/examples/example_quick_scenegraph_scenegraph_customgeometry.html)。

## 素材与复现

内置 image_gen 生成整尾和左右睫毛，保留原始 RGBA 文件，通过 spec 的 `target_bbox_px` 配准。内置额度耗尽后，通知用户并使用已授权的本地 Qwen Image 2.1 修补眉部。只将两块眉部区域合回已有脸图，原 alpha、其余区域保持不变。

- 新素材：`assets/rig_young/layers/tail_continuous.png`、`eyelid_l_clean.png`、`eyelid_r_clean.png`、`face_base_clean.png`。
- 完整提示词与 Qwen 输入参数：`assets/rig_young/repair_v016/prompts.json`、`qwen_request.json`。
- 本地模型：`qwen_image_2.1_int8_convrot.safetensors`、`qwen3vl_8b_w4a8.safetensors`、`qwen_image_2.1_vae_bf16.safetensors`；25 steps，Euler/simple，seed 20260922。
- 面部补丁复现：`python tools/patch_young_face.py assets/rig_young/repair_v016/face_qwen_crop.png`。
- 网格复现：`python tools/mesh_generator.py`。

## 验证

本机 Windows / PySide6 6.9.2：

- `test_skinned_regressions.py`：8 项通过（骨名权重混排、未知骨回退、拓扑序、覆盖率、额头/嘴部不随眨眼变形、眼白裁剪、阶段与 timing）。
- `test_skinned_mesh_rig.py`：22 项通过，截图检查实际 alpha 像素，不再只检查图像对象非空。
- `spike_skin_mesh_smoke.py`：20 项通过；软件后端验证分层位图回退。
- `test_v13_rig.py`：51 项通过；`test_v14_paperdoll.py`：35 项通过；`test_motion_engine.py`：32 项通过。
- `qa_skinned_visual.py --backend d3d11`：真实 Direct3D11 渲染，验证闭眼无虹膜/面部空洞、动作与阶段切换、损坏资产回退、归零后逐像素回到初始帧、46 帧连续播放期间顶点缓冲保持同一对象。

验收截图及动图：`spikes/_qa/v016_review/`（`rest.png`、`blink.png`、`walk.png`、`motion.gif`）。

验证范围：本次实机图形检查仅覆盖 Windows D3D11 和软件回退；未在 macOS/Metal 或 requirements 声明的 PySide6 6.5–6.7 上复测。生成素材已做合层验收，但不宣称与参考原画逐像素一致。完整桌宠业务流程未做长时浸泡测试。

## 用户复核后的第二轮修正

- 实际 Windows 存档为 final、age=3040.1255；已先备份为 pet_state.json.before-young-20260922，再仅将 stage 改为 young、age 清零并刷新 last_update。其余养成数值及分支保留，正常成长仍启用。此项为本机存档修正，不是全局启动时强制重置。
- 内置 image_gen 直接参考 assets/reference/young_ref.jpg 重出左上睫毛与右鞋，分别保存为 layers/eyelid_l_ref_v2.png、layers/leg_r_ref_v2.png。完整提示词保存于 repair_v016/*ref_v2_prompt.txt。生成 RGBA 保持原样，通过 spec 配准及重新生成网格接入。
- 左眼线提高并去掉旧图的长尖角，右鞋改成正面圆头，配准宽 110 px、底部 y=1166，与左鞋的宽度及落脚高度对齐。
- 重跑 8 项蒙皮回归及 D3D11 静止、眨眼、行走、连续动画检查，全部通过。当前网格 3215 顶点、5282 三角形。动图已更新。

## 真实运行与预览不一致：启动集成修复

实际配置来自 Windows 用户数据目录，不是仓库根目录 config.json。用户配置原为 paperdoll，存档虽已变 young，分支仍是 neglected、养成值为零，因此不会选择仅支持 healthy_neutral 的新蒙皮。paperdoll 行走还会覆盖为旧侧身立绘。先前独立 RigWindow 动图没有覆盖这些应用层选择。

用户明确选择恢复健康幼年后，已备份实际配置和存档，设置 presentation=rig、young/healthy、age=0、三项养成值=80。修复 app 启动未立即应用中性表情的问题：即使衰减不足通知阈值，启动也主动把当前 state 送入窗口。新增 skinned_motion_active 能力检查，rig 蒙皮可见时，walk/blink 不再被仅针对 paperdoll 的门禁遗漏而切换至旧帧。其他形态、软件后端与特殊动作继续使用既有回退。

新增 spikes/qa_app_visual.py，通过真实 PetApp、已安装配置、存档副本及延迟 QQuick 初始化验证；仅隔离保存位置并禁用聊天连接及全局热键。实测输出 rig/young/healthy/healthy_neutral/mesh=true/Direct3D11。覆盖启动、行走、眨眼、特殊动作回退及恢复，截图位于 spikes/_qa/v016_review/app/。另重跑 35 项 paperdoll 与 8 项蒙皮回归全部通过。
