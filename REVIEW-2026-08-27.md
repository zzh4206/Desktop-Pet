# 桌宠仓库静态只读 Review 记录（2026-08-27）+ 四批清偿

> 日期：2026-08-27；HEAD 起点 `6aef3e0`（v0.10.19），清偿终点 `v0.10.23`
> 范围：全量源码通读（app.py + pet/ 26 文件 + ui/ + spikes/ 14 + tools/ + config/docs）+ 全部回归套件实跑
> 方式：只读审查先行（不启动 GUI），随后按建议顺序四批修复，每批实跑回归后独立提交
> 上游：`REVIEW-2026-08-25.md`（其 H1-H4 已在该轮落地，本轮核验属实并清偿其 M/L 遗留）

---

## 一、本轮审查新发现

### N1 🟡 v09 记忆套件 T11 双红（28/30）——H4 修复提交自身打破且未察觉
- `test_v09_memory.py:158-161` × `chat_bridge.py`。H4（5a25248）把滚屏摘要改后台 QThread + `system_override/tools_override` 关键字调用，测试仍同步断言 + 假客户端旧签名（必 TypeError 走失败路径）。
- worktree 实跑对照：父提交 b933363 为 30/30 绿，5a25248 当场 28/2——提交信息未提。"失败保留原文"断言此前只是碰巧通过（未等 failed 信号）。
- **已修 v0.10.20**：假客户端补 `**kw` + `QTest.qWait` 等 worker；失败路径变为真实验证。30/30。

### N2 🟡 v10 动画套件 T6 红（16/17）——缓存键加 mtime 后断言未跟
- `test_v10_animation.py:127`：断言仍比 4 元组键，bded91b（v0.10.18b）键已是 5 元组。LRU 行为本身正确，纯断言过期。
- **已修 v0.10.20**：改比 5 元组。17/17。

### N3 🟢 文档/注释漂移
- `EatMouseSession.start` docstring 与 v0.7.4 两段式实现不符（已修 v0.10.21）；版本号三处漂移 + platform.py 六处幻影"v0.12.1"（已修 v0.10.23，单一源 `pet/__init__.__version__`）。

### N4 🔴（修复中实测发现）QThread 销毁竞态家族
- **done 信号送达主线程时 worker 线程可能仍在收尾**；此刻 Python 最后引用被丢弃 → shiboken 立即销毁运行中的 QThread → 无栈段错误。新增权限页异步测试 5 连发 3 崩复现；v04 聊天套件历史 ~30% 概率退出崩同根因（对照组 v0.10.21 亦崩，非本轮引入）。
- **已修 v0.10.22 根治**：五处 worker（perm×2 / proactive 汇报 / ChatWorker / 摘要）统一 **QObject parent + `finished→deleteLater` 单通道删除**；v04 替身 `start()` 改 no-op（替身本不应真起线程）。压力验证：探针 10/10、权限测试 6/6、v04 6/6 全净。

---

## 二、REVIEW-2026-08-25 遗留清偿（四批提交）

| 批 | 提交 | 内容 | 实跑 |
|---|---|---|---|
| 一 | v0.10.20 | N1/N2 测试断言修复 | v09 30/30 + v10 17/17 |
| 二 | v0.10.21 | M4 吃鼠标抑制失败补发 eat_mouse_off（FSM 不再冻死）；M5 proactive 独立 LLM 客户端（三 worker 各持实例）；M6 shutdown 收口决策线程（+超时保引用）；M11 hotkey `_ready/_reg_ok` 前置创建；M12 注册冲突走 bridge 信号；N3 docstring | v06 28/28 + v07 18/18 + v07win 22/22 + v11 13/13 + v04 13/13 + 热键真注册冒烟 |
| 三 | v0.10.22 | M1 动画中 on_change 只更新恢复目标（每秒闪静帧消除）；L4 动画切换恢复目标；M7 win 白名单 exe 归一化比对（+example 补 exe 形态）；M8 权限自检后台线程（win/mac）；M9 frames_for 缓存+防御拷贝；N4 竞态根治 | 全套 13 件（见下） |
| 四 | v0.10.23 | M10 schema 补 eat_mouse_hotkey_label；L1 死 `_follow` 分支；L6 自启失败回滚托盘；L10 黑名单 text_fields 豁免；L12 MemoryStore RLock；L13 user32 use_last_error；L14 tray 注释；L15 删 FOLLOW_CURSOR 死枚举（设计思路 §2.2 同步留痕）；L16 ds_key 去重；L17 冗余 import/noqa；L18 多余 pass；L2 版本单一源；T5 模块树对齐；T11 `*.png binary` | 全量回归（见下） |

**v0.10.22 全量实跑**：v02 13/13 · v03 66/66 · v03s 10/10 · v04 13/13（6/6 净）· v05 16/16 · v06 28/28 · v07 18/18 · v07win 28/28 · v08 18/18 · v08perm 4/4（新文件，6/6 净）· v09 30/30 · v10 25/25 · v11 13/13

---

## 三、仍未清项（移交下轮）

| # | 内容 | 备注 |
|---|---|---|
| L3 | window.set_sprite 加载失败路径残留（isfile-False 时把路径当文本画） | null 哨兵已入缓存（08-25 批），剩降级兜底 |
| L5 | `_setup_hotkeys` ImportError 分支 `_hk_hint` 理论未定义 | 实际不可达（import 失败必 ok=False 走 warning），留防御性提前定义 |
| L7 | `_FRAME_SPECS["eat_mouse"]`（12 张素材）零调用方 | 涉及功能决策（吃鼠标专用帧序是否接入），非纯代码修 |
| L11 | mouse_lock_win.start 1s 就绪等待超时仍置 active | 有 10s 看门狗兜底 |
| T3 | v07win `FakeLockInstance` 惰性求值写法（G1 `if False else`） | 重写单一表达式 |
| T4 | spikes 副作用警示（真装钩子/弹资源管理器）缺失于 README | 文档 |
| T6 | tools/postprocess_assets.py 整体过期（rembg 管线已废弃、尺寸 64/96/128 vs 实际 192/256/320） | 标注废弃或重写为 BiRefNet 现状说明 |
| T7 | 设计思路 §六仍写"get_frames 降级 emoji（AI 仅静态）"（v0.10.15 已实装帧动画）；psutil 幽灵依赖 | 文档 |
| T8 | 工作表.md 无 v0.10.1-v0.10.23 台账 | 补记账 |
| T9 | README 未提 AI 立绘/帧动画与 `provider` 配置键 | 文档 |
| T10 | config.example `llm.last_provider` 无消费、`behavior.pet_height_px` 启动即被覆盖 | 删或实现 |
| T11 | PNG 无 LFS（.git 已 248MB，143 个 >1MB blob 共 221MB） | 历史已定型，重写需协调；`*.png binary` 已补 |

---

## 四、流程结论（重申并新增）

1. commit 引用数字必须实跑（上轮教训）；**本轮新增：改异步/线程语义时，对应套件的"等待语义"必须同步改**——H4 当晚一行实跑即可发现 28/2。
2. 测试替身不应真起线程/真做 IO（v04 FakeChatWorker 空线程竞态潜伏多版）。
3. QThread 生命周期铁律（本轮制度化）：**worker 挂 QObject parent + `finished→deleteLater` 唯一删除通道；任何槽内不得对 worker 手动 deleteLater/丢最后引用**。
