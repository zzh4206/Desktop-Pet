# 桌宠 24h 浸泡测试报告（win 端）

> 依据：`版本规划.md` §0.3 质量门——v0.3 起每里程碑 24h 浸泡。
> 对应里程碑：v0.7（吃鼠标）+ v0.8.1（win 工具补全）联合验证。
> 日期：2026-08-19 22:40 → 2026-08-20 22:30（约 24h）

## 1. 结论

**通过。** 全部验收项达标，无一崩溃、零新增告警、内存不升反降。

## 2. 环境与启动

| 项 | 值 |
|---|---|
| 进程 | PID 24784，`python app.py`（CPython 3.12，PySide6 6.11） |
| 构建 | `v0.7.5+win`（浸泡期间仓库推进至 v0.8.1，未影响运行中进程） |
| 配置 | 正常值（久坐 45min / 冷却 30min / 锁定 10s / idle 门槛 5min） |
| 起步内存 | 162.6 MB |

## 3. 验收项（对照红线）

| 验收项 | 结果 | 数据 |
|---|---|---|
| 进程无崩溃 | ✅ | 24h 存活，正常终止 |
| 内存 ≤ 200MB | ✅ | 162.6 → 119.7（15h）→ **80.7MB（24h）**，单调回落 |
| 日志无异常 | ✅ | 浸泡窗口 WARNING/ERROR **0 条**（存量 101 条全部为 8-18 前修复期旧账） |
| 线程泄漏 | ✅ | 15h 抽查 2 线程，无增长 |
| 看门狗链路 | ✅ | 实战验证 3 次完整链路：`抑制开始(10s) → 看门狗超时释放 → 释放检测 → FSM 回 idle 坠落` |
| 吃鼠标循环 | ✅ | 久坐分支按 30min 冷却规律触发（11:30–13:30 每半小时，AFK 期间） |
| 键盘可用性 | ✅ | 全程无键盘抑制记录（铁律1 结构性保证） |

## 4. 内存曲线（3 个采样点）

```
162.6MB ──► 119.7MB (15h) ──► 80.7MB (24h)
```
无泄漏趋势；回落为 Qt/tuple 分配的 working set 自然修剪。v0.7 新增的
钩子线程/看门狗线程/QThread（proactive 决策）按次创建-销毁，未见驻留。

## 5. 发现的问题（非阻塞）

1. **通宵挂机噪音（设计改进，已拍板）**：AFK 超过久坐阈值后，吃鼠标按
   30min 冷却无限循环（夜间 11:30–13:30 记录证实）。已决定：**空闲
   ≥2h 后吃鼠标退化为纯气泡提醒**（不再抑制），实施于 v0.8.2。
2. **测试时代残留（用户侧）**：宠物 age=3007 天 / FINAL 为 fast-mode
   时期遗产（example 曾误带 age_speed_multiplier=5040，v0.4.12 已修）。
   建议托盘"重新开始"重置；非代码问题。
3. **视觉误读**：长时间未喂养 → HUNGRY/SAD emoji 变体字形偏小，
   观感"缩水"；逻辑尺寸恒为 128px。归入 v0.10 立绘升级时统一。

## 6. 附：日志统计

- 总行数 439（24h 新增 ~30 行，事件密度健康）
- 浸泡窗口事件类型：久坐提醒气泡 / 吃鼠标三段日志（开始-释放-回idle）/
  早安（1 次）；无 [物理] 异常、无 Traceback、无看门狗被动触发

---
*报告人：win 端（ZCode 会话）；数据采样见会话记录。*
