# 桌宠 版本规划

> 与 `设计思路.md` 配套。每版是一个**可验收里程碑**，工作内容对应已冻结的接口契约。
> 排序原则：差异化前置（养成/物理）+ 技术风险前置（透明窗口 / DS / CGEventTap / AI 立绘）早 de-risk。版本顺序可调，但每版验收标准不可妥协。

---

## v0.1.0 上屏（透明浮窗 + emoji 立绘 + 底边行走）

**目标效果**：启动后桌面底边出现一只 emoji 小东西，透明置顶，随机溜达两步，托盘有图标能退出。

**工作内容**
- `pet/window.py`：PySide6 frameless 透明置顶浮窗（`WA_TranslucentBackground` + `WindowStaysOnTopHint` + Mac floating level）
- `pet/asset_provider.py`：`AssetProvider` 接口 + `EmojiProvider` 最小实现（按 `mood` 返回 emoji）
- `pet/behavior.py`：`BehaviorFSM` 骨架，实现 `idle` + `沿底边随机走` 两个状态
- `pet/tray.py`：系统托盘图标 + 退出菜单
- `pet/config.py`：读 `config.json`（DS key 占位，不真调）
- `app.py`：入口，组装上述模块

**验收标准**
- [ ] 启动后窗口透明、无标题栏、置顶，不被其他普通窗口盖住
- [ ] 角色以 emoji 显示在屏幕底边某处
- [ ] 30 秒内至少出现一次自主位移（底边行走）
- [ ] 托盘图标可见，点"退出"能干净退出（无残留进程）
- [ ] 关闭终端后进程不随之死掉（或可接受前台运行，记为已知限制）

---

## v0.2.0 能长大基础（养成 state + 情绪切换立绘）

**目标效果**：宠物有了心情/饱食/清洁，随时间掉；摸头/喂食/洗澡能点；不同心情 emoji 不同，"活着"的感觉出来。

**工作内容**
- `pet/pet_state.py`：`PetState` 数据类 + `PetStateStore`（含 `on_change` observer）
- 养成数值自然衰减（心情/饱食/清洁随时间降）
- 点击交互：摸头(+心情) / 喂食(+饱食) / 洗澡(+清洁) / 戳一戳(-心情)
- `EmojiProvider` 扩展：按 `mood`（含 HUNGRY/SLEEPY 等）返回对应 emoji
- `BehaviorFSM`：行为受养成数值调制（饿了找食物走动、困了打哈欠、低心情发呆）
- 养成数值持久化（`pet_state.json`），重启不丢

**验收标准**
- [ ] 数值随时间可见下降
- [ ] 四种点击交互各自改变对应数值，数值变化有视觉反馈（emoji 切换）
- [ ] 饱食低时宠物表现 HUNGRY 状态（找食物走动 / 求食 emoji）
- [ ] 重启后数值与上次一致
- [ ] `PetStateStore.on_change` 至少被 renderer 与 behavior 各订阅一次（接口验证）

---

## v0.3.0 桌面物理（拖拽抛掷 + 重力掉落 + 跟随鼠标）

**目标效果**：能抓起来扔出去，它会抛物线掉回底边；能跟随鼠标走；会随机伸懒腰/打滚。

**工作内容**
- `BehaviorFSM` 扩展：`dragged` / `thrown`(带重力积分) / `falling` / `follow_cursor` / `random_idle`
- 拖拽：mouse press/move/release，释放时按速度做抛物运动 + 触底弹一下
- 跟随鼠标模式（可开关）
- idle 随机小动作：伸懒腰/打滚/追光标

**验收标准**
- [ ] 按住拖动宠物跟随指针
- [ ] 释放带初速度时走抛物线，落到底边后停止（不穿屏、不飞出）
- [ ] 跟随鼠标模式下，宠物朝指针移动
- [ ] idle 状态下 1 分钟内至少触发一次随机小动作
- [ ] 物理只改 `screen_pos`，不碰养成 state（接口隔离验证）

---

## v0.4.0 能聊（DS 聊天 + function calling 框架 + 1 个工具）

**目标效果**：能和宠物对话框聊天，DS 流式回复，能让它"打开 Safari"这种工具调用。

**工作内容**
- `pet/llm.py`：DS API 客户端 + function calling + usage 统计
- 聊天面板：多行输入、流式打字机、markdown 渲染、`Ctrl+Alt+P`/托盘唤出
- `pet/tools_schema.py`：`ToolRegistry` + `ToolHandler` 接口 + schema 注册
- `pet/tools_mac.py`：实现 1 个工具（`open_app`：`open -a`）作样板
- system prompt 编码身份/性格/规则
- 全局热键先占位（v0.11 做真全局，这版用托盘/窗口聚焦触发）

**验收标准**
- [ ] 能与宠物多轮对话，DS 回复流式打字机显示
- [ ] 说"打开 Safari"时触发 `open_app` 工具调用并真的打开
- [ ] 对话历史保留上下文
- [ ] usage 统计被记录（可只打日志，UI 留 v0.6）
- [ ] `ToolRegistry` schema 与 Mac 实现解耦（加工具不改 llm.py 验证）

---

## v0.5.0 进化（年龄→阶段→分支）

**目标效果**：宠物随时间长大，到年龄进化；养得好长成健康态，疏于照顾长成落寞态，不同样。

**工作内容**
- 年龄随真实时间增长（倍率可配，默认略加速）
- 年龄到阈值触发进化事件
- 成长阶段切换：`YOUNG` → `ADULT` → `FINAL`
- 进化时取本阶段养护平均分 → 决定分支 `HEALTHY` / `NEGLECTED`
- `EmojiProvider`：按 `(stage, branch, mood)` 返回对应 emoji
- 进化事件可视化（一次动画/提示）

**验收标准**
- [ ] 年龄随时间可见增长
- [ ] 到阈值触发进化，阶段切换有反馈
- [ ] 同一阶段内养护分高 → 进化成 HEALTHY；低 → NEGLECTED（可设加速倍率便于测试）
- [ ] 不同 `(stage, branch)` 返回不同 emoji
- [ ] 进化分支被持久化，重启不丢

---

## v0.6.0 主动关怀（链式唤醒 + 提醒 + 节日问候）

**目标效果**：会主动找你——久坐提醒喝水、到点提醒吃饭、节日祝福、早安晚安。

**工作内容**
- `pet/proactive.py`：`ProactiveScheduler`（链式唤醒 10–360 min + 空闲/深夜检测 + 隔离上下文）
- follow-up：识别事件（去吃饭/考试）→ 定时追评
- 提醒：久坐/喝水/护眼/按时吃饭
- 节日/生日/纪念日祝福、早安晚安
- 气泡消息 UI

**验收标准**
- [ ] 久坐 N 分钟后主动弹气泡提醒
- [ ] AI 自定下次唤醒时间在 10–360 min 区间
- [ ] 深夜时段不主动打扰
- [ ] 提及"去吃饭"后能定时 follow-up
- [ ] 唤醒决策走隔离上下文，不污染主对话（日志可验）

---

## v0.7.0 吃鼠标（休息提醒 + CGEventTap + 安全铁律）

**目标效果**：久坐该休息时，宠物冲过来吃掉你的鼠标，你动不了——但随时能吐出来，重要时段不打扰。

**工作内容**
- `EatMouseSession`：`start` / `force_spit` / `on_dnd_active`
- Mac `CGEventTap` 抑制鼠标事件（需 Accessibility 权限）
- 宠物在光标位置咀嚼动画
- 安全铁律：只锁鼠标不碰键盘 / 自动超时 ≤15s / 强制吐出热键(`Cmd+Option+Esc`)+托盘菜单 / DND 白名单跳过
- 养成回血：吃鼠标 → +饱食 +心情
- DND：自有开关 + 可选读日历 Busy 状态

**验收标准**
- [ ] 休息提醒触发时进入吃鼠标模式，鼠标输入被抑制
- [ ] **键盘始终可用**（按 Esc/热键能立即解锁）
- [ ] 强制吐出热键与托盘菜单均能立即释放鼠标
- [ ] 单次锁定自动 ≤15s 释放
- [ ] DND 开启时不触发吃鼠标，只发温和气泡
- [ ] 吃鼠标后饱食/心情数值上升

---

## v0.8.0 系统工具完整 + 权限向导

**目标效果**：聊天里能指挥它开程序、搜文件、控音量、锁屏；首次启动引导你开权限。

**工作内容**
- `tools_mac.py` 补全：`open_app`/`clipboard`/`mdfind`/`volume`/`process`/`lock`/`sleep`
- 危险操作（关机/睡眠/删文件）在 `ToolRegistry.dispatch` 拦截 + 确认弹窗
- 首次启动权限引导向导：辅助功能/自动化权限清单 + 跳系统设置
- 透明窗口/拖拽文件给它打开（快捷启动器）

**验收标准**
- [ ] 至少 6 个工具经聊天可调用并生效
- [ ] 危险操作必须二次确认，取消则不执行
- [ ] 缺权限的工具调用给出明确提示而非静默失败
- [ ] 首次启动出现权限引导，完成后不再重复弹
- [ ] 拖文件到宠物能打开该文件

---

## v0.9.0 长期记忆

**目标效果**：宠物记得你说过的事，跨会话不丢；能管记忆。

**工作内容**
- `pet/memory.py`：`memorize` / `recall` / `forget` + 重要性打分 + 遗忘
- 滚屏摘要压缩长对话
- 记忆注入 system prompt
- 记忆管理 UI：查看/删除/清空

**验收标准**
- [ ] 告诉它一个偏好（"我喜欢喝咖啡"），后续会话能正确 recall
- [ ] 低重要性记忆随时间被遗忘
- [ ] 长对话滚动摘要不丢关键信息
- [ ] UI 能查看/删除单条/清空记忆
- [ ] 删除某条后该条不再注入 prompt

---

## v0.10.0 AI 立绘 provider

**目标效果**：宠物从 emoji 升级成 AI 生成的立绘，按成长阶段/心情/分支出图，长得独一无二。

**工作内容**
- `AIArtProvider`：按 `(stage, mood, branch)` 批量生成 + 缓存到 `assets/`
- 接入图像模型（即梦/SD/某 diffusion API，选型待定）
- `config.json` 切换 provider：`emoji` / `ai` / `commission`
- 养成/物理/动画**零改动**（接口验证）

**验收标准**
- [ ] 切换 `provider=ai` 后立绘变为 AI 图，emoji 作为 fallback
- [ ] 不同 `(stage, branch, mood)` 出不同图，并缓存
- [ ] 切回 `emoji` 不影响养成/物理行为
- [ ] 生成失败时优雅降级到 emoji，不崩

---

## v0.11.0 全局热键 + 开机自启

**目标效果**：全局热键随时唤出聊天；开机自动起来。

**工作内容**
- `pet/hotkey.py`：pyobjc 全局热键注册（需 Accessibility）
- 默认 `Cmd+Option+P` 唤聊天、`Cmd+Option+Esc` 强制吐出（v0.7 已用，这版统一管理）
- 开机自启：`~/Library/LaunchAgents/` plist 或登录项

**验收标准**
- [ ] 任意应用聚焦时按热键能唤出聊天
- [ ] 强制吐出热键在吃鼠标时有效
- [ ] 开机后宠物自动启动
- [ ] 热键可配置

---

## v0.12.0 打包 .app

**目标效果**：双击 `.app` 就能用，不用开终端。

**工作内容**
- py2app 出 `.app` bundle
- 资源/权限/路径处理（bundle 内 assets 路径）
- 安装与权限引导文档

**验收标准**
- [ ] 双击 `.app` 启动，全部核心功能可用
- [ ] bundle 内 assets 路径正确
- [ ] 提供 README 安装与首次权限设置步骤
- [ ] 无终端报错残留

---

## v1.0.0 发布

整合 v0.1–v0.12，全量回归验收，打 tag `v1.0.0`。

---

## 未来扩展（不排期，仅占接口，见 `设计思路.md` §九）

- 3D Renderer + 光照（`Renderer3D` + `sun_direction`）
- 经济系统（金币/库存/商店/换装）
- 攀爬窗口边缘（Accessibility 枚举窗口框）
- 小游戏（答题：DS 出题 +心情/+年龄小加速）
- 插件/脚本/API 接入
