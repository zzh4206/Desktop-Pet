# 桌宠 版本规划

> 与 `设计思路.md` 配套。每版是一个**可验收里程碑**。
> 修订：v0.0.3 整合设计师评审 + 项目总监评审反馈。
> 修订：v0.0.7 同步双平台协作（双侧并行开发）与 UI 分层（应用型界面走 QML）。

---

## 0. 总则

### 0.1 排序原则
差异化前置（养成/物理）+ 技术风险前置早 de-risk（透明窗口 / DS / CGEventTap / AI 立绘）。版本顺序可调，但每版验收的 **Must 项不可妥协**。每版 Must 需 **macOS 与 Windows 各自达成**（macOS 标准见各版；Windows 适配项、平台路径与风险 Spike 见 `Windows适配计划.md`）。

### 0.2 技术风险清单（高风险项，对应版本前必须有验证结果，否则不进下一里程碑）
| 风险 | 验证版本 | 验证物 |
|---|---|---|
| 透明窗口 Mac 兼容性 | v0.1 | 透明置顶浮窗上屏 |
| DS function calling 稳定性 | v0.4 | 多轮对话+工具调用跑通 |
| CGEventTap 鼠标抑制 + 安全解锁 | Spike(v0.7前) + v0.7 | 抑制+看门狗5s释放 |
| AI 图像生成方案可用/成本/质量 | Spike(并行,不占版本号) | 静态图可生成且去背 |
| 窗口枚举性能 | v0.3 起监控 | 不每帧枚举，CPU 达红线 |

### 0.3 质量门（每版合入前）
- 核心模块单元测试：`PetStateStore`、`BehaviorFSM`、`ToolRegistry`、`memory.py`（按版本到位情况）
- 关键路径集成测试：DS 工具调用、吃鼠标安全解锁、进化分支
- 物理边界测试：不穿屏、不飞出
- 合入前跑快速回归（防 v0.3 物理改 `screen_pos` 连累养成数值）
- 性能基准：`psutil` 抽测 idle CPU / 内存达红线（§一）

### 0.4 Must / Should 分级
- **Must**：核心功能、安全、性能基准、接口契约——不可妥协。
- **Should**：体验优化、可配置项、后台运行等软性目标——允许记为已知限制后续补。
- 软性标准（如"可前台运行"）只进 Should，不进 Must。

### 0.5 内部可运行验证
每里程碑至少产出**可通过 `python app.py` 跑起来**的内部验证，风险前置消化；正式 `.app` 打包仍 v0.12。

### 0.6 里程碑评审
每版完成后做一次约 15 min 验收评审，确认 Must 全绿再进下一版，避免"后面一起补"。

### 0.7 并行 Spike（不占正式版本号）
- **S0 AI 立绘 Spike**：v0.2 完成后启动，验证图像模型 API（即梦/SD/其他）可用性/成本/质量/去背，产出结论供 v0.10 接入选型。不阻塞主线版本。
- **S1 CGEventTap Spike**：v0.7 开发前启动，验证鼠标抑制 + 看门狗解锁，产出可复用代码骨架。
- **W1 透明穿透 / W2 鼠标钩子 Spike（Windows 侧）**：见 `Windows适配计划.md` §6，分别于 v0.1 前、v0.7 前启动。

---

## v0.1.0 上屏（透明浮窗 + emoji 立绘 + 底边行走）

**目标效果**：启动后桌面底边出现一只 emoji 小东西，透明置顶，随机溜达两步，托盘能退出。

**工作内容**
- `pet/window.py`：PySide6 frameless 透明置顶浮窗
- `pet/asset_provider.py`：`AssetProvider` 接口（**`get_static` + `get_frames` 双方法，v0.1 即冻结**）+ `EmojiProvider`
- `pet/behavior.py`：`ActionType` Enum + `BehaviorFSM` 骨架（`idle` + `沿底边随机走`）
- `pet/tray.py`、`pet/config.py`（DS key 占位）、`pet/logging_setup.py`（日志地基）
- `app.py`（`--verbose`）

**验收**
- Must
  - [ ] `python app.py` 前台启动，窗口透明、无标题栏、置顶
  - [ ] 角色以 emoji 显示在底边工作区（`NSScreen.visibleFrame`）
  - [ ] 30 s 内至少一次自主位移
  - [ ] 托盘"退出"能干净退出，无残留进程
  - [ ] 日志写入 `~/Library/Logs/桌宠/`
- Should
  - [ ] 关闭终端后进程不随之死（后台运行），允许记为已知限制

---

## v0.2.0 能长大基础（养成 state + 情绪切换立绘）

**目标效果**：宠物有心情/饱食/清洁，随时间掉；摸头/喂食/洗澡能点；不同心情 emoji 不同。

**工作内容**
- `pet/pet_state.py`：`PetState` + `PetStateStore`（含 `on_change` + **`save/load`**）
- 数值自然衰减；点击交互（摸头/喂食/洗澡/戳）
- `EmojiProvider` 扩展按 mood；`BehaviorFSM` 受数值调制
- 养成持久化 `pet_state.json`；config 默认值（decay/gain/score 权重阈值）
- 单元测试：`PetStateStore` save/load + 衰减

**验收**
- Must
  - [ ] 数值随时间可见下降
  - [ ] 四种点击交互改对应数值，有 emoji 切换反馈
  - [ ] 饱食低时进入 HUNGRY 视觉/行为
  - [ ] 重启后数值一致（save/load 生效）
  - [ ] `on_change` 被 renderer 与 behavior 各订阅（接口验证）
  - [ ] `PetStateStore` 单元测试绿
- Should
  - [ ] 数值/权重可在 config 调

**里程碑后**：启动 **S0 AI 立绘 Spike**（并行，不阻塞 v0.3）

---

## v0.3.0 桌面物理（拖拽抛掷 + 重力 + 跟随鼠标 + 随机游走）

**目标效果**：能抓起来扔出去，抛物线掉回底边；能跟随鼠标；会自主全工作区随机游走；偶尔伸懒腰/打滚。

**工作内容**
- `AssetProvider.get_frames` 实现（动作帧序列）
- `BehaviorFSM` 扩展：`dragged`/`thrown`/`falling`/`follow_cursor`/**`WANDER`（全工作区随机游走）**/`random_idle`
- **WANDER**：工作区内随机选目标点 → 行走过去 → idle 随机时长 → 选下一点，循环；尊重重力（地板/窗口顶面走，跨表面掉落）；频率受养成数值调制（心情高更爱游走）
- 拖拽抛物 + 触底停止；跟随鼠标模式；idle 随机小动作
- **窗口枚举缓存**（事件驱动 / ≤2s 刷新，不每帧）
- 边界测试：不穿屏、不飞出

**验收**
- Must
  - [ ] 按住拖动跟随指针
  - [ ] 释放带初速度走抛物线，落到底边停止，不穿屏不飞出
  - [ ] 跟随鼠标模式朝指针移动
  - [ ] **WANDER：空闲时自主游走至工作区随机点，到达后 idle 再选下一点，不卡死不重复同一点**
  - [ ] 1 min 内至少一次随机小动作（`get_frames` 出帧）
  - [ ] 物理只改 `screen_pos`，不碰养成 state（接口隔离）
  - [ ] 窗口枚举不每帧，idle CPU 达红线
  - [ ] 物理边界测试绿

---

## v0.4.0 能聊（DS + function calling + 1 工具）

**目标效果**：能和宠物聊天，DS 流式回复，能让它"打开 Safari"。

**工作内容**
- `pet/llm.py`：DS 客户端 + function calling + usage + **错误降级**
- 聊天面板（**QML，`ui/`**）：流式打字机、markdown、`Cmd+Option+P`（v0.11 做真全局，这版聚焦/托盘触发）
- `tools_schema.py`：`ToolRegistry` + `ToolContext` + schema；`tools_mac.py` 实现 `open_app` 样板
- system prompt 编码身份/性格/规则（含 prompt 注入防护限制）
- **DS key**：Keychain（`keyring`）存储 + env `DEEPSEEK_API_KEY` 兜底 + 首次启动引导输入
- 集成测试：DS 工具调用链

**验收**
- Must
  - [ ] 多轮对话，DS 回复流式打字机
  - [ ] "打开 Safari" 触发 `open_app` 并真的打开
  - [ ] 对话历史保留上下文
  - [ ] 网络/超时/额度耗尽时降级为预设回复，不崩
  - [ ] key 不入明文文件、不入仓库；首次启动能引导输入
  - [ ] `ToolRegistry` schema 与 Mac 实现解耦（加工具不改 llm.py）
  - [ ] DS 工具调用集成测试绿

---

## v0.5.0 进化（年龄→阶段→分支）

**目标效果**：随时间长大，到年龄进化；养得好健康态，疏于照顾落寞态，不同样。

**工作内容**
- 年龄随真实时间增长（`age_speed_multiplier` 进 config，**含开发 fast-mode**）
- 年龄到阈值进化；阶段切换 `YOUNG→ADULT→FINAL`（默认 7 天/21 天）
- 加权养护分 `0.4*mood+0.4*fullness+0.2*clean`，≥70→HEALTHY 否则 NEGLECTED
- `EmojiProvider` 按 `(stage, branch, mood)` 出图；进化事件可视化
- 进化分支持久化
- 集成测试：进化分支判定（用 fast-mode）

**验收**
- Must
  - [ ] 年龄随时间增长
  - [ ] 到阈值进化，阶段切换有反馈
  - [ ] fast-mode 下：高养护分→HEALTHY，低→NEGLECTED
  - [ ] 不同 `(stage, branch)` 出不同 emoji
  - [ ] 进化分支重启不丢
  - [ ] 进化分支集成测试绿（含加速倍率字段）

---

## v0.6.0 主动关怀（链式唤醒 + 提醒 + 节日）

**目标效果**：会主动找你——久坐提醒喝水、到点提醒吃饭、节日祝福、早安晚安。

**工作内容**
- `pet/proactive.py`：`ProactiveScheduler`（链式唤醒 10–360 min + 空闲/深夜检测 + 隔离上下文）
- follow-up；提醒（久坐/喝水/护眼/吃饭）；节日/生日/早安晚安；气泡 UI
- **应用生命周期**：应用未运行时主动关怀不触发（v0.11 自启后才完整），记为已知限制

**验收**
- Must
  - [ ] 久坐 N 分钟后主动弹气泡
  - [ ] AI 自定下次唤醒在 10–360 min
  - [ ] 深夜不主动打扰
  - [ ] 提及"去吃饭"能定时 follow-up
  - [ ] 唤醒决策走隔离上下文（日志可验）
- Should
  - [ ] 应用常驻时主动关怀完整（未运行不触发，记已知限制，v0.11 解）

---

## v0.7.0 吃鼠标（CGEventTap + 安全铁律 + 最小权限引导）

**目标效果**：久坐该休息且空闲时，宠物吃掉你的鼠标——但随时能吐出来，重要时段/活动中不打扰。

**工作内容**
- **前置 S1 CGEventTap Spike**：验证抑制 + 看门狗解锁
- `EatMouseSession`（`start`/`force_spit`/`on_dnd_active`）+ `CGEventTap` 抑制鼠标
- **看门狗线程**：主逻辑崩也 5 s 自动释放
- `BehaviorFSM` 加 `EAT_MOUSE` 状态，`ProactiveScheduler` 发事件切换；动画由 FSM 驱动
- 安全铁律：只锁鼠标不碰键盘 / 超时≤15 s / 强制吐出热键 **`Cmd+Option+Shift+P`**（弃 `Cmd+Option+Esc`，与系统强制退出冲突）+ 托盘菜单 / **idle gate**（`idle_time<5min` 只气泡）/ DND 白名单
- **Accessibility 最小引导**（从 v0.8 提前到这版）：检测权限，未开给提示 + 跳系统设置入口；权限未开时只提示不抑制
- 养成回血：+饱食/+心情
- 集成测试：吃鼠标安全解锁（含看门狗）

**验收**
- Must
  - [ ] 休息提醒 + `idle>5min` 双条件触发吃鼠标，鼠标被抑制
  - [ ] **键盘始终可用**，Esc/热键能立即解锁
  - [ ] **看门狗**：主逻辑崩溃后 ≤5 s 自动释放
  - [ ] 强制吐出热键 + 托盘菜单均能立即释放
  - [ ] 单次锁定 ≤15 s 自动释放
  - [ ] DND 开启时不触发吃鼠标，只气泡
  - [ ] `idle<5min` 时只气泡不吃鼠标
  - [ ] Accessibility 未开时明确提示 + 跳设置，不抑制
  - [ ] 吃鼠标后饱食/心情上升
  - [ ] 安全解锁集成测试绿
- Should
  - [ ] 完整权限向导 UI 留 v0.8 完善

---

## v0.8.0 系统工具完整 + 危险操作拦截 + 权限向导完善

**目标效果**：聊天里能指挥它开程序/搜文件/控音量/锁屏；首次启动引导开权限。

**工作内容**
- `tools_mac.py` 补全：`open_app`/`clipboard`/`mdfind`/`volume`/`process`/`lock`/`sleep`
- 危险操作在 `dispatch` 拦截 + **`NSAlert`**（显示命令+风险）+ 参数黑名单（禁 `/`、`~` 根、`rm -rf`）+ 文件数上限
- `permissions.py`：**权限状态页（QML，`ui/`）**（列出权限+✓/✗）+ 深链系统设置 + 重新聚焦自动刷新
- 单元测试：`ToolRegistry` 危险拦截 + 黑名单

**验收**
- Must
  - [ ] ≥6 个工具经聊天可调用并生效
  - [ ] 危险操作必须二次确认（`NSAlert` 显示命令+风险），取消则不执行
  - [ ] 缺权限工具调用给明确提示，不静默失败
  - [ ] 权限状态页正确显示状态 + 跳设置 + 聚焦刷新
  - [ ] 危险拦截/黑名单单元测试绿
- Should
  - [ ] 拖拽文件给它打开（快捷启动器）→ 移至 v0.9 或未来扩展，不进本版 Must

---

## v0.9.0 长期记忆（+ 快捷启动器）

**目标效果**：宠物记得你说过的事，跨会话不丢；能管记忆；拖文件给它打开。

**工作内容**
- `pet/memory.py`：`memorize`/`recall`/`forget` + 重要性打分 + 遗忘 + 滚屏摘要 + 注入 prompt
- 记忆管理 UI（QML，`ui/`）：查看/删除/清空
- 快捷启动器：拖拽文件/文件夹给它打开（从 v0.8 移入）
- 单元测试：`memory.py`

**验收**
- Must
  - [ ] 告诉它偏好（"我喜欢喝咖啡"），后续会话正确 recall
  - [ ] 低重要性记忆随时间遗忘
  - [ ] 长对话滚动摘要不丢关键信息
  - [ ] UI 能查看/删除/清空；删除后不再注入
  - [ ] 拖文件到宠物能打开
  - [ ] `memory.py` 单元测试绿

---

## v0.10.0 AI 立绘 provider（正式接入）

**目标效果**：宠物从 emoji 升级成 AI 立绘，按成长阶段/心情/分支出图。

**工作内容**
- 基于已完成的 **S0 Spike** 结论选型
- `AIArtProvider`：`get_static` 按 `(stage, branch, mood, skin)` 生成；一致性要求（512×512 + 固定 prompt + 固定 seed + `rembg` 去背 + 缓存 `assets/ai/{stage}_{branch}_{mood}_{skin}.png`）
- `get_frames` 降级返回 emoji 帧（AI 仅静态，动画走 emoji/占位，降级策略明确）
- `config` 切 `provider: emoji/ai/commission`
- 养成/物理/动画**零改动**（双方法接口已在 v0.1 冻结）

**验收**
- Must
  - [ ] `provider=ai` 后 `get_static` 出 AI 图，失败降级 emoji 不崩
  - [ ] 不同 `(stage, branch, mood)` 出不同图并缓存
  - [ ] 切回 `emoji` 不影响养成/物理
  - [ ] 养成/物理/动画代码零改动（接口验证，git diff 可验）

---

## v0.11.0 全局热键 + 开机自启

**目标效果**：全局热键随时唤聊天；开机自动起来。

**工作内容**
- `pet/hotkey.py`：pyobjc 全局热键（需 Accessibility）
- 默认 `Cmd+Option+P` 唤聊天、`Cmd+Option+Shift+P` 强制吐出（统一管理）
- 开机自启：`~/Library/LaunchAgents/` plist 或登录项

**验收**
- Must
  - [ ] 任意应用聚焦时按热键唤出聊天
  - [ ] 强制吐出热键在吃鼠标时有效
  - [ ] 开机后宠物自动启动
  - [ ] 热键可配置
- Should
  - [ ] 应用常驻后主动关怀完整（解 v0.6 已知限制）

---

## v0.12.0 打包 .app

**目标效果**：双击 `.app` 就能用。

**工作内容**
- 打包：macOS 用 py2app 出 `.app`，**Windows 用 PyInstaller 出 `.exe`**；bundle 内 assets 与 QML 资源路径处理
- 签名公证（可选）：未签名 wizard 兜底 + 右键开；签名+公证顺滑（列可选）
- 安装/卸载文档；首次权限设置步骤
- 权限申请流程实测（需 .app 才能测，此前版本已 `python` 验证）

**验收**
- Must
  - [ ] 双击 `.app` 启动，核心功能可用
  - [ ] bundle 内 assets 路径正确
  - [ ] README 含安装与首次权限设置步骤
  - [ ] 无终端报错残留
- Should
  - [ ] 签名+公证（可选，非强制）

---

## v1.0.0 发布

整合 v0.1–v0.12，全量回归验收 + 性能基准复核，打 tag `v1.0.0`。

---

## 未来扩展（不排期，仅占接口，见 `设计思路.md` §九）

3D Renderer + 光照 / 经济系统 / 攀爬窗口 / 小游戏 / 插件脚本 / Live2D（已弃）/ 多宠互动（不做）
