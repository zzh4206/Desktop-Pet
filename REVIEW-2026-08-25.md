# 桌宠仓库静态只读 Review 记录（含解决方案）

> 日期：2026-08-25
> 范围：全量源码（app.py 967 行 + pet/ 26 个 .py + ui/ 4 桥接 + 3 QML）+ spikes/ 12 脚本 + tools/postprocess_assets.py + config.example.json + requirements.txt + 5 份契约文档 + 资产/git 卫生
> 方式：3 路并行静态通读（核心运行时/服务层/测试文档卫生），高危结论全部经一手源码复核（不启动 GUI、不发网络）
> 严重度：🔴 高（崩溃/功能全挂/安全/资源） / 🟡 中（健壮性/语义偏差/明显 UX 缺陷） / 🟢 低（整洁/文档同步/防御性）
> 每条含：**位置 / 问题 / 验证 / 解决方案**

---

## 〇、进度确认

- HEAD = `b933363`（v0.10.17，win 动画产线）。上轮 `REVIEW-2026-08-21.md` 之后经历：v0.9.3-v0.9.4（上轮问题清偿）、v0.10.0-v0.10.17（AI 立绘产线 + 帧动画接入）、v0.11.0-v0.11.2（热键统一/自启/权限页，win/mac 交错）。
- 上轮 17 项：**13 已修、2 部分修（H4/M7）、3 未修（L6/L8/L10）**，另有 L2/L3/L7 被 v0.9.4（ba41faa）标题声称"L1-L12 清零"但实际未清（diff 无版本行改动）——**commit 声明与事实不符，处置核对表结论需复核后采信**。
- 版本号体系失控：`APP_VERSION="v0.7.4+win"`（约 60 个提交未更新）、`pet/__init__.__version__="0.9.3"`、platform.py 6 处幻影"v0.12.1"注释；v0.10.x(win) 与 v0.11.x(mac) 版本号并行交错。
- 总体评价：v0.9.x 对上轮问题的修复质量普遍扎实（定时炸弹类 M1/M5/M6 全落地，H2/H3 回归点封死）；**但 v0.10.15-17 动画接入引入 4 个新高危，其中两个直接命中该交付物本身**（小动作动画不可见、渲染缓存内存超标），且该批次只做了"offscreen 手工目检"、无自动化测试（曾覆盖 AIArtProvider 的 test_v10_ai_provider.py 在 revert 78a01c9 中删除后未恢复）——再次验证上轮流程结论："新增集成点没有不走 mock 的自动化验证就一定会漏"。

---

## 一、🔴 高危

### H1. 随机小动作动画（stretch/blink/roll）在同一 tick 内被启动又立刻停止，v0.10.15"小动作帧序"整体不可见
- **位置**：`app.py:789-797` × `app.py:881-882`
- **问题**：`_tick` 先处理 `ActionType.ANIMATE` → `_play_animate(name)` 设 `_anim_key="blink/stretch/roll"` 并 `play_frames`；紧接着同一 tick 调 `_frame_tick(action, mode, prev_mode)`。FSM 只在 IDLE 分支返回 ANIMATE（behavior.py:555-563），此时 `mode=="idle"`，`_frame_tick` 走到末行兜底：
  ```python
  if getattr(self, "_anim_key", None) not in (None, "land"):
      self._stop_anim()
  ```
  刚设的 key 不在豁免表 → `stop_frames()` 立即恢复静帧。两次 `set_sprite` 都发生在返回事件循环之前，最终上屏的就是静帧。commit 9e912dc 声称"offscreen 验证 loop/stop/one-shot 通过"只测了 window 层，未测 app 集成路径。
- **验证**：一手复核上述控制流；IDLE 模式下 `_play_animate` 后必然落入 881 行兜底分支。
- **解决方案**：① 兜底 stop 豁免小动作 key（本 tick 或有效期内由 ANIMATE 启动的）；② 小动作需要终止机制——非循环序列（stretch/roll）由 `_advance_frame` 自然播完恢复静帧，循环序列（blink）加到期自动 `_stop_anim`（QTimer.singleShot + key 相同才停，防误停后续 walk/fall）；③ 补 offscreen 自动化测试锁行为（idle+ANIMATE 后 `window._frames` 非空）。

### H2. Windows 默认配置下两个全局热键全部注册失败（v0.11 Must 开箱不可用）
- **位置**：`config.example.json:37-40` × `pet/hotkey_win.py:35-39,67-80` × `pet/config.py:242`
- **问题**：example 的 `hotkeys.chat/spit = "cmd+option+p"/"cmd+option+t"` 是 mac 键位；`load_config` 以 example 为基底深合并（用户 config.json 不写 hotkeys 时该段**恒存在**），win 端 `parse_hotkey` 的 `_MOD_MAP` 只有 ctrl/control/alt/option/shift/win/meta、**无 "cmd"** → 逐段未命中即返 `(0,0)` → 两键均判"无效组合"（hotkey_win.py:166-168）→ `HotkeyManager.start` 返 False，app 只打日志"[热键] 全部注册失败"。连锁：① 聊天唤出/强制吐出在 win 开箱不可用；② 吃鼠标气泡宣传的"吐出按 Ctrl+Alt+T"逃生口不存在（叠加本轮 M4 卡死风险）；③ 权限页热键项恒显"热键管理器未运行"（perm_bridge.py:94-108 查询真实状态，状态就是没起来）。
- **验证**：一手复核 parse_hotkey 逐段查 map 逻辑与 config.py:242 `_deep_merge(cfg, user)`（example 先载入为 base）；platform.py:606 `hk_cfg = cfg.get("hotkeys", {})` 直接取合并后的 mac 串，win 默认键位永不生效。
- **解决方案**：win 端 `_MOD_MAP` 增加 `"cmd"/"command": MOD_CONTROL` 别名（option 已映射 alt，mac 风格串在 win 解释为 ctrl+alt+p/t，与 README 宣传一致，example 保持跨平台单一值）；`_loop` 里硬编码的 key_str 提示（hotkey_win.py:173,183 "ctrl+alt+p"）与实际配置解耦；补 parse_hotkey 单测（cmd 别名/非法串/数字键）。

### H3. QPixmap 缓存按 1024×1536 全分辨率缓存，上限 ≈400 MiB + 每次换帧全图高质量缩放
- **位置**：`pet/window.py:103-124`
- **问题**：帧图/静态图均为 1024×1536 RGBA，解码后每张 ≈6.3 MiB；`_pix_cache` 存**镜像后的全分辨率图**，上限 64 条 → 稳态上限 ≈400 MiB（对桌宠属超标，全部可能 key = 30 静态×2 朝向 + 48 帧 > 64，长会话必然触顶）。且缓存的是原图，`set_sprite` 每次换帧都从 1024×1536 做 `SmoothTransformation` 缩到 128-320px（150-320ms/帧的播放节奏下 CPU 持续消耗）。另外"LRU"实为 FIFO：命中不重排、`pop(next(iter(...)))` 删最早插入项，与提交信息"LRU 缓存"不符；也不随阶段进化清旧条目。
- **验证**：一手复核 window.py:103-124（缓存的是 transformed 全图、scaled 在命中路径每次执行、无 move_to_end）。
- **解决方案**：入缓存前先 `scaled` 到 `(sprite.width, sprite.height)` 显示档（键加尺寸），单条 ≤0.6 MiB、总量 <40 MiB；换用 `OrderedDict`（命中 `move_to_end`，逐出 `popitem(last=False)`）成真 LRU；顺带处理加载失败路径（null pixmap 也入缓存防每帧重试主线程 IO）。

### H4. 滚屏摘要在主线程同步调 `chat_once`，最长冻结 UI 130 秒（上轮 H4 残留）
- **位置**：`pet/ui/chat_bridge.py:130, 187-189` × `pet/llm.py:33`
- **问题**：上轮 H4 只修了 tool 轮配对（v0.9.3 切点推迟逻辑已确认），但摘要请求仍在 `send()`（QML 主线程 Slot）里同步执行 `chat_once`（connect 10s + read 120s 超时），期间宠物 50ms tick、气泡、输入全部冻结——正是 v0.6.2 给 proactive 修掉的同类问题。
- **验证**：一手复核 chat_bridge.py:130（send 内直调 `_maybe_summarize`）与 187-189（同步 `chat_once`）、llm.py:33 `_TIMEOUT=(10,120)`。
- **解决方案**：摘要移后台 QThread（完成后经信号回主线程替换历史；前缀校验防陈旧应用）；配合 M2/M3 一并修（见下）。

---

## 二、🟡 中危

### M1. 每秒衰减 on_change 覆盖正在播放的动画帧 → 行走/咀嚼期间周期性闪静帧
- **位置**：`pet/window.py:189-192` × `pet_state.py:324-333` × `app.py:231`
- **问题**：`apply_decay` 无条件追加 `deltas["age"]`（不看 config）→ 每 1s `update()` 必产生新 state → 必 notify → `window.on_state_change` 无条件 `set_sprite(get_static(state))` 把 QLabel 换成静帧；此时动画 QTimer 并未停（on_state_change 不知道 `_frames` 在播），下一个帧 tick（150-320ms 后）又换回动画帧——**walk/chew 循环播放期间每秒闪一次静帧、持续最长一个帧间隔**。另外 `on_state_change` 不更新 `_static_sprite`，动画期间 mood/阶段变化后 `stop_frames` 恢复的是动画开始时的旧静帧。
- **验证**：一手复核 on_state_change 无 `_frames` 判断；apply_decay 的 age 增量无条件性由通读确认。
- **解决方案**：`on_state_change` 检测 `self._frames` 非空时只更新 `_static_sprite` 不动 label；动画自然结束/stop_frames 恢复时即用新静帧。

### M2. 滚动摘要把降级兜底文案当"有效摘要"写回，10 轮原文被罐头话顶掉
- **位置**：`pet/ui/chat_bridge.py:187-196` × `pet/llm.py:35,163-171,230-242`
- **问题**：HTTP 4xx/5xx（额度耗尽/鉴权失败）时 `_stream_once`/`chat_once` **不抛异常而是返回 `FALLBACK_REPLY`**（"我开小差了～一会儿再问我吧。"）；`_maybe_summarize` 只判 `summary` 非空即执行历史替换 → `[此前对话摘要]\n我开小差了…` 顶掉 10 轮上下文。v0.9.3"DS 失败保留原文"的声明只对异常路径成立，对降级返回路径不成立。
- **验证**：一手复核 llm.py 4 处 `return FALLBACK_REPLY`（无 raise）与 chat_bridge.py:190-196 仅判空。
- **解决方案**：摘要侧判 `summary == FALLBACK_REPLY` 视为失败保留原文（与 H4 异步化同批修）。

### M3. 摘要后 `_messages[cut * 2:]` 与 history 轮数错位，UI 消息被过度删除
- **位置**：`pet/ui/chat_bridge.py:197` × `_on_done`（236-246 全量进 history）vs（272-281 只 user+assistant 进 messages）
- **问题**：tool 轮和多段 assistant 轮只进 `_history` 从不进 `_messages`，`cut*2` 按"每轮 2 条"假设切片：有工具调用的会话中 UI 删除条数远多于被摘要的轮数，仍在 history 里的消息从界面上消失（history/UI 失同步）。
- **验证**：通读 `_on_done` 双列表追加逻辑（history 全量 append、messages 每轮 2 条）。
- **解决方案**：切片按 `_messages` 自身轮界对齐（按 user 消息计数），或摘要时同步记录 messages 侧应删条数。

### M4. 吃鼠标抑制启动失败 → FSM 永久冻结在 EAT_MOUSE（叠加 H2 时无逃生口）
- **位置**：`pet/proactive.py:308-329` × `pet/behavior.py:482-488`
- **问题**：FSM 收 `eat_mouse` 事件进 EAT_APPROACH→EAT_MOUSE 后，若 `sess.start()` 失败（UIPI 前台管理员窗 / SetWindowsHookEx 失败 / mac tap 创建失败），else 分支只发"没管住鼠标"气泡：不发 `eat_mouse_off`，`_was_active` 仍为 False → `sync_release` 的 active→False 跳变检测永不触发；behavior.py 的 EAT_MOUSE 态无自超时。宠物从此卡在光标处咀嚼，仅热键/托盘 force_spit 可解——而 H2 下 win 默认热键没注册。
- **验证**：一手复核 proactive.py:308-329 else 分支无 FSM 回退事件；`_eat_pending` 在 313 行已清空。
- **解决方案**：`sess.start()` 失败分支补发 `eat_mouse_off`（或直接调 fsm 事件）让 FSM 退出 EAT_MOUSE；补一条门禁失败路径的 FSM 集成测试。

### M5. 同一 LLM 客户端被 ChatWorker 与 _ProactiveWorker 两个 QThread 共享，`_resp`/`usage` 无锁竞态
- **位置**：`app.py:193,359,416-418` × `pet/llm.py:156-162,213,380-389`
- **问题**：`_stream_once` 把当前响应挂实例级 `self._resp` 并在 finally 清空：两请求并发时后者覆盖前者引用；`ChatWorker.cancel()` 会 `self._client._resp.close()`——可能关掉 proactive 正在跑的流（主动关怀无端"离线退罐头"），或因 A 已 finally 置 None 而取消失效流式跑满 120s。`Usage.add` 与 `set_memory_context`（主线程）同样裸共享。
- **验证**：通读两个线程体与 `_resp` 读写点。
- **解决方案**：`_resp` 改为请求局部变量经 worker 持有（cancel 关 worker 自己的 resp），或客户端按线程隔离（chat/proactive 各建一个 client 实例——配置同源，改动最小）。

### M6. shutdown 不停 `_ProactiveWorker`，退出时决策线程可能仍在跑
- **位置**：`app.py:884-939` × `pet/proactive.py:511-518,608-640`
- **问题**：退出七步序处理了 EatMouseSession/热键/ChatWorker（cancel+wait 2000ms），但对 `proactive._wake_worker`（最长挂 120s read 超时）既不 cancel 也不 wait；`_fire_wake` 期间退出 → QThread 对象随 scheduler 被 GC，"QThread: Destroyed while thread is still running" 崩溃风险。
- **验证**：通读 shutdown 七步序无 wake_worker 语句。
- **解决方案**：shutdown 序补 `proactive.shutdown()`（cancel 在飞 worker + wait + 断信号），与 ChatWorker 同模式。

### M7. `video_apps` 白名单是 mac 应用名，win 侧活跃内容门禁形同虚设（M8 修复被配置抵消）
- **位置**：`config.example.json:51-61` × `pet/platform.py:647-649` × `pet/sensor_win.py:283-295`
- **问题**：win 的 `foreground_process_name()` 返回 `CHROME.EXE/MSEDGE.EXE` 等 exe 名（upper），白名单是 "Google Chrome"/"Microsoft Edge" 等 mac 显示名 → 恒不命中；唯一可能命中的 "VLC" 也因返回值带 `.EXE` 后缀失配。窗口化/全屏视频前台时吃鼠标照吃不误（上轮 M8 的修复在 win 被配置漂移抵消）。
- **验证**：通读 sensor_win 返回值格式与白名单集合比对。
- **解决方案**：win 侧匹配按 exe 名（含去后缀双格式）做归一化比对，或配置拆平台（`video_apps_win`）；最小修是比对时 `name.rstrip(".EXE")` 且白名单补 exe 形态。

### M8. PermBridge 构造即主线程同步跑全部自检，启动与"重新检测"都冻结 UI 数秒
- **位置**：`pet/ui/perm_bridge.py:29,45-54,112-121` × `pet/tools_win.py:88-99` × `app.py:353`；mac 同款 perm_bridge_mac.py
- **问题**：`__init__` 调 `refresh()`：win 端 `_check_clipboard` 走 PowerShell 子进程（典型 1-3s、超时 10s）+ COM 音量 + 文件探测全在主线程；app 启动路径与 QML 按钮同样冻结。
- **验证**：通读 refresh 调用链与 `_ps_run` 超时。
- **解决方案**：自检搬 QThread（或 QThreadPool 逐项异步），QML 侧逐项刷新；构造函数只建骨架。

### M9. `frames_for` 无任何缓存：walk 模式下每 50ms 重复 `os.path.isfile`
- **位置**：`pet/asset_provider.py:283-295` × `app.py:876`
- **问题**：`_frame_tick` 每 tick 调 `frames_for(stage,"walk")` → 每次执行 2 次 isfile + join/normpath + 新建 SpriteRef 列表 ≈ 40 次 stat/秒 持续发生在主线程（chew 同理）；`_miss_cache` 只服务 `get_static`，帧路径没有对应机制。冷盘/杀毒扫描时 tick 抖动。
- **验证**：通读 frames_for 无缓存路径与 _tick 调用频率。
- **解决方案**：`frames_for` 按 `(stage,key)` 缓存结果（None 也缓存）；阶段进化时清缓存或 key 天然含 stage。

### M10. `proactive.eat_mouse_hotkey_label` 配置键读但 schema 未收，一配即整段 proactive 回退默认
- **位置**：`pet/proactive.py:200-202` × `pet/config.py:132-168`（additionalProperties:false 在 167）
- **问题**：代码读该键定制气泡文案，但 `_SECTION_SCHEMAS["proactive"]` 未列它；用户在 config.json 设置 → jsonschema 校验失败 → 整段回退默认（quiet_hours/sedentary/dnd 等自定义全丢，仅日志 warning）。典型 schema 与代码漂移陷阱（v0.7.2 已犯过同类）。
- **验证**：grep 该键无 schema 定义。
- **解决方案**：schema 补键（string, 默认空）；顺手全量 grep `cfg.get(` 对照各段 schema 键集。

### M11. hotkey `_ready` Event 在 `thread.start()` 之后创建 + `_reg_ok` 读取竞态
- **位置**：`pet/hotkey_win.py:138-147` × `160-163`
- **问题**：`threading.Event()` 在 `start()` 后才建：子线程先跑到 `_ready.set()`（187 行）则 AttributeError 早退——热键已注册但没有 GetMessage 循环，静默失效且永不 Unregister；主线程 `wait(2.0)` 超时或 `_loop` 早抛后读 `self._reg_ok.items()` 同源 AttributeError（app._setup_hotkeys 无 try 包裹，启动即崩）。`stop()` 读 `_thread_id` 同理（join 白等 2s）。
- **验证**：一手复核 start/stop/_loop 三段的初始化时序。
- **解决方案**：`_ready = threading.Event()` 与 `_reg_ok = {}`、`_thread_id = None` 全部提到 `start()` 之前的 `__init__`/start 头部；`_reg_ok` 读取前判空兜底。

### M12. `on_conflict` 仍在热键线程直调 `bubble.show`（上轮 M7 残留）
- **位置**：`pet/hotkey_win.py:176-181` × `app.py:484-488`
- **问题**：on_chat/on_spit 已走 `_HotkeySignalBridge` 信号转主线程，但注册冲突回调仍在热键线程 `_loop` 内直接执行，而 app 侧 on_conflict 里 `bubble.show(...)` 是 QWidget GUI 操作——跨线程碰 Qt 是未定义行为（注册冲突时触发一次）。
- **验证**：一手复核 176-181 行直调与 app 侧 bubble.show。
- **解决方案**：冲突也编码进 bridge 信号（如 hid=0 表冲突、携带 name/key），主线程统一分发。

---

## 三、🟢 低（择要）

| # | 位置 | 问题 | 解决方案 |
|---|---|---|---|
| L1 | `app.py:875` | `getattr(self,"_follow",False)` 读 PetApp 不存在的属性恒 False——死分支（本意 `fsm.motion_mode=="follow"`） | 改读 `self.fsm.motion_mode` |
| L2 | `app.py:78` / `pet/__init__.py:3` / platform.py 6 处 | 版本号失控（v0.7.4+win / 0.9.3 vs HEAD v0.10.17）+ 幻影"v0.12.1"注释 | 单一版本源治理（打包前专项） |
| L3 | `window.py:100-127` | 加载失败路径与注释不符：`pm.isNull()` 时不清 label（注释称"清 pixmap 退文本"未实现）、失败不缓存每帧重试主线程 IO、运行期文件被删把完整路径当文本画在 119pt 窗口 | 失败入缓存 null 哨兵 + isfile-False 分支降级静态兜底 |
| L4 | `window.py:161` | `play_frames` 把"当前动画帧"捕获为 `_static_sprite` → 动画间切换（walk→chew）后恢复的是旧动画帧 | 播放中再入时保留原 `_static_sprite` |
| L5 | `app.py:493-516` | `_setup_hotkeys` ImportError 分支：`_hk_hint` 未定义（else 分支 NameError）且 bridge=None 时 M7 修复失效 | 提前定义 hint；import 失败时不启热键仅日志 |
| L6 | `app.py:535-542` × tray | 自启切换失败只弹气泡不回滚托盘勾选，状态脱节到重启 | 失败时 `tray.set_autostart_state(False)` |
| L7 | `asset_provider.py:237` | `_FRAME_SPECS["eat_mouse"]`（4 帧×3 阶段=12 张素材）无任何调用方——死配置（app 吃鼠标态用 chew） | 接入或删 spec+素材（v0.10.15 接入时漏了） |
| L8 | `asset_provider.py:292-294` | `frames_for` 返回 `width=1,height=1` 的 SpriteRef，依赖调用方先改尺寸——新调用方直接 `play_frames` 会把窗口 resize 成 1×1 | `frames_for` 内直接填 `_STAGE_SIZE` 档 |
| L9 | `hotkey_win.py:173,183` | 注册日志/冲突提示硬编码 "ctrl+alt+p/t"，与实际配置无关（H2 修复后更显突兀） | 从 parse 前的原始串生成提示 |
| L10 | `tools_schema.py:68-71,181-184` | 参数黑名单对所有工具任意字符串一刀切：clipboard set 复制含 ".."/"rm -rf" 字样的文本、memory fact 含 "..." 全被拒 | 按工具字段白名单式校验（纯文本载荷跳过路径/命令扫描） |
| L11 | `mouse_lock_win.py:199-212` | `start` 的 1s 就绪等待超时后仍置 `_active=True`，迟到挂钩失败被吞（有 10s 看门狗兜底） | 超时按失败处理或等待后复查 `_hook_ok` |
| L12 | `memory.py` × memory_tools × app | MemoryStore 跨线程读写无锁（ChatWorker 线程 memorize vs 主线程 save/forget） | 一把 `threading.Lock` 收口 |
| L13 | `perm_bridge.py:75-88` | `_check_ll_hook` 的 WinDLL 未开 `use_last_error`，失败详情恒陈旧值 | `ctypes.WinDLL("user32", use_last_error=True)` |
| L14 | `tray.py:24` | `_on_autostart` 的注释是 `_on_spit` 的（复制粘贴错误） | 改注释 |
| L15 | `behavior.py:34` | `ActionType.FOLLOW_CURSOR` 声明后全仓无构造点——死枚举 | 删除 |
| L16 | `platform.py:126-129,142-144,405-409,652-656` | get_ds_key/set_ds_key 基类+mac+win 三处同实现重复 | 只留基类 |
| L17 | `asset_provider.py:203-243` | 局部冗余 `import os`、失效 `# noqa: F821`；commit 9e912dc 声称"④帧贴底平移"实为 PNG 烘焙、无运行时代码（描述误导） | 清理；commit 口径注意 |
| L18 | `window_mac.py:65` | except 分支 warning 后多余 `pass` | 删 |

### 测试与文档专项（🟡-🟢）

| # | 位置 | 问题 | 解决方案 |
|---|---|---|---|
| T1 | `pet/window.py` 动画层 / `app.py` 播帧 / `asset_provider.frames_for` | **零自动化测试**：v0.10.15-17 只做"offscreen 手工目检"；曾覆盖 AIArtProvider 的 test_v10_ai_provider.py 在 revert 78a01c9 删除后未恢复——H1 正是因此漏网 | 恢复/新建 offscreen 动画层测试（本批已补，见修复①） |
| T2 | `spikes/test_v03_fsm.py` | 静态 grep 数 65 个 `check(` 与提交口径"66/66"差 1——**实跑复核：脚本自计数 66 通过，提交口径属实**（grep 漏计循环/动态调用）。留档为审查方法教训：数字争议必须实跑裁决，静态计数不可作为指控依据 | （无需处理；本条已修正） |
| T3 | `spikes/test_v07_eat_mouse_win.py:235-237` | `FakeLockInstance` 未定义、靠 `if False else` 惰性求值不炸（上轮 L13 未修） | 重写单一表达式 |
| T4 | `spikes/test_v07_eat_mouse_win.py` / `test_v09_memory.py:211` / w1 | 真装 LL 钩子锁鼠标≤1s、真弹资源管理器窗口、阻塞式 exec 演示——README 无副作用警示 | 文件头加警示 + README 补测试说明 |
| T5 | `设计思路.md:43-66` | 模块树列不存在的 renderer.py/permissions_*.py；实存文件未列（上轮 L15 未修） | 同步模块树 |
| T6 | `tools/postprocess_assets.py` | 整体过期：STAGE_SIZE 64/96/128 vs 实际 192/256/320；rembg+gap_fix 管线 v0.10.14 已推翻；引用文件不在仓库 | 标注废弃或重写为现状说明 |
| T7 | `设计思路.md`/`版本规划.md` | 仍写"动画走 emoji 降级（AI 仅静态）"，v0.10.15 已实装帧动画；psutil 幽灵依赖（零 import） | 文档同步 |
| T8 | `工作表.md` | 无 v0.10.1-v0.10.17 十七个提交的台账；历史测试数字全部过期 | 补记账 |
| T9 | `README.md` | 未提 AI 立绘/帧动画与 `provider` 配置键（example 默认 `"ai"`）；测试指引无命令示例/副作用警示 | 补功能与配置说明 |
| T10 | `config.example.json` | `llm.last_provider` 无代码消费（上轮 L8）；`behavior.pet_height_px:64` 是 v0.3 时代值且启动即被覆盖（死配置） | 删或实现 |
| T11 | 资产与 git | 112MB PNG 无 LFS 且历史整组重写 ≥6 轮 → .git 已 197MB；.gitattributes 未显式声明 png binary | 引入 LFS 或接受现状（历史已定型，重写需协调）；补 `*.png binary` |
| T12 | 仓库根 | 7 份中文过程文档平铺根目录无归类；.gitignore 为未裁剪 toptal 模板 | docs/ 归类（低优先） |

---

## 四、上轮 Review（2026-08-21）处置状态核对

| 旧编号 | 内容 | 状态 |
|---|---|---|
| H1 | `_setup_chat` 引用未定义 `paths` | ✅ 已修（现取 `self.adapter.get_paths()`，通读确认） |
| H2 | llm.py 三行初始化误挪 | ✅ 已修（`_timeout/_resp/usage` 回到 `__init__` 尾部，`set_memory_context` 只换 system） |
| H3 | file_search 单引号注入 | ✅ 已修（`_PATTERN_BAD/_SCOPE_BAD` 均含 `'`/`"`/`$`） |
| H4 | 摘要切碎 tool 轮 + 主线程同步 | 🔶 部分修（配对已对齐；同步网络调用转本轮 **H4**） |
| M1 | GetTickCount 32/64 混算 | ✅ 已修（两侧 `& 0xFFFFFFFF` 回绕相减） |
| M2 | Ctrl+Alt+T 双重注册 | ✅ 已修（钩子线程不再自注册，统一 HotkeyManager）；但默认键位本身在 win 解析失败（本轮 **H2**） |
| M3 | 权限页热键自检恒假阴性 | ✅ 已修（查 `adapter._hotkey_mgr` 真实状态）；但 H2 下该项显示的"未运行"是真的没运行 |
| M4 | 自检真装钩子/写剪贴板 | ✅ 已修（装后即卸不进循环；剪贴板改 `action=get` 只读） |
| M5 | `_quiet_end_after` 月末 ValueError | ✅ 已修（`+ timedelta(days=1)`） |
| M6 | UIPI 降级检测不可能触发 | ✅ 已修（OpenProcessToken + TokenElevation，句柄 finally 关闭） |
| M7 | 热键回调跨线程操作 Qt | 🔶 部分修（on_chat/on_spit 走信号桥；on_conflict 仍直调，本轮 **M12**） |
| M8 | 活跃内容绑定全屏 | ✅ 已修（新增 `foreground_process_name()` 不要求全屏）；但 win 白名单名不匹配（本轮 **M7**） |
| M9 | 托盘自启勾选不同步 | ✅ 已修（`_act_auto` 引用 + blockSignals 防回环） |
| M10 | window/FSM 钳制不一致 | ✅ 已修（横向改 availableGeometry 合集，注释留痕） |
| M11 | recall 假 TF-IDF | ✅ 已修（df/IDF 真实消费） |
| M12 | eat_mouse 缺 return | ✅ 已修（显式 `return sess`） |
| M13 | 气泡点击关闭 + 流式转义 | ✅ 已修（mousePressEvent → hide；streamingText 走 `_md_to_html` 同管道） |
| L1 | set_ds_key 定义两次 | ✅ 已修（基类只留一处）；三处子类重复转本轮 L16 |
| L2 | 版本号滞后 | ❌ 未修且恶化（v0.7.4+win vs v0.10.17）；**v0.9.4 标题声称"L1-L12 清零"与 diff 事实不符** |
| L3 | w1 结论位"待实测" | ❌ 未修（仍"待实测后填写"） |
| L4 | 断言名"五项"实六项 | ✅ 已修 |
| L5 | mac Finder 过滤过宽 | ➖ 维持（未见改动） |
| L6 | win 无 lock 工具/denylist | ❌ 未修 |
| L7 | psutil 幽灵依赖 | ❌ 未修（文档仍列） |
| L8 | last_provider 无消费 | ❌ 未修 |
| L9 | memory load 不滤类型 | ✅ 已修（try-float 全字段） |
| L10 | confirm 无超时 | ❌ 未修 |
| L11 | _tick 吞异常 | ✅ 已修（warning + exc_info） |
| L12 | _windows_cache 死条件 | ✅ 已修（时间戳 TTL） |
| L13 | FakeLockInstance 惰性求值 | ❌ 未修（转本轮 T3） |
| L14 | T12 真弹资源管理器 | ❌ 未修（转本轮 T4） |
| L15 | 文档漂移三件 | ❌ 部分未修（转本轮 T5-T7） |

---

## 五、汇总与建议处理顺序

- **统计**：🔴 4 项 / 🟡 12 项 / 🟢 低 18 项 + 测试文档专项 12 项；上轮 17 项中 13 已修、2 部分修、3 未修 + 3 项被"清零"声明掩盖。
- **第一批（本批立即修）**：H1（动画被停）+ H2（win 热键键位）——核心功能实际不可用且修复面小；H3（缓存降档 + 真 LRU）；H4+M2+M3（摘要异步化/降级判定/切片对齐，同域一批修）。
- **第二批（状态机/线程类）**：M4（吃鼠标卡死，与 H2 叠加无解）+ M5/M6（线程竞态与退出）+ M11/M12（热键收尾）。
- **第三批（体验/性能）**：M1（闪帧）+ M8（权限页异步）+ M9（frames_for 缓存）+ M7（win 白名单）。
- **随批**：M10（schema 补键）、L 级随手修、T1 动画层自动化测试保持、T2 测试口径、版本号与文档治理（打包前专项）。
- **流程改进（重申上轮）**：① commit 引用的测试数字必须实跑核对（本轮曾以静态 grep 误判 v03"66 实为 65"，实跑 66/66 后修正——数字争议以实跑为唯一裁决）；② commit 声明"清零/完成"前必须逐条 diff 复核（v0.9.4 L1-L12 声明失实）；③ 每个新交付物至少一条不走手工目检的自动化用例（v0.10.15 动画三连提交全靠目检，H1 三个版本无人发现）。
