# 桌宠仓库静态只读 Review 记录

> 日期：2026-08-17
> 范围：全量源码 + 4 份契约文档 + 5 个测试脚本 + W1 spike
> 方式：逐文件静态阅读，对照接口契约与 git 历史，未改正仅指出
> 严重度：🔴 高（影响功能/默认体验/契约） / 🟡 中（健壮性/一致性） / 🟢 低（整洁/文档/小瑕疵）

---

## 🔴 高

### H1. fast-mode 测试值污染默认配置模板
- **位置**：`config.example.json` + git 历史 `47be390 v0.5.0`
- **问题**：`decay_per_hour` 从 v0.2 的正常值 `mood:2/fullness:3/cleanliness:1.5` 被改成 `400/400/400`；`age_speed_multiplier` 从正常值改成 `5040`。这些是 v0.5 进化测试用的 fast-mode 值，写进了**作为默认配置模板**的 example。后果：新用户或重置后，宠物满值 ~15 分钟衰减到 0，年龄 1 个 wall-clock 天顶 14 个宠物年（7 天阈值秒级到）。`config.py` 的 jsonschema 校验对 decay 上限是 100，**400 反而合法**（不触发回退），所以会真实生效。
- **验证**：`python app.py` 全新环境启动，观察日志/气泡中 mood/fullness 在十几分钟内从 80 跌到 0；或读 `config.example.json` 当前内容（decay 全 400、age 5040）。对照 `设计思路.md §三`（`"decay_per_hour": {"mood": 2, ...}`）与 `版本规划.md v0.5`（fast-mode 应进 config 可调，非默认）。
- **注**：`memory/commit-version-milestone-rule.md` 已记"commit 引用测试数字必须实跑一致"——此处是测试值进了默认模板，方向相反但同类问题。

### H2. `requirements.txt` 把 mac 专属 `pyobjc` 列为共享依赖
- **位置**：`requirements.txt:5`
- **问题**：`设计思路.md §一` 明确"平台额外依赖见平台文档（mac: pyobjc；win: pywin32/ctypes）"，`pyobjc` 是 mac 专属。win 端 `pip install -r requirements.txt` 会尝试装 pyobjc（在 win 上要么失败要么装无意义包）。反之 win 的 ctypes 是标准库不需列、pywin32 完全没列。
- **验证**：在干净 win 虚拟环境 `pip install -r requirements.txt`，看 pyobjc 是否报错/装上；对照 `平台适配与分工.md §一`（三层结构：共享核心不 import 平台库，平台库进 _mac/_win shim）。

### H3. `app.py` 重复 import `os` / `sys`，且 `main()` 描述符版本号过期
- **位置**：`app.py:20-21` 与 `app.py:59-61`（os/sys 两次 import）；`app.py:565` `parser = argparse.ArgumentParser(description="桌宠 v0.5.0")`
- **问题**：① `os`、`sys` 在文件顶部 import 后，argparse/logging/signal 块前又 import 一次（无害但说明早期为插 stderr 过滤管道插入代码后未清理）。② `APP_VERSION = "v0.6.0+win"`（行 78）但 argparse 描述还写 "v0.5.0"，`main()` 启动日志打印 APP_VERSION 是 v0.6.0，两处不一致——单实例锁下用户肉眼易误判版本。
- **验证**：读 `app.py` 行 20-21 与 57-61；`grep -n "v0.5.0\|v0.6.0" app.py`。

### H4. `pet/__init__.py` `__version__` 严重滞后
- **位置**：`pet/__init__.py:3` `__version__ = "0.1.0"`
- **问题**：实际已到 v0.6.x，`__version__` 仍停在 0.1.0。若有任何代码/打包脚本读这个值做版本判断，会误判。
- **验证**：`python -c "import pet; print(pet.__version__)"` 对照 `app.py:APP_VERSION`。

### H5. 聊天面板"流式打字机"Must 未达成，且代码主动丢弃流式
- **位置**：`pet/ui/chat_bridge.py:140-141` `_on_delta` 空实现（`pass`）；`版本规划.md v0.4 Must`
- **问题**：v0.4 Must 明确要求"DS 回复流式打字机"。但 `_on_delta` 注释写"delta 丢弃——不显示流式黄框，DS 回复直接落定为 assistant 气泡"。即流式逐字效果被刻意去掉了，用户看到的是"发送后等几秒一次性出整条回复"。`llm.py` 的 ChatWorker 流式管道、`main.qml` 的 streamingText Property 都建好了，唯独桥接层把 delta 丢了。
- **验证**：读 `chat_bridge.py:139-141`；实机 `python app.py` 设好 DS key，发消息观察是否逐字出现（预期：不逐字，整条弹出）。对照 `版本规划.md v0.4 Must: "DS 回复流式打字机"`。

---

## 🟡 中

### M1. `proactive._decide` 的 LLM 隔离决策实际掺入了宠物人设 system prompt
- **位置**：`pet/proactive.py:202-215`
- **问题**：注释自称"隔离上下文（独立 history，不掺聊天上下文）"，但调用 `self._client.chat_once(history, None)` 时，`DeepSeekClient.__init__` 已把 `SYSTEM_PROMPT`（宠物人设）固化进 `self._system`，每次 `chat_once` 都会前置（`llm.py:236`）。所以"隔离"只是隔离了聊天 history，并未隔离 system prompt——决策器实际仍以宠物人设身份思考，而非纯 JSON 决策器角色。`_DECISION_SYSTEM`（行 46）作为 user 消息塞进去，与人设 system 并存，可能被人设语气带偏。
- **验证**：读 `llm.py:99`（`self._system` 初始化）+ `llm.py:236`（messages 前置 system）+ `proactive.py:210-215`。可构造测试：让 FakeClient 记录收到的 messages，断言第一条 role=system 的 content 是 SYSTEM_PROMPT 而非 _DECISION_SYSTEM。

### M2. `apply_decay` 离线补衰减 + `check_evolve` 瞬时分判分支，离线回来连续进化失真
- **位置**：`pet/pet_state.py:240-272`（apply_decay）+ `277-313`（check_evolve）
- **问题**：离线很久回来（如 fast-mode 或真实多日未开），`apply_decay` 一次性把 age 推过多个阈值。`check_evolve` 一次只进一阶（注释"防刷屏"），但 app `_apply_decay` 每 1s 调一次，第二次 `check_evolve` 会再进一阶——理论上能补齐，但**每次进化都取当前瞬时养护分判分支**。离线期间数值已衰减到底（decay 按时间算），所以离线回来连续进化大概率全判 NEGLECTED。这是 `check_evolve` TODO 注释（行 310）承认的"瞬时分非时间平均"问题，但在离线补衰减场景下尤为失真。
- **验证**：构造测试——store last_update 回拨相当于 30 天，apply_decay 后连续调 check_evolve 直到返 None，观察分支；预期全 NEGLECTED（因数值衰减到底）。

### M3. `sensor_win.solid_at` 的"自身遮挡下探"在多屏不同 DPR 下坐标换算可能错位
- **位置**：`pet/sensor_win.py:232-270`（solid_at）+ `212-229`（_window_below_point）
- **问题**：`solid_at` 主体把逻辑坐标 (x,y) 按所在屏 DPR 换算成物理点 pt 喂 `WindowFromPoint`。但 `_window_below_point` 用 `GetWindowRect` 拿物理矩形与 pt 比对——这部分是对的。然而 `solid_at` 开头的 DPR 查找（行 244-249）遍历 screens 找包含 (x,y) 的屏，如果点恰在屏间隙或边界，`dpr` 保持 1.0，多屏混合缩放下换算会偏。此外 `visible_windows()` 返回的 hwnd 集合在 `_window_below_point` 里没用来过滤（只跳过 own），可能匹配到枚举时被过滤掉的工具窗/cloaked 窗。
- **验证**：在双屏不同缩放（如主屏 100%、副屏 150%）下，宠物站副屏窗顶时观察支撑是否被误否决；或单测 `_window_below_point` 对 cloaked 窗的过滤。这是边界场景，v0.3 工作表也标注"多缩放率专项实测待补"。

### M4. `_refresh_sensors` 每 2s 重建 Sensors 对象，`_tick` 50ms 写 `sensors.mouse_pos` 存在引用替换窗口
- **位置**：`app.py:465-466`（_refresh_sensors）+ `516-518`（_tick 写 mouse_pos）
- **问题**：`_refresh_sensors` 执行 `self.sensors = self.adapter.get_sensors()` 整体替换；`_tick` 每 50ms 读 `self.sensors.mouse_pos` 并赋值。两者都是主线程 QTimer 回调，Qt 事件循环串行执行，**无真竞态**。但若未来 sensor 改成异步或 _tick 改多线程，会有 TOCTOU。当前安全，记为潜在脆弱点。`register_own_windows` 登记的 own_hwnds 是 sensor_win 模块级全局，重建 Sensors 不丢——这点 OK。
- **验证**：读 `app.py` 两个 timer 的连接；确认均为同线程 QTimer（是）。

### M5. `window.py move_bottom_center` 的多屏钳制与 FSM `_clamp_x` 语义不一致
- **位置**：`pet/window.py:127-146`（move_bottom_center 多屏钳制）vs `pet/behavior.py:221-223`（_clamp_x 工作区钳制）
- **问题**：FSM 用 `work_area`（多屏合集）钳制 x 在 `[left+1, right-1]`；window 层 move_bottom_center 用所有屏 geometry 的合集钳制 x 在 `[min_x+w/2, max_x-w/2]`。两者大多数情况一致，但 work_area 排除了任务栏（availableGeometry），而 window 层用的是 `s.geometry()`（含任务栏的整屏）。任务栏占据侧边的屏（如任务栏在左/右），两者钳制边界会差一个任务栏宽度，可能造成 FSM 想去的位置被 window 层再钳一次，视觉上宠物到不了工作区边缘。
- **验证**：win 任务栏放左侧，观察宠物 WANDER 是否能走到屏最左工作区边缘；对比 `s.geometry()` 与 `s.availableGeometry()` 的 x 差值。

### M6. `proactive._quiet_now` 跨午夜判定写法绕且有一处恒真条件
- **位置**：`pet/proactive.py:118-122`
- **问题**：`return start <= 24 and (h >= start or h < end) if start > end else (start <= h < end)`。当 `start > end`（跨午夜，如 23→8）时，`start <= 24` 几乎恒真（start 是小时 0-23，`<=24` 必然成立），这个前置条件无实际作用。逻辑结果对（跨午夜 `h>=23 or h<8` 正确），但 `start <= 24` 是死代码/误植，可读性差，疑似想写别的边界检查。
- **验证**：读行 121；代入 start=23,end=8,h=2 → True（对）；但 `start <= 24` 对所有合法 start 都真，无意义。

### M7. `chat_bridge._on_done` 在 worker 被 cancel 后仍可能处理 done 信号
- **位置**：`pet/ui/chat_bridge.py:131-136`（cancel）+ `143-154`（_on_done）
- **问题**：`cancel()` 调 `worker.cancel()` + `worker.quit()`。但 `ChatWorker.run`（`llm.py:302-330`）里 `cancel` 只设标志，`done` 信号在 `run` 末尾无条件 emit（行 330），除非走异常分支。`quit()` 是 QThread 退出请求，但 `run` 正在 `chat_once` 阻塞中不会立即停。所以关面板后，若 DS 请求恰好完成，`done` 仍会 emit，`_on_done` 把消息追加进已关闭的面板 history——下次打开会看到一条"幽灵"回复。`cancel` 没有断开信号连接。
- **验证**：发一条耗时消息，发送后立即 Esc 关面板，等几秒再打开，看是否多出一条 assistant 回复。

### M8. `tray.py` 托盘图标用 emoji "🐾" 绘制，多平台渲染不一致
- **位置**：`pet/tray.py:62`
- **问题**：`QPainter.drawText` 画 emoji 在不同平台/字体下可能渲染为黑白方块或缺字（win 早期版本、部分 Linux）。托盘图标是 32×32 小尺寸，emoji 渲染失败时图标几乎是空圆。设计思路未要求 emoji 图标，可用纯几何。
- **验证**：win 实机看托盘图标是否正常显示爪印；切换系统字体主题复测。

---

## 🟢 低

### L1. `test_v02_interaction.py` 断言名"菜单五项"与实际六项不符
- **位置**：`spikes/test_v02_interaction.py:87-91`
- **问题**：断言名写"菜单五项"，但期望列表和 window.py 实际都是 6 项（喂食/洗澡/戳一戳/跟随鼠标/设置/退出）。测试能过（列表比对正确），只是名字误导。
- **验证**：读行 87 与行 90 的列表长度。

### L2. `w1_transparent_window.py` 文件头结论"待实测后填写"未填
- **位置**：`spikes/w1_transparent_window.py:15`
- **问题**：注释写"【W1 结论（待实测后填写）】"，但 `工作表.md v0.1` 已记录 W1 结论（窗口即宠物尺寸无穿透问题，全屏 mask 方案留 v0.3）。spike 文件头的结论位一直空着，与工作表脱节。
- **验证**：对照 `工作表.md v0.1 win端` 的 W1 记录与 spike 文件头。

### L3. `behavior.py` `_step_walk` 走出边缘分支后已 return，但函数末尾仍隐含 fall-through
- **位置**：`pet/behavior.py:527-533`（ns > cur_y 分支 return）+ `535-542`
- **问题**：`_step_walk` 中 `ns > cur_y + 1` 分支 `return Action(FALL)`，`ns < cur_y - 1` 分支可能 `_enter_climb` 后 return 或 fall through 到 `ns = cur_y`。逻辑上能走通（climb 分支 return，否则 ns 重置后继续到到达判定），但控制流依赖隐式 fall-through，可读性弱。无 bug。
- **验证**：静态读 `_step_walk` 全函数控制流。

### L4. `app.py _show_chat` 的全屏移 Space 逻辑在 win 上会触发 import objc 失败
- **位置**：`app.py:350-377`（_move_chat_to_desktop_space）
- **问题**：win 全屏时 `_fullscreen=True` 也会调此方法，内部 `from objc import objc_object` 在 win 上抛 ImportError 被 except 兜住，仅打 warning。功能上 win 全屏时聊天面板直接 show 会被全屏盖住（无任何处理），与 mac 行为不一致。设计上 win 全屏下聊天面板该放哪未定义。
- **验证**：win 全屏视频时点托盘"聊天"，观察面板是否可见。

### L5. `llm.py` `_non_stream_once` 假设 `data["choices"][0]["message"]` 必存在
- **位置**：`pet/llm.py:198-204`
- **问题**：DS 返回 200 但 body 异常（如 choices 为空、content_filtering 命中）时，`data["choices"][0]` 会 KeyError 未被 try 包裹。`_stream_once` 的等价路径有 iter 容错，`_non_stream_once` 没有。虽上层 `ChatWorker.run` 有兜底 except 降级，但会把"DS 业务拒绝"误当异常。
- **验证**：mock DS 返回 `{"choices": []}` 调 `chat_once`，观察是否抛 KeyError 被外层吞。

### L6. `bubble.py` `show` 的 `kind` 参数在多处调用被省略，WARNING 永不消失语义未充分用
- **位置**：`pet/bubble.py:51-68`（show 支持 kind）+ `app.py` 多处 `self.bubble.show(msg, anchor=...)` 未传 kind
- **问题**：`BubbleWidget` 实现了 WARNING 需点击关闭、INFO 5s 自动消失（设计思路 §2.4）。但 app 绝大多数调用只传 text + anchor，默认 INFO。`_on_reset_requested` 用了 WARNING（行 451），但其他本该 WARNING 的（如"当前离线"）用了默认 INFO。功能可用，只是 kind 语义未充分贯穿。
- **验证**：`grep "bubble.show" app.py` 看每处是否传 kind。

### L7. `config.py` schema 校验未覆盖 `behavior` / `proactive` 段
- **位置**：`pet/config.py:24-67` `_SECTION_SCHEMAS`
- **问题**：只校验 decay/interaction_gain/score/age_speed/evolve_threshold。`behavior`（walk_speed/gravity 等）和 `proactive`（quiet_hours/sedentary_min）未校验，非法值会直接进 FSM/proactive 导致行为异常（如 gravity 负数、quiet_hours 非元组）。设计思路 §2.2 说"config 加载对数值范围做 jsonschema 校验，非法值回退默认"——未完全落实。
- **验证**：在 config.json 写 `"behavior": {"gravity_px_per_s2": -1}`，启动观察是否报错或回退（预期：不校验，直接用 -1，物理异常）。

### L8. `sensor_mac.idle_seconds` 无 Quartz 时返 0.0，与工作表"待补"状态已不符
- **位置**：`pet/sensor_mac.py:84-94`
- **问题**：`工作表.md v0.6 mac端` 仍标"空闲传感器(NSEvent/CGEvent)"未打 ✅，但代码已实装（`CGEventSourceSecondsSinceLastEventType`），commit `565deed` 标题也确认"mac 补 idle_time 实装"。工作表落后于代码。这是文档同步问题，非代码 bug。
- **验证**：对照 `工作表.md v0.6 mac端` 与 `sensor_mac.py:84-94` + commit `565deed`。

### L9. `proactive.follow_up` 时间语义与 `schedule_wake` 不同但文档未统一说明
- **位置**：`pet/proactive.py:106`（follow_up 用绝对时间戳 `when`）vs `100`（schedule_wake 用相对分钟 `delay_min`）
- **问题**：两个 API 时间语义不同（绝对 vs 相对），签名都合法但易混。docstring 各自说了，但无统一对照。app 调用方 `app.py:328` 用 `_t.time() + 30*60`（绝对）正确。无 bug，仅文档可改善。
- **验证**：读 proactive.py 两处 docstring。

### L10. `pet_state.check_evolve` 用 `_STAGE_ORDER.index` 做 idx+1，FINAL 越界靠前置 return 兜
- **位置**：`pet/pet_state.py:289-299`
- **问题**：`if state.stage == Stage.FINAL: return None` 在前，所以 `idx+1` 不会越界。但依赖前置 guard，若 guard 被误删会 IndexError。可读性弱，可用 `try/except IndexError` 或显式判断更稳。
- **验证**：静态读，确认 guard 在前。

---

## 汇总
- **必处理（H1-H5）**：默认配置污染、依赖分类错、版本号不一致、流式 Must 未达成——其中 H1（fast-mode 值进默认配置）和 H5（流式打字机未实现）直接影响 v0.4/v0.5 的 Must 验收真实性。
- **应处理（M1-M8）**：隔离决策不纯、离线进化分支失真、多屏 DPR 边界、cancel 幽灵回复等，影响健壮性与契约准确性。
- **可选（L1-L10）**：文档同步、命名、防御性编码、配置校验覆盖。

**整体评价**：架构清晰，平台隔离（platform.py 注入 + _mac/_win shim + 共享层零平台库）执行得很干净，物理 FSM 经过多轮精修且有测试覆盖，持久化原子写+migrate 链+ .bak 完整。主要问题集中在**测试值污染默认配置（H1）**和**流式 Must 退化（H5）**——这两个会让"Must 全绿"的结论打折扣，建议优先复核。其余多为健壮性与文档同步问题。
