# 本地 LoRA 生图产线方案（替代 gpt-image-2 批量出图）

> 日期：2026-08-25
> 目标：本地部署绘图模型 + 用现有 GPT 生成图做 LoRA 微调，批量生成与参考**画风/人物特征/外貌基本完全一致**的静态图与动画帧（RGBA 透明），替代高成本的 gpt-image-2 产线。
> 结论先行：**底模选 Anima Base（2B 动漫特化）为主、Z-Image Base 为备**；2026-08 时点没有成熟的"原生透明背景"开源 T2I，走**纯灰底生成 → BiRefNet-ToonOut 抠图 → 已知底色去污**管线；训练器用 **ai-toolkit**（Windows GUI）为主。

---

## 一、现状盘点（已核实）

### 1.1 素材资产
| 集合 | 位置 | 数量 | 规格 |
|---|---|---|---|
| 原始参考图（角色设定源头） | `gptpipeline/final_library/refs/` | 3 张微信图 | JPG 带底（Q版/幼年立绘/成年立绘） |
| GPT 终稿·静态 | `final_library/static/`（=仓库 `assets/ai/`） | 30 | 1024×1536 RGBA 透明 |
| GPT 终稿·帧 | `final_library/frames/` | 48 | 832×1216 **RGB 灰底** |
| 仓库在用帧（已后处理） | `assets/frames/` | 48 | 1024×1536 RGBA（贴底） |
| 历史 LoRA 训练数据 | `gptpipeline/lora_data/` | 90 | 混合 |

角色三变体：`young`（Q版）/ `adult`（成年）/ `final`（定稿设计），共享画风与服装（藏青女仆装+白围裙鲸鱼刺绣+鲸尾+蓝渐变发）。每变体 10 静态（healthy/neglected × happy/hungry/neutral/sad/sleepy）+ 16 帧（walk2/stretch3/roll1/blink2/fall_air+fall_land/chew2/eat_mouse4）。

### 1.2 历史 LoRA 尝试（2026-08-24，效果不满意的原因已定位）
kohya sd-scripts SDXL DreamBooth 训了 `pet_young/pet_adult/pet_final` 三个 LoRA（12 epoch，产物 228MB）。**三个教科书级错误**：
1. **全部图片无 caption**（日志明确警告 30/30 缺 caption）→ 姿势/状态/背景全部折叠进触发词，身份与画风必然糊；
2. **1024×1024 分辨率硬训 1024×1536 素材** → 构图裁切/比例失真；
3. LoRA 体积 228MB（推测 dim≥128）→ 小数据集高秩极易过拟合画风漂移。

### 1.3 硬件与已有组件
- RTX 5080 Laptop **16GB**（Blackwell sm_120，PyTorch cu128+ 已验证可用——kohya 跑通过），系统内存 31.4GB（这是排除 20B 级训练的主要约束）。
- ComfyUI 已装：`ComfyUI-BiRefNet-Hugo`（抠图节点）、`ComfyUI_IPAdapter_plus`、ComfyUI-Manager。
- **`anima-aesthetic-v1.1.safetensors` 已在 `models/diffusion_models/`**，`qwen_image_vae.safetensors` 已在 `models/vae/` —— 主选底模家族的推理侧已经就位。

---

## 二、底模选型（截至 2026-08-25 调研）

| 模型 | 规模 | 许可 | 16GB 训 LoRA | 动漫 Q 版适配 | 判定 |
|---|---|---|---|---|---|
| **Anima Base v1.0**（CircleStone Labs×Comfy Org） | 2B + Qwen3-0.6B TE + Qwen VAE | 非商业权重但**允许商用产出图** | 极轻松，分钟级迭代 | ★★★ 全程动漫预训练（数百万动漫图），512²–1536² 原生窗口 | **首选** |
| **Z-Image Base**（阿里通义，2026-01） | 6B | **Apache-2.0**（最干净） | 舒适（musubi 有 blocks_to_swap） | ★★ 通用底，画风全靠 LoRA 扛 | **备选** |
| FLUX.2 klein Base 4B（2026-01） | 4B | Apache-2.0 | 可训（实测 4B 推理仅 ~9GB） | ★★ 通用底；**原生多参考图条件** | 编辑/参考通道黑马 |
| Qwen-Image-2512 | 20B | Apache-2.0 | 勉强（fp8+swap45 → 12GB 显存但**建议 64GB 内存**，本机 31GB 堪忧） | ★★ 通用底 | 仅推理（Edit-2511 做参考编辑） |
| FLUX.2 dev | 32B | 非商业 | 不可行（Mistral-3 24B TE，内存爆） | — | 排除 |
| SD 3.5 | — | 商业限制 | — | — | 排除（生态停滞） |
| Illustrious/NoobAI/Animagine（SDXL 系） | 3.5B | 宽松 | 轻松 | ★★★ danbooru 生态最厚 | 兜底（SDXL 天花板低） |

**选型逻辑**：画风保真第一 → 预训练分布与目标画风距离最小的底模胜出。Anima 是当前唯一"纯动漫预训练"的新一代模型，30-100 张小数据集需要补的画风鸿沟最小；2B 规模在本机可以快速多轮迭代（对"几十帧都要在模"的需求，迭代速度比单张峰值质量更重要）。`anima-aesthetic-v1.1`（已下载）是它的审美微调版，适合**推理出图**；**训练基底用 Anima Base**（结构更干净，LoRA 兼容性更好），出图时可叠 aesthetic 或直接用 Base。

---

## 三、透明背景方案（2026-08 时点结论）

**没有可投产的原生 RGBA 开源 T2I**：LayerDiffuse（SDXL 时代，2024-06 起弃更）线条边缘发虚；Flux 移植版仅研究级且继承非商业；Qwen-Image-Layered（Apache-2.0，2025-12）是"图→RGBA 分层"分解器，640/1024 桶分辨率不匹配。**结论：灰底生成 + 抠图，是当前唯一可靠路线。**

关键决策——**生成底色用纯灰 `#808080`，不要纯白、不要绿幕**：角色有大量白色区域（白围裙/白色裙撑褶边），白底会让抠图在白布边缘产生 alpha 不确定与光晕；绿幕会污染半透明边缘像素（despill 麻烦）。灰底与黑线稿、白裙两端都拉开对比，且底色精确已知 → 边缘去污可用确定性公式 `F = (C − (1−a)·BG) / a`，无需 ML 去污。

抠图模型选 **ToonOut**（BiRefNet 的动漫特化微调，MIT，arXiv 2509.06839，专为"发丝/线稿/透明元素"修正过，作者称像素准确率 95.3%→99.5%）。**不要用 RMBG-2.0**（CC BY-NC，商用需 BRIA 授权）。备选：BiRefNet-dynamic / HR-matting（均 MIT）、BEN2（MIT）、rembg `isnet-anime` 交叉校验。

ComfyUI 侧：加装 **`1038lab/ComfyUI-RMBG`**（GPL-3.0，2026-07 仍活跃，v3.1.0），一个节点族覆盖 ToonOut + BiRefNet 全家 + sensitivity + Mask Enhancer（offset/blur）+ Refine Foreground（前景色估计去污）+ 直接输出 alpha。现有 BiRefNet-Hugo 节点可退役或做交叉校验。

后处理规范（保护抗锯齿线稿）：
1. **绝不二值化 mask**，保留连续灰度 alpha；
2. `mask_offset = −2px`（切底色光晕）→ `mask_blur = 1px`（恢复柔边）；
3. Refine Foreground 或公式去污；
4. 导出**直通 alpha（非预乘）** RGBA PNG；
5. 棋盘格 + 深色桌面双背景目检褶边/发梢。

---

## 四、训练方案

### 4.1 训练器
- **主：ostris ai-toolkit**（MIT，Windows 一等公民，Web GUI `localhost:8675`；README 明确支持 Anima、Z-Image、Qwen、FLUX.2-klein 等；Windows 一键包 AI-Toolkit-Easy-Install 自带 Python 3.12 + PyTorch cu130 + FlashAttention）。
- 备：kohya musubi-tuner（Windows 已确认，Z-Image/Qwen/FLUX.2 覆盖全；本机 kohya 环境已跑通，迁移成本低）。
- 扩散-pipe 需 WSL2，跳过。

### 4.2 数据集规范（修正上次全部错误）
1. **展平**：所有训练图 RGBA 合成到 `#808080` 纯灰底成 RGB（透明区域不处理会毒化 latent）；
2. **Caption 必写**：每图同名 `.txt`，格式 = `触发词 + 自然语言差异描述`（姿势/表情/镜头/朝向），**画风词不写**（让画风融进触发词与身份）。用本地 VLM 批量生成初稿（fancyfeast **BigASP-3**（2026-08）或 Qwen-VL，musubi 自带 `caption_images_by_qwen_vl.py`）+ 人工过一遍模板统一措辞；
3. **触发词体系**：`whalemaid_young` / `whalemaid_adult` / `whalemaid_final`（或统一 `whalemaid` + 变体词，见 4.4）；caption 里固定写死不变特征（蓝渐变长发、ahoge、鲸耳鳍、鲸尾、藏青女仆裙白围裙鲸鱼刺绣、蓝蝴蝶结），让模型把身份烙进触发词；
4. **不翻转**（角色不对称：蝴蝶结/尾巴朝向）；允许局部裁切变体（脸部/半身），bucketing 处理混合尺寸；
5. **数量**：每变体 30 张终稿已够（社区共识 10-30 张），质量>数量，逐张过质量门（半调抖动残留、肢体崩坏一律剔除）；
6. refs 3 张微信原图**不进训练集**（风格/形态有差异），留作验收对照锚点；
7. train/val：每变体留 2-3 张做验证采样对照。

### 4.3 超参起点（Anima Base，ai-toolkit）
| 项 | 值 |
|---|---|
| resolution | 1024×1536（bucket/multi-res，**不再压 1024 方图**） |
| network dim/alpha | 16/16 起步（上限 32；**严禁上次 228MB 级高秩**） |
| LR | 1e-4，cosine，adamw8bit |
| steps/epochs | 每变体 ~1500-3000 步；`sample_every 250` 出采样图 |
| caption_dropout | 0.05 |
| 正则图 | 不用（现代共识：caption+低秩替代） |

每 250 步采样与 GPT 原图并排出图，**人工挑最佳 checkpoint**（社区标准做法），不默认取最后一个。

### 4.4 三变体策略
**先训 3 个独立 LoRA**（每变体一个，互不污染、可单独重训；本机单轮成本低）。若验收发现跨变体画风不一致，再合并为"单 LoRA 三触发词"方案（共享数据增强一致性）。不推荐风格 LoRA+身份 LoRA 叠加（相互作用不可控）。

### 4.5 帧一致性（动画帧的难点）
- 基线：LoRA + 固定种子 + prompt 差异批量出帧；
- 增强通道（本机 16GB 可跑）：**参考编辑**兜底——把已验收的 GPT 帧或 LoRA 最佳帧喂给 **FLUX.2-klein-4B**（Apache-2.0，实测 ~9GB）或 **Qwen-Image-Edit-2511**（fp8mixed，推理可行）做多参考/姿态迁移，把身份细节"钉"回参考。此通道同时是**数据扩充手段**：用编辑模型从终稿派生新姿势→重新 caption→回填训练集（社区 2025-26 主流扩数据法；纯自举自蒸馏循环标记为实验性）。

---

## 五、批量生成管线（与现有产线对齐）

1. **Prompt 源**：整理 78 张 GPT 终稿的原始生成 prompt（gptpipeline 会话记录导出；拿不到的用 VL 反推+人工校正），形成 `{variant}_{asset}.png → prompt` 清单（30 静态 + 48 帧）；
2. **ComfyUI 工作流**（API JSON + Python 批量脚本）：Anima(Base 或 aesthetic) 加载 → LoRA → 固定种子逐条 prompt 出图，**灰底 #808080**，negative：`shadow, gradient background, vignette, multiple characters`；
3. **后处理链**（脚本化，接 `tools/postprocess_assets.py` 风格）：ToonOut 抠图 → offset/blur/去污 → 中心裁切对齐 → 缩放 1024×1536 → **贴底对齐**（沿用现有贴底约定，`fall_air` 除外）→ RGBA PNG 落 `assets/ai/`、`assets/frames/`；
4. 每张生成保留：种子、checkpoint、LoRA 版本、工作流 JSON → 可复现。

---

## 六、验收门禁（v0.10.19 画风漂移回退的教训制度化）

**任何帧要进 `assets/` 必须过三道门：**
1. **自动指标**：与同位 GPT 原稿的 DINOv2 特征余弦相似度、主色板距离、线稿密度统计、alpha 边缘质量（半透明像素占比）、尺寸/贴底校验——阈值用"GPT 重出图 vs 原稿"样本标定（v0.10.18 那 12 张贴底修复是零画风变化的天然标定集）；
2. **人工 contact sheet 目检**：新图与 GPT 原稿并排大图，逐张过目（用户终审，标准=与参考基本完全一致）；
3. **仓库回归**：替换素材后跑 v03 66/66 + v02 13/13 + v05 16/16 + offscreen 渲染目检（帧播放/贴底/朝向镜像）。

**不过关处置**：重roll（换种子/加参考编辑）→ 仍不过 → **该帧保留 GPT 原图**（混合产线，GPT 管线永久保留为难帧兜底）。绝不为了"全本地"降低验收标准。

---

## 七、分阶段执行计划

| 阶段 | 内容 | 产出 | 预估 |
|---|---|---|---|
| **P0 环境** | 装 `1038lab/ComfyUI-RMBG` + ToonOut 权重；下载 Anima Base + Qwen3-0.6B TE（VAE 已有）；装 ai-toolkit（Easy-Install） | 抠图单图跑通；Anima 出 1 张灰底测试图 | 0.5 天 |
| **P1 数据** | 78 张盘点/质量门 → 灰底展平 → VLM caption + 人工统一 → lora_sets 重建（含 val） | 3 个合格训练集（每变体 ~30 训 + 3 val，带 .txt） | 1 天 |
| **P2 训练** | Anima Base × 3 变体；250 步采样对比；选最佳 checkpoint | `whalemaid_{young,adult,final}.safetensors` + 采样对比图 | 1-2 天 |
| **P3 生成** | prompt 清单整理 → 批量工作流 → 78 张全量生成 + 抠图后处理 | 候选素材集（灰底原图 + RGBA 成品） | 1-2 天 |
| **P4 验收** | 自动指标 + contact sheet 终审；难帧参考编辑重试或保留 GPT | 验收报告（逐张 pass/fail） | 0.5-1 天 |
| **P5 集成** | 过关素材替换 `assets/`，回归测试三件套，commit | 新版本提交 | 0.5 天 |

总计约 **4.5-6 个工作日**，本地成本≈电费。P2 结束即有明确 go/no-go 点：**若 Anima LoRA 采样与 GPT 原稿并排仍达不到"基本完全一致"，升级路径 = Z-Image Base 重训（P2 备用跑道）→ 仍不行则维持 GPT 主产线、本地线只做草稿/扩充**。

## 八、风险清单
1. **画风保真不达标**（最大风险）：小数据集 LoRA 复刻"GPT-image-2 特有质感"有不确定性 → 缓解：动漫原生底模、低秩防过拟合、参考编辑钉细节、GPT 兜底；
2. Anima 生态年轻（2025 末发布），工具链可能有毛刺 → 备用 Z-Image/musubi 跑道；
3. 帧间抖动（逐帧独立生成细节漂移）→ 固定种子+参考编辑通道+验收门禁；
4. 白色服装抠图边缘 → 灰底+ToonOut+双背景目检已针对性处理；
5. 许可：Anima 权重非商业但**产出图可商用**；Z-Image/klein 全 Apache-2.0；训练数据是自有 GPT 产出，无传染问题；避免 RMBG-2.0 入产线。

## 附录：下载/安装清单
- Anima Base：`huggingface.co/circlestone-labs/Anima`（split_files：diffusion_models / text_encoders `qwen_3_06b_base` / vae `qwen_image_vae` 已有）
- ai-toolkit：`github.com/ostris/ai-toolkit`（或一键包 `Tavris1/AI-Toolkit-Easy-Install`，需驱动 ≥580，本机 610.88 ✓）
- ComfyUI-RMBG 节点：`github.com/1038lab/ComfyUI-RMBG`；ToonOut 权重：`huggingface.co/joelseytre/toonout`
- 备用跑道：Z-Image Base `huggingface.co/Tongyi-MAI/Z-Image`（Base 2026-01-23）+ musubi-tuner `github.com/kohya-ss/musubi-tuner`
- 参考编辑：FLUX.2-klein-base-4B `black-forest-labs/FLUX.2-klein-base-4B`；Qwen-Image-Edit-2511（fp8mixed 单文件）
- caption：fancyfeast/BigASP-3 或 musubi `caption_images_by_qwen_vl.py`
