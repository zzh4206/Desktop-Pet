# 桌宠仓库静态只读 Review 记录（含解决方案）

> 日期：2026-08-21
> 范围：全量源码（app.py + pet/ 21 个 .py + ui/ 4 桥接 + 3 QML）+ config.example.json + requirements.txt + 4 份契约文档 + 浸泡报告 + spikes/ 全部 10 个脚本
> 方式：逐文件静态阅读，对照接口契约、git 历史 与 `REVIEW-2026-08-17.md`；关键结论做了最小实跑验证（不启动 GUI、不发网络）
> 严重度：🔴 高（崩溃/功能全挂/安全） / 🟡 中（健壮性/语义偏差/明显 UX 缺陷） / 🟢 低（整洁/文档同步/防御性）
> 每条含：**位置 / 问题 / 验证 / 解决方案**

---

## 〇、进度确认

- HEAD = `5000a8e`（v0.9.2）；`ae2487a`（v0.11.0 win 热键+自启）为乱序插入提交，标题以"——"截断。
- 按 `版本规划.md` 口径：v0.1–v0.9 完成，v0.11 win 端完成（MacPlatformAdapter 未覆盖 `start_hotkeys/set_autostart`，mac 端 v0.11 待认领）；剩 v0.10 / v0.12 / v1.0。
- 实跑复核纯逻辑套件：v06 主动关怀 **25/25**、v08 危险拦截 **18/18**、v03 FSM **62/62** 全绿（数字大于 commit 所记 20/20、33/33 系后续增补用例）。v07/v09 涉及 GUI/钩子未跑。
- 总体架构评价：平台隔离干净、持久化原子写+migrate+.bak 完整、FSM 物理有 34 用例锁行为。**但 v0.9.x 引入了两处使真实聊天链路完全不可用的高危回归（H1/H2），现有测试全部使用 FakeClient，恰好绕开。**

---

## 一、🔴 高危

### H1. `_setup_chat` 引用未定义变量 `paths` → 配置了 API key 时启动必崩
- **位置**：`app.py:326`
- **问题**：`paths` 只在 `__init__`（L108）和 `main()`（L815）局部存在，`_setup_chat` 内无赋值。v0.9.0（c0d0fde）把记忆初始化搬进 `_setup_chat` 时引入。无 key 时走 L301 early-return 掩盖了此行——冒烟/测试都在无 key 环境跑的。
- **验证**：设置 `DEEPSEEK_API_KEY` 后 `python app.py` → `NameError: name 'paths' is not defined`；`grep -n "paths" app.py` 对照 L271–330 可见方法内无定义。
- **解决方案**：方法开头取 `paths = self.adapter.get_paths()`（或 `__init__` 把 `paths` 存为 `self._paths` 后引用）；同时给 `_setup_chat` 整体包一层 try/except——key 存在时聊天初始化失败不应炸整个启动（宠物本体仍应可跑，与"无 key 只禁聊天"同语义）。

### H2. `__init__` 尾部三行初始化被误挪进 `set_memory_context` → 真实请求必挂 + 记忆注入静默失效
- **位置**：`pet/llm.py:120-124`
- **问题**：
  ```python
          content = self._base_system + segment
          self._system = {"role": "system", "content": content}
          self._timeout = timeout      # ← 此作用域无 timeout
          self._resp = None
          self.usage = Usage()
  ```
  后果四连：① 实例上没有 `_timeout`/`_resp`/`usage`；② 首次 `chat_once` 在 `requests.post(timeout=self._timeout)` 抛 AttributeError → 聊天永远走降级回复；③ `usage.add` 同样 AttributeError；④ `set_memory_context` 每次 NameError（被 `app._on_user_message` 的 try/except 吞成 warning）→ **长期记忆注入静默失效，v0.9 Must 不成立**。已实跑复现：`_timeout attr: False; usage attr: False; set_memory_context raises NameError name 'timeout' is not defined`。
- **验证**：上述三条命令可直接重放；测试全绿是因为 FakeClient/monkeypatch 从未实例化真 client 走网络路径。
- **解决方案**：三行移回 `__init__`（`self._timeout = timeout`、`self._resp = None`、`self.usage = Usage()`），`set_memory_context` 只保留 system 替换两行。
- **配套补测试**：加一条**真客户端冒烟**（不联网即可）：实例化 `OpenAICompatibleClient` 后断言 `hasattr(c,'_timeout') and hasattr(c,'usage')`、`set_memory_context('')` 不抛——现有矩阵全是 FakeClient，这正是两颗雷漏网的原因。

### H3. `file_search` PowerShell 单引号注入
- **位置**：`pet/tools_win.py:166-167,186-190`
- **问题**：pattern/scope 黑名单 `< > | : $ " / \` 加 `".."` **不含单引号 `'`**，而命令用单引号拼接 `Get-ChildItem -Path '{root}' ... -Filter '{pattern}'`；含 `'` 的 pattern 逃逸引号可执行任意 PS 命令。DS 被 prompt 注入即可触达；`dispatch` 通用黑名单（`../`、`rm -rf` 等）拦不住此类注入。
- **验证**：单测直调 `FileSearchHandler().execute({"pattern": "x'; Start-Process calc; echo 'y"}, ctx)` 看计算器弹出。
- **解决方案**（任选其一，建议①+②叠加）：
  1. 把 `'` 加入 `_PATTERN_BAD` 与 `_SCOPE_BAD`；
  2. 放弃字符串拼接，改 `-EncodedCommand`（UTF-16LE base64）或 PS 数组参数传递；
  3. mac `mdfind` 走 argv 无此问题，无需改（保持对照）。

### H4. 滚屏摘要切碎 tool 轮配对（聊天永久报错）+ 主线程同步网络调用
- **位置**：`pet/ui/chat_bridge.py:146-185`
- **问题**：
  1. `_history` 含 role=`tool` 的 ChatTurn（`_on_done` L225 全量追加），而 `_maybe_summarize` 的阈值判断 `len>20` 把 tool 轮计入、压缩按 `[:10]` 定长切片物理删除——**切片边界落在 assistant(tool_calls) 与其 tool 结果之间时**，之后每次 send 喂 DS 都是非法序列（assistant 带 tool_calls 无 tool 消息）→ 400 且无自愈路径；
  2. 摘要的 `chat_once` 在 `send()` 里主线程同步执行，read 超时 120s 冻住整个 UI（含宠物 50ms tick）——正是 v0.6.2 给 proactive 修掉的问题。
- **验证**：① 构造前 10 条含跨边界 tool_calls/tool 配对的 history，调 `_maybe_summarize` 后检查配对完整性（现 T11 只有 user/assistant 交替测不到）；② 实机连续调工具推过 20 轮观察后续发送持续失败；③ FakeClient 加 sleep(3) 感受发送冻结。
- **解决方案**：
  1. 切片按"完整轮次"对齐：从头部向后扫描，跳过 tool 轮并保证 assistant(tool_calls)+tool 成组整体删除（或最简：摘要只在 history 尾部最近 K 条全为 user/assistant 时触发，否则推迟到下一轮）；
  2. 摘要异步化：复用 ChatWorker/_ProactiveWorker 模式丢后台线程，完成后替换历史；失败保留原文语义不变；
  3. 补一条含 tool 轮的摘要单测锁行为。

---

## 二、🟡 中危

### M1. 空闲秒数 32/64 位混算，开机 49.7 天后 idle gate 失效
- **位置**：`pet/sensor_win.py:351-358`
- **问题**：`GetTickCount64()`（64 位 ms）减 `dwTime`（DWORD 32 位 tick）。uptime 越过 2^32 ms 后差值恒多 ≈4294967 s → idle_time 常年巨大，铁律5"idle≥5min 才吃鼠标"与"挂机 ≥2h 降级"全部失真。
- **验证**：开机超 49.7 天机器上动一下鼠标立刻查 `build_sensors().idle_time`（应≈0 实际≈4.29e6 s）；或 mock GetTickCount64 返回 2^32+1000。
- **解决方案**：两侧统一 32 位域再相减：`now32 = _tick_count_ms() & 0xFFFFFFFF`，`(now32 - info.dwTime) & 0xFFFFFFFF) / 1000.0`（回绕天然正确）；或改用 `GetTickCount()`（本身就是 32 位）。

### M2. Ctrl+Alt+T 双重注册冲突（v0.11 × v0.7 叠加）
- **位置**：`pet/hotkey_win.py`（启动即注册）× `pet/mouse_lock_win.py:250-267`（吃鼠标期间再注册）
- **问题**：HotkeyManager 启动即全局持有 Ctrl+Alt+T；吃鼠标时钩子线程再注册同一组合必然失败，每次吃鼠标刷"热键注册失败(被占用?)"warning。逃生靠 hotkey_win 的 on_spit 兜底所以能用，但版本规划 v0.11"统一管理"未落实；config 改 spit 键后吃鼠标内部仍硬编码尝试旧 T 键（`mouse_lock_win.py:44`）。
- **验证**：实机触发一次吃鼠标看日志；临时注销 HotkeyManager 对比。
- **解决方案**：吃鼠标期间不再自注册热键，统一走 HotkeyManager 的 on_spit（`mouse_lock_win.start` 删除 RegisterHotKey 段，`_hook_loop` 只泵消息处理 WM_QUIT/测试消息）；若要保留独立逃生口，则 HotkeyManager 对 spit 键改为吃鼠标开始时 Unregister、结束时还原（复杂度高不建议）。

### M3. 权限页"强制吐出热键"自检恒假阴性
- **位置**：`pet/ui/perm_bridge.py:79-88`
- **问题**：探测 `RegisterHotKey(None, 0xB08, ctrl|alt, VK_T)`，而本 app HotkeyManager 已持有该组合 → 只要 v0.11 热键活着此项永远显示 ✗ 被占用。v0.11 对 v0.8 页面的回归。
- **验证**：运行 app 打开右键→设置页看该项；禁用 HotkeyManager 后对比。
- **解决方案**：探测键改用一个 app 不占用的组合仅验"API 可用"，或改为向 HotkeyManager 查询注册状态（`_reg_ok`/active 属性透出），权限页显示真实注册结果。

### M4. 权限页两项自检有真实副作用
- **位置**：`pet/ui/perm_bridge.py:69-77`
- **问题**：`_check_ll_hook` 真·安装 0.3s WH_MOUSE_LL 钩子（打开页面瞬间鼠标冻结 0.3 秒）；`_check_clipboard` 用 `"perm-check"` 覆盖用户剪贴板（数据丢失）。
- **验证**：复制一段文字 → 打开设置页 → 粘贴；打开页面瞬间晃鼠标。
- **解决方案**：LL 钩子项改为结构性检测（SetWindowsHookEx 后立即 Unhook、不进消息循环不吞事件，耗时 <10ms 无感）；剪贴板项改为只读探测（`Get-Clipboard` 或 PowerShell 空查询），不再写 "perm-check"。

### M5. `_quiet_end_after` 月末深夜 ValueError（已实跑复现）
- **位置**：`pet/proactive.py:473-481`
- **问题**：`end_today.replace(day=d.day + 1)` 注释自称"datetime 自动进位"——replace 不进位。31 号（及 4/6/9/11 月 30 号、平年 2 月末）quiet 时段内唤醒到期：poll 抛 ValueError，且 `_next_wake_at` 未更新导致 30s 轮询反复炸到午夜。复现：`2026-01-31 23:30 → CRASH: ValueError day is out of range for month`。
- **验证**：重放上述两条命令。
- **解决方案**：改 `end_today + datetime.timedelta(days=1)`；补月末日期的单测（FakeClock 打在 1-31 23:30）。

### M6. UIPI 降级检测基本不可能触发
- **位置**：`pet/mouse_lock_win.py:104-124`
- **问题**：`OpenProcess(PROCESS_QUERY_LIMITED_INFORMATION)` 对管理员进程通常成功（limited query 本就是跨完整性级别读取设计），`get_last_error()==5` 几乎不命中 → "前台管理员窗口只气泡不抑制"门禁形同虚设（LL 钩子抑制本身仍生效故表现碰巧正常）。
- **验证**：非提升 python 调 `_foreground_elevated()` 且前台放管理员 cmd → 期望 True 实际 False。
- **解决方案**：换可靠判据，任选：① `GetTokenInformation(TokenElevation)` 对前台 pid 开 PROCESS_QUERY_LIMITED_INFORMATION 读其令牌提升态（limited 权限即可读）；② `IsProcessDPIAware` 类不可靠，勿用；③ 简化口径：检测前台窗口类是否 UAC 提升对话框（`#32770` + Consent/UIAutomation 屏蔽）+ 保留现逻辑作兜底。同时补一条真机用例（管理员 cmd 前台时 start 应返 False）。

### M7. 热键回调在非 GUI 线程操作 Qt
- **位置**：`pet/hotkey_win.py:168-176`（回调在热键线程直接执行）
- **问题**：WM_HOTKEY 在热键线程调 `on_chat`（QML 窗口 show/hide/raise）、`on_conflict`（`bubble.show` 创建 QWidget）——Qt GUI 操作跨线程是未定义行为；on_spit → `fsm.handle_event` 也跨线程改 FSM 状态（与主线程 50ms step 有竞态窗口）。
- **验证**：连续快速按 Ctrl+Alt+P 数十次观察 stderr QObject 线程告警/偶发异常。
- **解决方案**：HotkeyManager 回调一律经 Qt 信号转发主线程——给 HotkeyManager 一个 QObject 化的信号桥（`hotkey_fired = Signal(int)`，`start()` 收三个普通 callable 后在主线程 connect 到实际 handler），或在 app 侧包一层 `QMetaObject.invokeMethod(..., QueuedConnection)`。FSM 事件同理经主线程队列投递。

### M8. win 活跃内容检测绑定"全屏"，与 mac 及 Must 文案不一致
- **位置**：`pet/platform.py:604-614` × `pet/sensor_win.py:348`
- **问题**：取 `foreground_fullscreen()` 的进程名，而该名字仅在 fs=True 时返回 → **窗口化**播放视频不触发白名单豁免；mac 版 `frontmost_app_name()` 不要求全屏。Must 文案是"前台播放视频（白名单应用）"。
- **验证**：窗口化 Films&TV/PotPlayer 播放视频 + 构造久坐/idle 条件，观察照吃不误。
- **解决方案**：win 补一个不依赖全屏的前台进程名管道（`GetForegroundWindow`→pid→`QueryFullProcessImageNameW`，sensor_win 已有全部零件，抽一个 `foreground_process_name()`），`is_active_content` 改用它；`foreground_fullscreen` 保持原样供全屏检测。

### M9. 托盘自启勾选态不同步
- **位置**：`pet/tray.py:33-36,56-58`
- **问题**：`set_autostart_state` 只写内部标志不动 `act_auto.setChecked`。自启已启用时重启 app，托盘菜单仍显示未勾选。
- **验证**：开启自启 → 重启 app → 看托盘"开机自启"勾选状态。
- **解决方案**：`set_autostart_state` 内保存 QAction 引用并 `setChecked(enabled)`（注意 `setChecked` 会触发 toggled → 回环调用 `_emit_autostart`，需先 `blockedSignals` 或用 `action.setChecked` 前 disconnect，推荐 `act_auto.blockSignals(True); act_auto.setChecked(...); act_auto.blockSignals(False)`）。

### M10. window 层与 FSM 钳制不一致（旧 review M5，未修）
- **位置**：`pet/window.py:135-143` vs `pet/behavior.py:_clamp_x`
- **问题**：window 层用整屏 `geometry()` 合集钳 x，FSM 用 `availableGeometry` 工作区钳 x；任务栏停靠左/右时差一个任务栏宽度，宠物到不了工作区边缘。
- **验证**：任务栏移到屏幕左侧，观察 WANDER 能否贴到最左工作区边缘。
- **解决方案**：`move_bottom_center` 的横向钳制改按各屏 `availableGeometry()` 合集（纵向保留整屏范围允许抛出屏幕顶的视觉缓冲，或同样收紧——与 FSM 语义对齐为准）。

### M11. memory.recall 名为 TF-IDF 实为词面 overlap
- **位置**：`pet/memory.py:142-166`
- **问题**：IDF 表 `df` 构建后零消费（死代码），打分就是 token 重合率；高频词（"主人""喜欢"）不降权。功能可用但算法声明失真（工作表写"TF-IDF(中文2-gram)"）。
- **验证**：读该段；对比高频词低相关 vs 低频词高相关的得分是否符合 IDF 预期。
- **解决方案**（二选一）：① 实现 IDF：`score = overlap_idf*0.5 + importance*0.3 + recency*0.2`，其中 overlap 按 `sum(idf[t] for t in common)/sum(idf[t] for t in q_tokens)`；② 若有意简化，删掉 df 死代码并把文档/工作表的"TF-IDF"改成"词面重合率"。

### M12. `eat_mouse` 成功路径无 return，违反冻结签名
- **位置**：`pet/proactive.py:234-305`
- **问题**：注解 `-> EatMouseSession`（§2.2 冻结接口），全门禁通过走到函数尾隐式返 None。当前调用方不接返回值所以无感，属契约破损。
- **验证**：静态读控制流；或断言 `s.eat_mouse(10)` 返回值类型。
- **解决方案**：函数末尾补 `return sess`（其余分支均已 return sess，风格统一）；顺手把 §七 门禁不过时的返回语义写进 docstring。

### M13. 气泡 WARNING"点击关闭"未实现 + 流式占位未转义 HTML
- **位置**：`pet/bubble.py`（无鼠标事件处理）/ `pet/ui/main.qml:187`
- **问题**：WARNING 只能等下一次 show/drag 隐藏（§2.4"需点击关闭"缺一半）；流式占位把未转义的 DS 增量按 RichText 渲染，最终消息却经 `html.escape`——不对称，构成本地 HTML 注入面。
- **验证**：让 DS 输出 `<h1>x</h1>`，流式期间与落定后对比渲染。
- **解决方案**：① BubbleWidget 装 MousePressEvent → 点击即 hide()（WARNING 语义补齐，顺带 INFO/CHAT 点关也符合直觉）；② 流式文本走同一套 `_md_to_html`（或至少 html.escape）后再绑 RichText——可在 ChatBridge 维护 `streamingRich` Property。

---

## 三、🟢 低

| # | 位置 | 问题 | 解决方案 |
|---|---|---|---|
| L1 | `platform.py:131-133,145-147` | 基类 `set_ds_key` 定义两次，后者 no-op 覆盖前者（子类各自覆盖暂无实害） | 删第一个定义或删第二个，保留委托 `set_llm_key` 版本 |
| L2 | `app.py:78` / `pet/__init__.py:3` | `APP_VERSION="v0.7.4+win"`、`__version__="0.6.2"` 落后 HEAD；多处注释引用不存在的"v0.12.1" | APP_VERSION 提到 v0.9.2（或引入单一版本源，打包前再治理）；注释里的 v0.12.1 改为实际 commit 号 |
| L3 | `spikes/w1_transparent_window.py:15` | W1 结论位仍"待实测后填写"（结论在工作表 v0.1） | 把工作表 W1 结论抄入文件头 |
| L4 | `spikes/test_v02_interaction.py:87-91` | 断言名"菜单五项"实为六项 | 改名"菜单六项" |
| L5 | `pet/sensor_mac.py:72,214` | `_DESKTOP_OWNERS` 含 "Finder" 按 owner 整体过滤，普通访达窗口一并被排除（不可站/爬/全屏忽略），win 只排桌面类 | mac 过滤条件改为"owner==Finder 且 layer!=0 或 bounds 覆盖整屏"之类更精确的桌面窗判定，普通 Finder 文件夹窗放行 |
| L6 | `tools_win.py` vs `tools_mac.py` | win 无 `lock` 工具（规划 v0.8 要求同步补全；可用 `LockWorkStation`）；win process 无 mac 式关键进程 denylist（explorer.exe 可杀） | win 补 LOCK_SCHEMA+rundll32 user32 LockWorkStation；process 补 explorer/csrss/winlogon 等 denylist 对齐 mac |
| L7 | requirements.txt / 平台文档 | pyobjc 已加 marker（旧 H2 ✔）；psutil 被设计思路列为共享依赖但仓库零引用；平台文档 §四/§六 仍写 EnumWindows(pywin32)/PowerShell 音量/shutdown 睡眠，实际 ctypes/ctypes CoreAudio/rundll32 | psutil 标注 dev-dependency 或从共享列表移除；平台文档两处命令映射改为与实现一致 |
| L8 | `config.example.json` | `"llm.last_provider"`、顶层 `"provider"` 无代码消费，多 provider 每次启动弹选择框不记忆 | 实现：选择后 `set_llm_key` 时顺手写 last_provider（或存 config），启动优先用它；或删键 |
| L9 | `pet/memory.py:76-92` | load 不过滤数值字段类型，损坏条目在 recall 打分/排序 TypeError | load 时对 importance/recall_count 做 try-float 缺省兜底 |
| L10 | `pet/tools_schema.py:233-245` | confirm 跨线程等待无超时（主线程模态阻塞时 worker 永久挂起）；`__import__(...)` hacky | call_blocking 加超时（如 60s 超时返 False fail-closed）；顶部正常 import QThread |
| L11 | `app.py:728-731` | `_tick` 里 `except Exception: pass` 吞 eat_mouse_tick 全部异常 | 至少 `log.warning(..., exc_info=True)` 限频打点 |
| L12 | `pet/sensor_win.py:177` | `_windows_cache is not None` 死条件（初始 `[]`） | 直接删该条件 |
| L13 | `spikes/test_v07_eat_mouse_win.py:235-237` | G1 断言引用未定义 `FakeLockInstance`，靠 `if False else` 惰性求值才没炸 | 重写为 `(ev3 == [] and pr4._eat_pending is None)` 单一表达式 |
| L14 | `spikes/test_v09_memory.py:211-212` | T12 open_path 真弹资源管理器窗口 | 断言返回元组结构即可，去掉真打开；或打开后自动关闭 |
| L15 | 文档漂移 | ① 设计思路 §2.5 重置语义仍是"清 pet_state+memory"，实现保留记忆（工作表 v0.9 已记录但设计文档未同步）；② 模块树列 renderer.py/hotkey_mac.py/permissions_*.py 不存在且未标注"待补"；③ behavior/platform 注释多处称"mac 不填 solid_at→纯几何"，实际 sensor_mac 已实装 | 设计思路 §2.5/§2.2 补记"重置保留记忆"决策与理由；模块树标注 mac 待补项；过时注释清理 |

---

## 四、旧 Review（2026-08-17）处置状态核对

| 旧编号 | 内容 | 状态 |
|---|---|---|
| H1 fast-mode 值污染 example | ✅ 已修（decay 2/3/1.5、multiplier=1） |
| H2 pyobjc 平台 marker | ✅ 已修（`sys_platform == "darwin"`） |
| H3 argparse 版本描述 | ✅ 描述已改用 APP_VERSION；但 APP_VERSION 本身又滞后（本轮 L2） |
| H4 `__version__` 滞后 | 🔶 更新到 0.6.2 后又滞后（本轮 L2） |
| H5 流式打字机被丢弃 | ✅ 已修（`_on_delta` 真累积 + QML footer） |
| M1 决策掺入人设 prompt | ✅ 已修（system_override + tools_override，T8 锁行为） |
| M2 离线进化瞬时判分 | ✅ 已修（avg_score 快照 + 循环补阶，v05 M2 测试锁定） |
| M3 多屏 DPR 边界 | ➖ 未动（维持已知限制，工作表标"专项实测待补"） |
| M4 sensors 替换 TOCTOU | ➖ 维持原判（同线程 QTimer，无实害） |
| M5 钳制不一致 | ❌ 未修（本轮 M10） |
| M6 quiet_now 恒真死条件 | ✅ 已修 |
| M7 cancel 幽灵回复 | ✅ 已修（disconnect + `_worker None` 双保险，C2 锁定） |
| M8 托盘 emoji 图标 | ➖ 未动（仍 drawText "🐾"） |
| L1/L2 | ❌ 未修（本轮 L4/L3） |
| L5 `_non_stream_once` KeyError | ✅ 已修（choices 空兜底 FALLBACK_REPLY） |
| L7 schema 未覆盖 behavior/proactive | ✅ 已修（v0.4.13/v0.7.2 两轮；llm/hotkeys 段仍未覆盖，见本轮 L8 类） |
| L8/L9/L10 | ✅ 相应文档/代码已跟进 |

---

## 五、汇总与建议处理顺序

- **统计**：🔴 4 项 / 🟡 13 项 / 🟢 15 项；另确认旧 review 8 项已修复、3 项维持已知限制、3 项未修转入本轮。
- **第一批（立即）**：H1 + H2 —— 同一批 v0.9 提交引入、互相叠加："配了 key 就崩，不崩聊天也是死的，记忆注入还是静默失效"。修复必须配套一条**真客户端冒烟测试**（现有矩阵全是 FakeClient 是漏网根因）。
- **第二批**：H3（注入面收口）+ H4（摘要配对 + 异步化，v0.9 Must 的真实性依赖它）。
- **第三批（定时炸弹类，尽早清）**：M1（开机 49.7 天）、M5（月末深夜）、M7（按键瞬间跨线程 GUI）——触发条件看似边角，但都是"用户日常挂着就一定会撞上"的时间问题。
- **第四批**：M2/M3/M4（v0.11 与 v0.7/v0.8 叠加回归，一起收拾）、M6/M8（门禁有效性）、其余 🟡 与 🟢 随手修。
- **流程改进建议**：commit 信息引用的测试数字与套件现状已出现漂移（20/20 vs 实跑 25/25 属良性，但说明"全套件 N/N"口径需要每次实跑刷新）；v0.9 两颗雷的共性是**新增集成点没有最小真机/真客户端验证**，建议里程碑质量门里加一条"每个新 Must 至少一条不走 mock 的冒烟用例"。
