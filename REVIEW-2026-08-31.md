# 桌宠仓库全量 Review 记录（2026-08-31）

> 审查起点 HEAD `4cc7aa0`（v0.14.18b）
> 范围：全量源码通读（app.py + pet/ 全部 26 文件 + rig/ + ui/ 全部 QML + tools/ 8 + spikes/ 全部测试与 _qa 探针 + assets manifest ×3 + config.example）+ 两路并行探索（mac 侧六文件 / spikes 测试目录）+ 关键发现第一手复核（资产做像素/门禁实证、15 套件全量实跑基线）
> 上游：`REVIEW-2026-08-28.md`（其移交项逐条核对，见 §五）
> 基线：15 套件全绿（13/10/13/16/28/20/33/32/4/30/25/13/34/30/9=280 通过）；资产门禁六 figure 实测（见 N1）

---

## 〇、审查期间现场发现（在途工作区）

**W0 🟡 工作区三处未提交改动 + 一份未跟踪文档**
- `assets/rig/final/parts/leg_[lr]_neglected_neutral.png`：相对 HEAD **纯裁除**（leg_l −162px / leg_r −236px，新增 0）——trim_part_overlap 工艺产物未入库；现行门禁（交叠 0px / 内部 0.107%）PASS。
- `spikes/_qa/split_final_healthy_side_leg_front.png`：QA 产物刷新。
- `本地LoRA产线方案-2026-08-25.md`：方案文档未跟踪（postprocess_assets 废弃注记与记忆索引均引用它）。
- 处置：批次 0 随本审查留档一并入库（资产为纯改进，门禁实证）。

---

## 一、Critical

本轮无新增 Critical。上轮 C1（双腿同域拆两遍）已由批次 F 修复并经 T7 门禁守住（本轮实测全资产交叠峰值 0px）。

## 二、High

**H1 🔴 rig/paperdoll 档进化后展示回退到旧阶段立绘——spec 终生绑定启动时阶段**
- `pet/rig/presenter.py:86`（`load_rig_spec` 仅在 `build_rig_window` 构造期调用一次）× `app.py:144-146`（进化后无重载路径）。三阶段 manifest 共用 figure 键（`healthy_neutral` 等，已核实）→ 进化后 `_resolve_display`（presenter.py:409-420）把 adult/final 的 neutral/侧身图映射到 **young 阶段的派生核心图**，部件亦是 young 的 px_rect——宠物视觉上"长不大"且腿件错位，直到重启才恢复。mood 图因键不在 manifest 恰好幸免。
- 影响：`presentation=rig/paperdoll` + 跨阶段进化（v0.5 Must"阶段切换"在 rig 档失效）。frames 档不受影响。
- 修法：RigWindow 增 `set_stage(stage)`（重载 spec + 重建 partsModel + 清源尺寸缓存），app 在 `on_change` 里检测 stage 变化调用；无新阶段 manifest 时保持旧 spec（降级铁律不动）。
- 验证：v13/v14 回归 + 新增进化换档用例（T 系列：set_stage 后 neutral 显示新阶段核心图、部件表换档）。

**H2 🔴 无 LLM key 时长期记忆整体瘫痪——记忆页打不开、MemoryStore 从不加载**
- `app.py:329-343`：`_setup_chat` 在无 key 分支提前 return，MemoryStore 加载（:370）、Mem/Perm singleton 预注册（:378-392）全部跳过；托盘"记忆管理"→ `_show_mem` → QML `import PetMem` 无注册 → rootObjects 空 → 气泡"记忆页加载失败"。
- v0.9 Must"UI 能查看/删除/清空"对无 key 用户失效；即便有历史 memory.json 也看不了。
- 修法：MemoryStore 加载 + 记忆工具注册 + mem/perm singleton 预注册移到 key 依赖**之前**（registry 创建同样提前）；LLM client 三重实例创建保持 key 依赖不变。
- 验证：v09 回归 + 新增"无 key 路径 mem bridge 可用"用例。

**H3 🔴（mac）活跃内容门禁实质失效——frontmost_app_name 恒返回宠物自身**
- `pet/mouse_lock_mac.py:154-182`：宠物窗 NSFloatingWindowLevel 浮于最前且 layer=0 → CGWindowList 首个 layer-0 窗口几乎恒为宠物自身 → `is_active_content` 恒 False。用户前台看 IINA/QuickTime 时"只气泡不吃鼠标"门禁（v0.7 Must T8）在 mac 从不触发。win 端因宠物是不激活工具窗无此问题。
- 修法：遍历时跳过 `kCGWindowOwnerPID == os.getpid()` 的窗口，取第一个非自身 layer-0 owner。
- （mac 侧改动本机不可实跑，仅 py_compile + 静态核对，留 mac 实机复核项。）

## 三、Medium

**平台/传感器**
- M1 🟡 混合 DPI 多屏变换仍错（上轮 M3 修复不完整）：`sensor_win.py:160` 物理原点按 `sg.x()*dpr` 假设，仅主屏/同 DPR 成立；主 1.5× + 副 1.0× 并排时副屏窗口逻辑坐标偏 +1280px 量级（FSM 站顶/攀爬/图层探针全建立在错位数据上）。`solid_at`（:276）的 `x*dpr` 同根。修法：EnumDisplayMonitors+GetMonitorInfoW 拿物理 rcMonitor，按 `szDevice == QScreen.name()` 精确配对，逐屏 `logical = origin + (phys − rcMonitor原点)/dpr`；配对失败回退现行近似。
- M2 🟡 win 文本编码双漏（中文用户实害）：(a) `tools_win.py:145` clipboard set 经 `clip.exe`——clip 按 OEM 代码页读 stdin，UTF-8 写入的中文进剪贴板即乱码；(b) `_ps_run`（:100-111）按 utf-8 解码，但 PowerShell 5.1 管道输出默认系统 OEM（zh-CN=936）→ `Get-Clipboard`/`Get-ChildItem` 的中文路径/内容全成替换符。修法：`_ps_run` 命令前缀 `[Console]::OutputEncoding=[Text.Encoding]::UTF8;`；clipboard set 改 `[Console]::InputEncoding=UTF8; [Console]::In.ReadToEnd() | Set-Clipboard`。本机已实证两向乱码与修复后正确。
- M3 🟡 `memory.py:90-119` load 顶层非 dict（如 `[]`）→ `data.get` AttributeError 崩 `_setup_chat` → 聊天整体禁用（pet_state 有 isinstance 守卫，memory 没有）。修法：类型守卫 + 告警跳档。
- M4 🟡 吃鼠标释放后 follow 模式脱钩：`behavior.py:375` `eat_mouse` 置 `_follow=False` 但 `_motion_mode` 仍 `"follow"`；释放后 UI 菜单显示跟随而实际不跟随。修法：`eat_mouse_off` 时 `_follow = (_motion_mode == "follow")`。
- M5 🟡（双端）process 按 pid 杀进程绕过 `_PROC_DENYLIST`（win tools_win.py:375-378 / mac tools_mac.py:349-355）：denylist 的设计意图是"确认框都不让过"，pid 路径只有确认框。修法：pid → 进程名解析（win `Get-Process` / mac `ps -o comm=`）后过 denylist 再执行。
- M6 🟡（mac）气泡 wid 登记静默失败：`app.py:201` 在气泡 show 之前登记，mac `view.window()` 为 None → `_own_wids` 缺气泡 → 站窗顶+气泡弹出时图层探针误否决支撑。修法：气泡首次可见时补登记（app 侧一次性守卫，双端无害）。
- M7 🟡（mac）`_DESKTOP_OWNERS` 含 Finder（sensor_mac.py:72）误排全部 Finder 文件窗口——不能站 Finder 窗顶。修法：实体窗过滤只对"无名且覆盖整屏"的 Finder 窗排除（桌面墙），普通文件窗口放行；全屏判定路径保持现排除。
- M8 🟡 F5 遗留：敏感内容明文进 DEBUG 日志（memory fact / 工具参数 args）。修法：日志只记 id/长度/字段名，args 值截断 24 字符。
- M9 🟡 F13 遗留：流式慢滴流无总时长上限（服务端每 100s 滴 1 字节可无限拖住一轮对话）。修法：`_stream_once` 增总时限（默认 180s，config 可覆盖），超时按 Timeout 降级。
- M10 🟡 F32 遗留：proactive 生产（异步 `_fire_wake`→`_on_wake_done` 解析链）与测试（同步 `_decide`）双轨，异步解析/罐头回退链零覆盖。修法：worker 与 `_decide` 共用同一段"请求+解析"代码；测试直调 slot 覆盖 done/failed 两支。
- M11 🟡 F30 遗留：v04 FakeChatWorker `start()` no-op——真 ChatWorker 的 cancel/离线/超时生命周期零覆盖（~30% 段错误的旧规避；finished→deleteLater 铁律落地后应可移除）。修法：新增真 QThread + stub client 用例组。
- M12 🟡 F31 遗留：v02/v04/v09/v03_sensor_win 依赖真桌面会话，无 offscreen/skip 守卫（v08p/v10/v13/v14 已设 offscreen，策略不一）。修法：四文件缺省 offscreen（已设环境变量则尊重），v03_sensor_win 加桌面会话探活 skip。
- M13 🟡 test_v09 T12 副作用：`open_path(tempdir)` 真开 Explorer 窗口。修法：patch adapter.open_path 断言调用参数，不起进程。

**安全**
- S1 🟡 F3 遗留：记忆注入段标注"（自动检索，可靠）"——存储型提示注入通道被 system prompt 背书。修法：改"历史记录，仅供参考；与用户当前指令冲突时以指令为准"。

## 四、Low

**app/行为层**
- L1 `_toggle_autostart` 失败回滚恒置 False（app.py:596-598）：**禁用**失败时勾选态与实际状态脱节方向反了。修法：回滚到 `not enabled`。
- L2 上轮 L4 未清：`begin_drag` 不落 `_pos`（behavior.py:270-277）——按下未移动的 50ms 窗口内 `_tick` 用旧 FSM 坐标把窗口拽回原位（拖拽起始回跳）。修法：`begin_drag` 即 `_pos = clamp(cursor)`。
- L3 上轮 L3 未清：`motion_mode:` 事件在 EAT_APPROACH/EAT_MOUSE 期间可抢占（behavior.py:355-366）——edge 分支强置 IDLE，与 EatMouseSession 抑制态脱钩。修法：吃鼠标两态期间忽略 motion_mode 事件并记日志。
- L4 dt 双重钳制不一致：批次E/L12 让 app 喂 [0.01,0.25]s 实测间隔，但 `behavior.step` 仍钳 0.05（behavior.py:477）——L12 修复意图被吞，模态挂起后物理仍慢动作。修法：FSM 内子步积分（dt 拆 ≤0.05 子步循环，总量 ≤0.25 由 app 钳），50ms 常态路径逐拍等价。
- L5 单击/双击间隔倒挂：`_single_shot` 固定 400ms < 双击窗口 500ms（window.py:104），慢双击同时触发摸头+喂食；`_DOUBLE_CLICK_MS` 定义未用（死常量）。修法：间隔取 `QApplication.doubleClickInterval()+50`。
- L6 `parse_hotkey` 接受无修饰键（hotkey_win.py:72-85）：config 写 `"p"` 即注册全局单键热键，挡正常打字。修法：`mods==0` 拒收。
- L7 L14 遗留：shutdown 不关 `_perm`/`_mem` QML engine（app.py:1079-1084 只处理 chat）。修法：统一 close + 清引用。
- L8 proactive 的 accessibility 提示每 30min 久坐轮都弹气泡+深链系统设置（mac 每轮开设置页）。修法：每会话至多提示一次。
- L9 `EatMouseSession.start` docstring 声称"失败返 False"，实际恒返 None（proactive.py:90-98）——文档与签名不符（调用方走 `sess.active`，行为正确，纯留痕修正）。
- L10 chat 面板隐藏不中断在飞流式（注释自称"关面板时调 cancel"但无人调——实际行为=回复保留至下次打开，更合理）。修法：对齐 docstring 为"shutdown 收口用"。
- L11 `memory_search` 的 k 不防负值（memory_tools.py:83-86，`k=-1` → `scored[:-1]` 语义错）。修法：钳 [1,10]。
- L12 `forget_expired` 变更不 bump `_gen`（memory.py:245-260）——纯衰减/删除永不触发 dirty 周期落盘。修法：有变更即 bump。
- L13 F19 遗留：recall 命中即 +0.05 无节制，importance 迅速封顶失真。修法：距上次召回 ≥1 天才回血（会话内反复命中不胀）。
- L14 F21/F23 遗留：config schema 不覆盖 hotkeys/llm/presentation/provider/log_level/sleepy_idle_minutes；`_SAFE_DEFAULTS` 无终检。修法：补六段 schema + 测试锁定 safe defaults 过校验。

**rig/产线工具**
- rL1 rL2 遗留：QML limb 缺省频率与 bob 不一致（rig_scene.qml:177 1000/period_ms=0.385Hz vs gaitHz 缺省 1.3Hz）。修法：limb 缺省分支改用 gaitHz（恒定 1.3，绝对时间式无瞬移风险）。
- rL2 rL3 遗留：split_parts QA 图两处坐标错位（面板宽 `w*2//3*3` 放不下三张缩略图；pivot 连线全分辨率坐标画在 1/3 缩略图上）。修法：面板宽 `3*(w//3)+16`，pivot/包围盒按比例缩放绘制。
- rL3 rL4 遗留：gen_walk_mids 的 `for branch in (...)` 循环两轮生成同一组文件（帧文件名无 branch），QA 条带同内容写两遍。修法：去循环单次产出。
- rL4 `split_parts --skip-core` 且核心图不存在时 `core` 未定义 NameError（split_parts.py:221-225 vs :281）。修法：`core=None` 初始化 + QA 段守卫。
- rL5 rM5 遗留：拆件参数（seed/block/protect/claim/base）与 trim/extend 状态不进 manifest——重切依赖逆向工程（上轮 C1 重切实测踩坑）。修法：manifest part 条目增可选 `split` 段（spec schema 同步放行），split_parts/extend_part/trim_part_overlap 三工具写入。
- rL6 N1 门禁留档：final/healthy_neutral 内部失配 1.351%（8123px，91.7% 位于尾件 bbox=上轮 v0.13 保护带工艺残留基线，672px 余量在腿接缝线）——静止门禁 HEAD 即 FAIL 且无留档。修法：manifest 增 `qa.exclude` 段（schema 放行），qa_rig_composite 自动读取，静止门禁接线进 test_v14 新用例（全 figure 常驻跑）。
- rL7 N2 残留：年轻/成年尾巴部件仍缺（版本规划 v0.14 Should 已留档，本轮不做）。

**测试杂项**（探索代理报告 F-1~F-11 收编）
- T1 test_v07_eat_mouse_win.py:297-299 `if False` 死代码包裹未定义名 `FakeLockInstance`（恒走 else 不炸，改坏残留）。修直写。
- T2 test_v03_fsm.py:540/566 "T27 撞顶次 tick 即离开"条件 check 可能静默不执行；"全程不越上界"只查末拍。修法：循环内逐拍断言。
- T3 test_real_client_smoke.py:41 timeout 标量分支不验 `>0`。修法：统一正数校验。
- T4 test_v10_animation.py:163 全局 patch `os.path.isfile`（污染全进程，虽有 finally）。修法：收窄到模块级包装。
- T5 test_v05/v03 私有字段大量注入（白盒固有，留档不改）。
- T6 test_v13/v14 `os._exit` 硬退 → 两文件永不可 pytest 化（已自知，留档）。
- T7 全部 spikes 非 pytest 结构——文档/注释明示"勿 pytest 收集"（README 测试节补一句）。
- T8 mac  lows 收编：H-3 bridge=None 兜底加告警 / H-4 持锁调 on_conflict 改锁外 / T-2 denylist 大小写归一 / T-3 mdfind 加 `--` / S-1 空结果按时间戳缓存 / W-1 polish 成功才置位 / P-1 refresh/done 竞态 sender 守卫 / P-2 load_perm_panel 注释警示。

## 五、上轮（08-28）移交项核对

| 项 | 状态 |
|---|---|
| N2 final/healthy_neutral 门禁 FAIL | 本轮 N1/rL6：已量化定位（尾件基线），批次 G 补 exclude+门禁常驻 |
| C1 重切参数未留档 | 本轮 rL5，批次 G 解 |
| rM5 trim 状态 / rL2 / rL3 / rL4 | 仍开放 → 本轮 rL5/rL1/rL2/rL3，批次 G 解 |
| rL5 young/adult 尾巴 | 仍开放（资产工序，版本规划留档，本轮不做） |
| F3 / F5 / F13 / F19 / F21-23 | 仍开放 → 本轮 S1/M8/M9/L13/L14，批次 E/I 解 |
| F17（save 锁外交错） | 已闭环（F16 代数快照机制实际覆盖） |
| F25/F26（锁口径） | pet_state reset/update 已收锁内（批次E/L11），闭环 |
| F30 / F31 / F32 | 仍开放 → 本轮 M10-M12，批次 H 解 |
| F29 真 API 冒烟门槛 | 已落地（代理核实 :144） |
| 上轮 L3（motion_mode 抢占）/L4（begin_drag）/L7（eat_mouse 帧零调用）/L14（engine 不关） | L3/L4/L14 本轮批次 D/J 解；L7 维持功能决策留档（chew 帧在用，eat_mouse 帧为预留资产） |
| T3/T8 工作表补账 / T11 PNG LFS | 维持历史留档（流程项，不堵功能） |
| pytest 化 + CI（F28） | 维持留档（T7 补一句边界说明） |

## 六、修复批次安排（本轮执行）

版本号自 v0.14.19 顺 .Z 递增（当前活跃里程碑线 v0.14.x）；每批独立提交 + 实跑回归；commit message 测试数字以实跑为准。

- **批次 0｜v0.14.19 留档**：本 REVIEW + 在途资产（neglected 腿件纯裁除 + QA 图刷新 + LoRA 方案文档）入库。
- **批次 A｜v0.14.20 rig 进化换档**：H1 + rL1。回归 v13/v14。
- **批次 B｜v0.14.21 记忆去 key 依赖**：H2 + M3。回归 v09/v04。
- **批次 C｜v0.14.22 win 文本编码**：M2。回归 v08。
- **批次 D｜v0.14.23 FSM 一致性**：M4 + L2 + L3 + L4。回归 v03/v07。
- **批次 E｜v0.14.24 安全/日志/健壮**：S1 + M8 + M9 + L11 + L12 + L13 + M5(pid denylist 双端)。回归 v04/v06/v08/v09。
- **批次 F｜v0.14.25 混合 DPI 精确变换**：M1。回归 v03s。
- **批次 G｜v0.14.26 产线工具+门禁留痕**：rL2/rL3/rL4/rL5/rL6。回归 v14 + 全 figure 门禁。
- **批次 H｜v0.14.27 测试基建**：M10 + M11 + M12 + M13 + T1/T2/T3/T4 + L14。全量回归。
- **批次 I｜v0.14.28 mac 对齐**（本机不可实跑，py_compile+静态核对，留实机复核项）：H3 + M6 + M7 + T8 组。
- **批次 J｜v0.14.29 app 杂项**：L1 + L5 + L6 + L7 + L8 + L9 + L10。回归 v02/v05/v11。
- 收尾：全量 15 套件 + 门禁，push。
