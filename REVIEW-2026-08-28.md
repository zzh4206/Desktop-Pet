# 桌宠仓库全量 Review 记录（2026-08-28）

> 审查起点 HEAD `6508cda`（v0.14.4）；审查期间在途推进至 `941e29c`（v0.14.7）
> 范围：全量源码通读（app.py + pet/ 全部 26 文件 + rig/ + ui/ + tools/ 5 + spikes/ 全部测试与 _qa 探针 + assets manifest + 四份文档）+ Critical/High 逐条第一手复核（竞态读源码、资产做像素/哈希实证、门禁做 grep 证实）
> 方式：只读审查先行（三路并行探索 + 交叉验证），随后分批清偿；每批实跑回归后独立提交
> 上游：`REVIEW-2026-08-27.md`（其未清项 L3/L5/L7/L11/T3-T11 在本轮核对，见 §五）

---

## 〇、审查期间现场发现（在途提交完整性）

**N1 🟡 v0.14.6（`a9f7a1c`）提交不完整——提交信息声称的路由不在提交里**
- (a) 提交信息含"路由：paperdoll 行走载体优先级 侧身→正面 neutral→None"，但 **app.py 不在提交文件列表**：`_walk_refresh` 的 `side_walk_static` 路由 hunk 至今躺在工作区未提交。HEAD 上 `side_walk_static` 全仓零调用——侧身部件步态实际**不生效**，paperdoll 行走仍走正面 neutral（连带 C1 的四腿重影在 HEAD 上即可触发，见下）。
- (b) `assets/rig/final/manifest.json` 引用 `figs\healthy_side.png`，该文件**未跟踪**——克隆仓库后 healthy_side figure 源图缺失，已验收的侧身资产不可复现。
- 处置：属在途工作，本轮不代提交（见 §三批次 0 说明）；**实跑回归前必须先补齐这两项**，否则跑出来的"通过"测的是未入库代码。

---

## 一、Critical

**C1 🔴 final/neglected 双腿部件是同一双腿连通域拆了两遍——正面 paperdoll 行走四腿重影**
- `assets/rig/final/manifest.json` 的 `leg_l_neglected_neutral` / `leg_r_neglected_neutral`；根因 `tools/split_parts.py:141-159`（每次运行从原图全量 `im.crop()`，无部件间像素归属机制）。
- 像素实证：leg_l 的 27114 个 α>0 像素 **100% 落在 leg_r 的 27160 像素内，重叠区 RGB 逐像素一致**（27114/27114），核心图对这些像素覆盖为 0——两件就是同一个 α 连通域各拆一遍，仅 bbox 边缘 1px 清零造成 246px 差异；文件大小佐证（healthy 腿 ~23KB vs neglected 腿 ~52KB，后者装了双腿）。pivot 却不同（474,1273 vs 553,1273）→ QML 中两件反相绕不同 pivot 各转 ±7° = 四条腿。
- 门禁失明：`qa_rig_composite.py` 只验"静止叠加≈原图"，两件静止叠放恰好等于原图 → PASS。摆动失配只有 `render_swing.py` 人工目检。
- 影响路径：paperdoll 档 + final + neglected + 行走回退到正面 neutral（v0.14.6 路由落地后该组合=无侧身资产时的回退分支；N1 未补齐时=全部行走）。**v0.14.5"正面踏步观感不佳"的实机结论很可能部分源于此资产缺陷，归因错记在方案上。**附带：young/neglected 有 723px（3.1%）鞋尖重叠。
- 修法：split_parts 增部件间归属（左右腿按 x 分界先到先得扣除）→ 重切 final/neglected 腿件（纯像素操作零画风风险）→ qa_rig_composite 增"部件两两 α 交叠 ≤ 阈值"门禁并入 test_v14（约 20 行检查永久挡住此类事故）。

## 二、High

**H1 🔴 吃鼠标看门狗/启动竞态——可致"永久吞鼠标"突破 15s 铁律**
- `pet/mouse_lock_win.py:174-248`：`start()` 先起看门狗再装钩子，但 `_active=True` 要等 `_ready.wait(1.0)` 返回后才置位（:206-210）。若钩子安装 >0.5s（系统繁忙/安全软件扫描），看门狗轮询到 `_active=False` → `:243-244` 直接 return 退出；随后钩子装好、start 置 `_active=True`——钩子活着、deadline 已过、看门狗已死，**无任何机制再自动释放**。`_release` 用 `_active` 做幂等标志，表达不了"取消一个尚未完成的启动"（mac 版 `_create_taps` 内有主动复查，win 缺对称防御）。
- 附带 M7：`_hook_loop` 退出清理（:288-291）读写的 `self._hook` 与新会话共享——force_spit 后旧线程尚未退出时再次 start，旧线程退出清理会拆掉**新会话的钩子**（抑制假活直到超时）。
- 修法：会话化取消语义（start 前置 `_starting`，watchdog 检查 `_active or _starting`，_release 可取消 starting：hook_loop 装钩后查取消标志自拆）；钩子句柄由线程局部变量持有，退出清理不碰共享 `self._hook`。

**H2 🔴 全屏/演示模式：气泡仍会弹出 + 吃鼠标门禁不含全屏**
- `app.py:801-810` `_check_fullscreen` 只 `self.window.hide()`，**不藏 BubbleWidget**（独立 `Qt.Tool|StaysOnTopHint` 窗）；`proactive.py` 全文零 fullscreen 引用——久坐提醒/链式唤醒无条件 `bubble.show()`，气泡可盖全屏 PPT/视频。
- eat_mouse 四门禁（idle/DND/活跃内容/accessibility，`proactive.py:250-300`）**无全屏项**：放映中 5 分钟无输入（idle 默认 5min），FSM 照走 EAT_APPROACH→EAT_MOUSE 启动 WH_MOUSE_LL 抑制——吞演示者鼠标 10s。`sensor_win.py:333` 注释宣称"演示软件前台全屏则完全隐藏 + 禁吃鼠标"，实现只做了前半句。
- 修法：进全屏 `bubble.hide()`；eat_mouse 门禁加第五项全屏拒绝。

**H3 🟡 rig 步频/倾斜映射是死代码——FSM 行走期间 velocity 恒 (0,0)**
- `app.py:855-860`（`hz = 0.9+|vx|/400`、`tilt = vx/140`）× `pet/behavior.py`：全部 `_vx` 赋值点（120/273/297/344/364/374/458/501/526/538/626/744-747/793）均为初始化清零、拖拽、抛掷或反弹——`_step_walk` 的 stride 计算从不写 `_vx`。
- 后果：walkHz 恒 0.9Hz（注释宣称"walk 120px/s≈1.2Hz、follow 600≈2Hz"不可达）、bodyTilt 行走期间恒 0；follow 600px/s 平移时腿仍 0.9Hz 慢踏=视觉滑步。测试 T3a 只验证属性写入，未验证从 FSM 推导——盲区。
- 前置依赖：必须**先修 rM1**（QML 相位累加器），否则速度一接上就激活相位瞬移。

**H4 🟡 set_facing 在 paperdoll 行走期间把画面翻回 mood 立绘——隐藏的第三类帧回退**
- `pet/rig/presenter.py:295-301` `set_facing` 无 `_walk_showing` 守卫（对比 `on_state_change:344-353` 有）× `pet/window.py:160-161` 基类 `set_facing` 调 `self.set_sprite(self._sprite)` 虚分发回 RigWindow → `_show_now(mood图)` → `activeFigure` 变为无 limb 的 figure → 下一拍 `part_walk_active()=False` → 回退 GPT 帧行走 + 夹一拍 mood 图闪现。
- 触发：任意一次与上段位移方向相反的行走开始（`app.py:877-879` 每 tick 末尾按位移方向调 set_facing）。违背 v0.14.4"帧行走只剩两类回退"承诺。测试完全没碰行走中 set_facing——盲区。
- 修法：一行守卫（`_walk_showing or _frames` 时只落 `_facing` 标志）+ 补测试。

**H5 🔴 剪贴板读取无确认、无截断，整段回灌 LLM**
- `pet/tools_win.py:139-144` / `pet/tools_mac.py:188-194`：clipboard get/set 标 `dangerous=False` 静默执行；`pet/llm.py:273-276` 把 `res.message`（剪贴板全文）作为工具结果回灌 DS——用户剪贴板（常含密码/token）直送第三方 API，无长度上限（F12 同根）。
- 对比：process/sleep 都有确认框，隐私面最大的工具反而没有。写剪贴板（覆盖用户数据）同样无确认。
- 修法：clipboard 读写标 dangerous 走确认框；llm.py 工具结果 message 截断（data 保留全量）。

**H6 🔴 file_search 的 PowerShell 拼接存在引号逃逸——用户名含 `'` 时任意命令执行**
- `pet/tools_win.py:180-192`：`root = os.path.expanduser("~")` 未过滤直接拼进 `Get-ChildItem -Path '{root}' ... -Filter '{pattern}'`。pattern 禁了 `'`（`_PATTERN_BAD:168`）但 root（Windows 用户目录）没有——用户名 `O'Brien` 类路径使引号提前闭合，pattern（LLM 可控）脱离引号上下文成为独立语句。argv 列表传参本无注入面，是单引号拼接自己打开的面。
- 修法：root/scope 统一转义（`'`→`''`）或加入字符黑名单。

## 三、Medium

**平台/物理**
- M1 防抖退化为约每秒落盘一次：`_on_state_changed_persist`（app.py:769-772）只 start(500) 不 restart；衰减默认 2-6.5/h 让 1s tick 每秒判"实际变化"→ json dump+fsync+copy2 bak+replace+fsync 以 ~1s 频率持续 ~27h。
- M2 Windows 第二实例"唤醒已有实例"失效：`platform.py:512-534` 锁字节 [0,1) 与 pid 读区重叠，LockFile 语义下他进程 ReadFile 必 ERROR_LOCK_VIOLATION → `_read_pid` 吞异常返 None → 唤醒永不生效（mac flock advisory 无此问题）。
- M3 混合缩放多屏 `_hwnd_to_rect` 系统性错位：`sensor_win.py:132-160` 假设"物理原点=逻辑原点×dpr"只在全屏同 dpr 成立；归属判定失败时把物理像素当逻辑坐标返回（偏 1.5 倍）。FSM 攀爬/站顶全部建立在 windows 列表上。正确变换 `(phys-phys_origin)/dpr+logical_origin`。
- M4 mac 检测不到最小化窗口：`sensor_mac.py:335-347,393-414` `kCGWindowListOptionIncludingWindow` 不带 OnScreenOnly → 最小化窗仍"存活"+旧 bounds → 宠物骑幽灵窗（win 端 IsIconic 是对的，双端分叉）。
- M5 win 默认配置下"活跃内容"门禁完全失效：win 默认 `video_apps: []` → `is_active_content` 首行返 False 恒通过；mac 空名单却回退内置 9 项。同一默认 config 双端语义分叉（config.py:60 + platform.py:656-667 vs 358-364）。
- M6 每 2s 主线程全量窗口枚举且每窗重复调 `QGuiApplication.screens()`（app.py:780 + sensor_win.py:144-156,168-200）——30 窗 × N 屏 Python 循环脉冲式卡顿。

**LLM/记忆**
- F9 流式读超时误判"离线"且粘死：`llm.py:209-211` 把 urllib3 ReadTimeout 包装的 ConnectionError 转 OfflineError；`chat_bridge.py:367-377` 置 `_offline=True`，`reset_offline` 仅 `_show_chat` 调——一次网络抖动后聊天永久走离线预设，必须关开面板。
- F10 `cancel()` 对未退线程无条件 `deleteLater`：`chat_bridge.py:315-318` `wait(2000)` 不查返回值——慢网络下销毁运行中 QThread（同文件 :258-261 注释明确规避的原生崩溃；proactive.shutdown 是正确的保引用模式）。
- F11 无 id 流式 tool_call 被跳过 → 续轮消息序列非法 → HTTP 400 整轮失败：`llm.py:262-264`。
- F12 无 token 上限/预算：`_payload` 不设 max_tokens（llm.py:132-142）；工具结果 message 不截断（clipboard get 可一次打爆上下文）；usage 只内存累计不展示无熔断。
- F15 失败/离线轮 user turn 永不进 `_history`：user 只经 `_on_done` 的 head+append（llm.py:419-421）入史；失败轮 DS 历史"无问之答"，且摘要按 user 数 `pairs` 删 UI 行累计错位。v04 测试 `hist_before_fail+1` 断言把这个 bug 固化了。
- F16 记忆只在 shutdown/记忆页操作时落盘：30s 周期存档只存 pet_state；ChatWorker 线程 memorize 的写入崩溃即全丢（违背 v0.9"跨会话不丢"Must）。

**rig/资产**
- rM1 QML 步态相位用"绝对时间×瞬时频率"：`rig_scene.qml:66-68,163-165` `sin(2π·t·hz/1000)`——hz 一变相位瞬移（10min 后 0.05Hz 变化=30 个整周期跳变）。当前因 H3 恒 0.9Hz 不发作；**修 H3 前必须先修**（改相位累加器）。
- rM2 split_parts 路径归一化是空操作：`split_parts.py:196,204` 源码 `"\\\\"` （运行时串两个反斜杠）替换 `relpath` 产出的单反斜杠永不命中 → manifest 混入 `figs\...`/`parts\...`（本轮在 final/manifest.json 实证，含 v0.14.6 新条目）。Windows 靠 normpath 兜住；非 Windows 端静默弃件。
- rM3 limb 缺 `sway` 时"冻结腿+压制帧回退"无校验：`spec.py:63-74` 不要求 limb 带 sway；缺省摆角 0 但 `part_walk_active()=True` → 不播帧也不摆腿=平移滑行。schema 也不校验 px_rect 合法性。
- rM5 拆件跨工具不幂等：split 永远从原图全量 crop；trim_part_overlap 覆写 parts/*.png 且 manifest 不记录"已 trim"——对某腿调参重切会复活被核心覆盖的冗余像素（"摆尾连带裙角"类回归）；`--qa-swing` 条带用的是 trim 前部件。
- rM6 硬切策略按首帧文件名子串枚举：`presenter.py:246-256` 名单外新动作默认走淡化=回归"双臂残影"（v0.13.5/v0.14.3 两轮实机事故的同族）；`_roll` 无下划线子串可能误命中。

**测试**
- F29 真"客户端冒烟"名不副实：`test_real_client_smoke.py:20-39` monkeypatch `requests.post` 抛 ConnectionError——SSE 聚合/`[DONE]` 解析/续轮回灌/400 降级零真实覆盖（上两轮"mock 掩盖真客户端问题"教训未闭合）。
- F30 真线程生命周期被测试禁用：v04 FakeChatWorker `start()` no-op——cancel 中断流、wait 时序、deleteLater 时序（正是 F10 崩溃窗口）无自动化覆盖。

## 四、Low

**app 层**：L1 `_hk_hint` NameError 窗口+except 类型不匹配（app.py:536-562，=上轮 L5）；L2 hotkey stop 线程收口竞态+持锁 wait 2s（hotkey_win.py:169-184）；L3 `motion_mode:` 事件可抢占 EAT 态与 EatMouseSession 脱钩（behavior.py:355-366）；L4 begin_drag 不设 `_pos`→拖拽起始回跳（behavior.py:270-277）；L5 窗口 clamp 退化（比屏还宽时钉 max_x）+每 tick 取 screens()（window.py:219-242）；L6 帧动画每帧 isfile+getmtime（window.py:107-112）；L9 mac stderr 过滤管道边界死锁（过滤线程退出后 64KB 缓冲写满即永久阻塞，app.py:34-57）；L10 `_check_fullscreen`/`_refresh_sensors` 无兜底 except（app.py:780-789）；L11 pet_state reset 无锁/update 锁外读观察者（pet_state.py:177,393-400）；L12 dt 硬编码 0.05 模态期间物理变慢（app.py:841）；L13 mouse_lock start 主线程 wait 1s（并入 H1 批）；L14 shutdown 不关 _perm/_mem engine（app.py:980-1043）。
**mac**：L7 Carbon 回调内直接 Qt GUI 操作（hotkey_mac.py:293-320）；L8 tap 回调无条件 return None 不查 `_active`（mouse_lock_mac.py:234-237，win 版有检查）。
**安全顺带**：F3 记忆=存储型提示注入通道且 system prompt 标注"可靠"（memory_tools.py:37,113）；F4 `open_app url` 仅校验 https? 前缀无确认（tools_win.py:66-74）；F5 记忆 fact/工具参数明文进 DEBUG 日志且无清理（memory.py:150 + llm.py:270-271 + logging_setup.py:49-55）；F6 win process 无关键进程硬拒名单（mac 有 _PROC_DENYLIST）；F7 缺 base_url 的 provider 回落 DeepSeek 端点=凭据误送（llm.py:434-441）。
**LLM 杂项**：F13 慢滴流永超时（chunk 间隔 120s，服务端每 100s 滴 1 字节无限拖住）；F14 工具轮触顶/末轮空文本时缺终 assistant turn（llm.py:327-338，UI 可能显示中间轮文本）；F17 memory.save 锁内快照锁外写（并发交错）；F18 load 不校验 id→mem_bridge `m["id"]` KeyError 炸 QML 槽；F19 每条消息 recall 命中即 +0.05 回血，importance 迅速封顶失真。
**配置**：F20 `llm.last_provider` 死键（=上轮 T10）；F21 `_SAFE_DEFAULTS` 不再过 schema 终检；F22 `_defaults()` 硬依赖仓库内 example 文件路径（打包即崩）；F23 schema 不覆盖 hotkeys/llm/presentation。
**rig 杂项**：rL1 `partsModelInit` 上下文属性死代码（presenter.py:166）；rL2 QML 频率缺省不一致（bob 1.3Hz vs limb 0.385Hz）；rL3 split_parts QA 图两处坐标错位（面板宽 `w*2//3*3`、pivot 连线全分辨率画 1/3 缩略图）；rL4 gen_walk_mids branch 循环两轮生成同一组文件；rL5 young/adult 尾巴部件缺失（版本规划已留档）；rL6 bubble resize 后又 adjustSize+mac 字体硬编码；rL7 asset_provider get_frames 逐帧 isfile 无缓存（与 frames_for 不对称）；rL8 gen_fill patch 1.57M 次纯 Python 像素循环；rL9 `{stage}_{branch}_{mood}` 命名契约分散 5 处。
**文档/流程**：F31 GUI 测试依赖真实显示会话（headless 必挂）+v09 混入拖放测试职责漂移；F32 proactive 生产（异步 _fire_wake）与测试（同步 _decide）两套代码；F33 设计思路 §七热键行陈旧（Cmd+Option+Shift+P 应为 Cmd+Option+T）；F34 README 权限声明强于实现（"AI 工具调用会先确认"实际仅 process/sleep）；F35 README 快捷键只列 Windows。

## 五、上轮（08-27）未清项核对

| # | 状态 |
|---|---|
| L3 set_sprite 加载失败残留 | 仍开放（本轮 L 类，批次 G 处理） |
| L5 `_hk_hint` | 仍开放（=本轮 L1） |
| L7 eat_mouse 12 张帧零调用 | 仍开放（功能决策，批次 G 留档或接线） |
| L11 mouse_lock 1s 就绪等待 | 仍开放（并入批次 B） |
| T3/T4/T7/T8/T9 文档类 | 仍开放（批次 G） |
| T6 postprocess_assets 过期 | 仍开放（尺寸 64/96/128 vs 实际 192/256/320，批次 G 标注废弃） |
| T10 config 死键 | 仍开放（=本轮 F20，批次 G） |
| T11 PNG 无 LFS | 仍开放（历史定型，批次 G 决策留档） |

## 六、修复批次安排

每批独立提交+实跑回归；版本号自 v0.14.8 顺延（避开已入库的 v0.14.5-7）。**批次 0 前置：等待在途方补齐 N1 的 app.py 路由 hunk 与 figs/healthy_side.png（本轮不代提交）**；A-E 不触碰该 hunk 所在区域，可先行。

- **批次 A｜安全红线**：H5（clipboard dangerous+确认、结果截断）、H6（PS 转义）、F4（url 确认）、F6（win _PROC_DENYLIST）。回归 v08 系。
- **批次 B｜吃鼠标竞态根治**：H1+M7（会话取消语义+钩子句柄局部化）、L13、L8。回归 v07/v07win。
- **批次 C｜全屏门禁**：H2（bubble 联动+第五门禁）、L10。回归 v06/v07。
- **批次 D｜LLM/记忆健壮性**：F7/F9/F10/F11/F12/F14/F15/F16/F18。回归 v04/v06/v09。
- **批次 E｜平台对齐+成本**：M1-M6、L1/L2/L5/L6/L11/L12。回归 v02/v03/v03s/v05/v11。
- **批次 F｜rig 接线+资产**（v0.14.6 已入库即可做，顺序敏感）：rM1 相位累加器 → H3 velocity 接线 → H4 set_facing 守卫+测试 → C1 重切+归属+α 交叠门禁 → rM2/rM3/rM5/rM6/rL1-L4。回归 v13/v14+门禁全量。
- **批次 G｜测试基建+文档收尾**：F29 真 API 冒烟（环境门槛）、F30、F31、F33-F35、F20、T6/T7/T9、上轮 L3/L7/T3/T8/T11。全量回归。

## 七、流程结论（新增）

1. **提交完整性自查**：commit message 里声称的每个要点都应对着提交里的文件（N1 的路由 hunk）；manifest 引用的源图必须同批入库（克隆可复现是门禁底线）。
2. 静止门禁挡不住动态缺陷：部件资产门禁必须含"部件两两 α 交叠"检查（C1 从生产到入库全程无报警的根因）。
3. "宣称的行为"须有端到端断言：H3（速度→步频注释 vs 死代码）、H4（"只剩两类回退" vs 第三类隐藏路径）都是文档/注释先行、集成路径无验证的产物。
