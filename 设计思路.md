# 桌宠 设计思路

> 本文件是**共享核心设计**的 single source of truth——平台中立，不含 Mac/Windows 实现细节。
> 接口契约一旦定下，改动须在此处显式记录并同步对方。
> 平台实现细节、协作与分工见 `平台适配与分工.md`；版本里程碑见 `版本规划.md`；每版任务认领见 `工作表.md`。
> 状态：设计阶段（v0.0.x）。修订：v0.0.9 文档重组——Mac 特定实现抽出至 `平台适配与分工.md`，本文件瘦身为纯共享主体。
> 修订：v0.1.1 §七/§十三 双端对齐（win 强制吐出热键定 `Ctrl+Alt+T`；Git 本地路径移出共享文档）。
> 修订：v0.10.23 §2.1 模块树对齐实际文件（原列 renderer.py/permissions_*.py 不存在）；§2.2 删 `ActionType.FOLLOW_CURSOR` 死枚举（全仓无构造点，跟随是 FSM motion_mode）。
> 修订：v0.13.0 §一/附 决策清单增补**渲染路线 C（分层绑骨展示层）**——与"放弃 Live2D"的既有结论不矛盾（见 §一路线 C 说明）；性能红线"动画帧率限 30fps"按呈现后端修订；§2.2 BehaviorFSM 增公开只读 `velocity`、WindowBase 增 `is_playing()`/`set_motion_params()` 缺省。
> 修订：v0.14.0 §一 路线 C 增补**第三档 `presentation: paperdoll`（部件驱动动作）**——neutral 立绘确定性拆出 limb 肢体（双腿），行走由场景 limb 驱动器程序化合成正面原地步态（gaitK 包络 × 步频正弦，频率随 FSM 速度），figure 无 limb 部件自动回退帧动画；拆件工艺新增多部件累计挖除、非对称分墙（鞋尖连通区像素归属各自部件，杜绝核心图静态残条）与静止合成门禁 `tools/qa_rig_composite.py`（内部失配 ≤0.5%）；WindowBase 增 `part_walk_active()` 缺省、manifest parts 增可选 `kind` 字段（sway|limb，缺省 sway 向后兼容）。

---

## 一、定位与差异化

一只**会长大、会在桌面动来动去、立绘靠 AI 生成**的桌宠，双平台（macOS / Windows）并行开发。常驻轻量进程，本地零重计算。

对比参考仓库 `sjmsltx/deepseek-desktop-pet`（会聊天的静态桌宠：固定约稿立绘 + Live2D + LLM 工具）。本作差异化集中在参考仓库**都没有**的三件套：

| # | 我们的 | 参考仓库 | 说明 |
|---|---|---|---|
| ① | 桌面物理行为（Shimeji 式） | 无 | 行走/掉落/抛掷/跟随鼠标/随机游走 |
| ② | 年龄驱动的成长养成 | 无 | 时间长大 + 养护决定长成什么样 |
| ③ | 可替换立绘演化 | 固定约稿，不可演化 | emoji→AI→约稿，接口统一 |

**渲染路线**：2D sprite + 行为物理，**放弃 Live2D**（需手工绑骨，无法 AI 生成/随阶段演化，与②③冲突）。3D + 太阳方位/光影为未来扩展（§九）。

**路线 C（v0.13 增补）：分层绑骨展示层（paper-doll-lite）**——在路线 A 之上叠加第二套呈现后端：以**已验收立绘的确定性切件**为部件（部件与派生核心图都源自原图像素，动画过程零新生成 ⇒ 同阶段画风构造性一致），运行时参数驱动（交叉淡化/呼吸/速度倾斜/部件弹簧）。与被否决的 Live2D 的分界：不引入 Cubism 那类需人工绑骨的外部美术管线与闭源运行时；"骨骼"是我们自定义、可脚本批量生成的 JSON 参数格式（`assets/rig/{stage}/manifest.json`），拆件由 `tools/split_parts.py` 半自动完成且三阶段共用同一动作规范，随阶段演化不受阻。配置键 `presentation: frames|rig|paperdoll`（默认 frames；v0.14 paperdoll=rig 之上叠加部件驱动动作，详见修订记录）任一资产/环境缺件自动回退——展示层永不阻断启动。

**UI 分层**：桌宠本体（透明浮窗/立绘/帧动画/物理）走 Qt 原生渲染（`Renderer2D`）；**应用型界面**（聊天面板/设置页/记忆管理/权限向导）走 **QML（Qt Quick）**，双平台共享、无平台差异。不引入 QWebEngine/Chromium，守住性能红线。

### 性能红线（量化，开发期用 `psutil` 监控，双端各自验证）
- 空闲平均 CPU < 1%
- 动画运行峰值 CPU < 5%
- 常驻内存 < 200 MB
- 网络请求间隔 > 5 s（主动对话除外）
- 达成手段：timer/事件驱动而非轮询；窗口枚举缓存；~~动画帧率限 30fps~~（v0.13 修订：该条款针对 frames 后端的 QTimer 换图开销；rig 后端走 Qt Quick 场景图 GPU 合成，帧步进为 33ms 属性插值而非整图换载，CPU 预算同红线、上限放宽至 vsync——**违背既有条文的原因：交叉淡化与部件弹簧在本就该 GPU 侧做，CPU 换图反成热点**）。

### 目标环境（双端对齐）
- Python 3.10+；共享依赖 `PySide6` / `keyring` / `requests`（`requirements.txt` 为准；性能监控临时用 `psutil`，非运行依赖）。
- 平台额外依赖见 `平台适配与分工.md`（mac: pyobjc；win: pywin32/ctypes）。

---

## 二、模块划分与接口契约

### 2.1 模块总览
```
pet/
├── __init__.py            # __version__（版本单一源，app.APP_VERSION 派生）
├── window.py              # 透明浮窗薄基类（纯 Qt；平台 polish 在 window_mac/_win）
├── window_mac.py / window_win.py  # 平台浮窗（mac NSWindow polish / win 直继承）
├── bubble.py              # BubbleWidget 气泡（Qt 原生，与本体同层）
├── ui/                    # QML 应用型界面（聊天/设置/记忆/权限），双平台共享
│   ├── main.qml / mem.qml / perm.qml
│   ├── chat_bridge.py / mem_bridge.py
│   └── perm_bridge.py / perm_bridge_mac.py
├── asset_provider.py      # AssetProvider + emoji/AI/约稿 三实现
├── pet_state.py           # 养成 State（单一真相源）+ observer + 持久化
├── behavior.py            # ActionType + 行为 FSM + 传感器接口 + 调制表
├── llm.py                 # OpenAI 兼容客户端 + function calling + usage + 降级
├── tools_schema.py        # ToolRegistry + ToolContext + 危险操作拦截
├── memory.py / memory_tools.py   # 长期记忆 + DS 记忆工具
├── proactive.py           # 主动关怀 + 链式唤醒 + 吃鼠标 session
├── config.py              # config 读写、角色定义、schema 校验
├── logging_setup.py       # 日志（路径与轮转见平台文档）
├── tray.py                # 系统托盘（QSystemTrayIcon 跨平台）
├── platform.py            # 按 sys.platform 选平台 shim 工厂（app.py 入口只碰它）
├── sensor_mac.py / sensor_win.py            # 平台传感器（鼠标/空闲/工作区/窗口枚举）
├── tools_mac.py / tools_win.py              # 平台工具实现
├── hotkey_mac.py / hotkey_win.py            # 平台全局热键
└── mouse_lock_mac.py / mouse_lock_win.py    # 平台鼠标抑制
```
> 渲染层未单独成 `renderer.py`——v0.1 起即由 `window.py` 直接 blit（sprite + 帧动画），`Renderer` 接口位保留在 §2.2 供未来 3D 扩展。权限页未拆 `permissions_*.py`——检测内联在 `ui/perm_bridge*.py`（win 运行时自检 / mac 系统特权探测）。

### 2.2 接口契约（核心，改此 = 留痕）

#### AssetProvider —— 立绘来源（差异化③）
```python
class Stage(Enum): YOUNG, ADULT, FINAL
class Branch(Enum): HEALTHY, NEGLECTED
class Mood(Enum): HAPPY, NEUTRAL, SAD, SLEEPY, HUNGRY, ...

@dataclass
class SpriteRef:
    path: str
    width: int             # 逻辑像素
    height: int
    anchor: str = "bottom_center"

class AssetProvider(Protocol):
    def get_static(self, state: PetState, skin: str = "default") -> SpriteRef: ...
    def get_frames(self, state: PetState, action: ActionType, skin: str = "default") -> list[SpriteRef]: ...
```
- `get_static`：静态表情/阶段图（AI 立绘主战场）；`get_frames`：动作帧序列（伸懒腰/打滚/咀嚼）。
- **v0.1 即冻结双方法**，避免 v0.10 切 AI provider 时动画崩。`EmojiProvider` 两方法都实现；`AIArtProvider` 双方法已全量实装（v0.10.15 起帧动画走 AI 帧，仅动作缺帧时降级 emoji）。
- `skin`：可选皮肤 variant，默认 `"default"`。
- **默认显示尺寸**：`YOUNG` 192×192 / `ADULT` 256×256 / `FINAL` 320×320（HiDPI 自动处理；v0.10.12 用户多轮反馈小尺寸发糊后定档，1:1 显示无损）。所有物理计算以 `bottom_center` 为锚点。

#### Renderer —— 渲染层（为 3D 留缝）
```python
class Renderer(Protocol):
    def draw(self, state: PetState, screen_pos: tuple,
             animation_state: str, facing: int, dragging: bool, dt: float) -> None: ...
```
- `Renderer2D`（现在）：blit sprite + 帧动画。`Renderer3D`（未来）：增 `sun_direction`。
- 应用型界面不走 Renderer（QML 直接订阅 `PetStateStore.on_change` 与 FSM 事件）。

#### PetState —— 养成单一真相源 + 持久化
```python
@dataclass
class PetState:
    mood: float          # 0-100
    fullness: float
    cleanliness: float
    age: float           # 宠物天
    stage: Stage
    branch: Branch

class PetStateStore:
    def get(self) -> PetState: ...
    def update(self, **deltas) -> None: ...          # 触发 observer
    def on_change(self, cb) -> None: ...
    def save(self, path) -> None: ...                # 原子写：.tmp → os.replace
    @classmethod
    def load(cls, path) -> "PetStateStore": ...     # version 字段 → migrate 链
```
- 原子写入 + 顶层 `"version": N` + `migrate_v{N}_to_v{N+1}` 链 + `.bak` 备份（load 失败从 .bak 恢复）。
- `config.py` 加载对数值范围做 `jsonschema` 校验，非法值回退默认值 + 日志告警。

#### ActionType + Action —— 行为输出（强类型）
```python
class ActionType(Enum):
    MOVE_TO = "move_to"; FALL = "fall"; ANIMATE = "animate"
    SPEAK = "speak"; EAT_MOUSE = "eat_mouse"
# WANDER 是 FSM 行为模式（反复发 MOVE_TO 随机目标），非独立 ActionType。
# FOLLOW_CURSOR 曾列于此，v0.10.23 删（死枚举：跟随是 FSM motion_mode，无构造点）

@dataclass
class Action:
    type: ActionType
    params: dict
```

#### BehaviorFSM —— 桌面物理行为
```python
@dataclass
class Sensors:
    mouse_pos: tuple
    work_area: dict         # 可用工作区（已排除任务栏/Dock/菜单栏），多屏取合集
    windows: list           # 其他窗口框（进阶，事件驱动+缓存，不每帧枚举）
    idle_time: float        # 系统空闲秒（吃鼠标 gate 用）

class BehaviorFSM:
    def step(self, state: PetState, sensors: Sensors, dt: float) -> Action: ...
    def handle_event(self, event: str) -> None: ...   # 如 ProactiveScheduler 触发 EAT_MOUSE
```
- `work_area` / `idle_time` / `windows` 数据源由平台 sensor 填充（见平台文档）；FSM 只消费 `Sensors`，不碰平台 API。

#### ToolContext + ToolRegistry —— LLM 工具
```python
@dataclass
class ToolContext:
    pet_state: PetState
    user_name: str
    config: dict
    window_info: Optional[dict]

class ToolHandler(Protocol):
    def execute(self, args: dict, ctx: ToolContext) -> ToolResult: ...

class ToolRegistry:
    def schemas(self) -> list[ToolSchema]: ...
    def dispatch(self, name: str, args: dict, ctx: ToolContext) -> ToolResult: ...
```
- `dispatch` 处拦截危险操作 + 确认对话框（平台原生）+ 参数黑名单校验（见 §五）。

#### 记忆 / 主动消息
```python
class MemoryStore:
    def memorize(self, fact: str, importance: float) -> str: ...
    def recall(self, query: str, k: int) -> list: ...
    def forget(self, mem_id: str) -> None: ...
    def summarize_session(self) -> None: ...

class ProactiveScheduler:
    def schedule_wake(self, delay_min: float, reason_ctx: dict) -> None: ...
    def follow_up(self, event: str, when: float) -> None: ...
    def eat_mouse(self, duration_s: float) -> "EatMouseSession": ...

class EatMouseSession:
    def start(self, duration_s: float) -> None: ...     # 仅抑制鼠标（实现见平台文档）
    def force_spit(self) -> None: ...
    def on_dnd_active(self) -> bool: ...
```

### 2.3 交互入口（v0.1 冻结，v0.2 实现点击，v0.3 扩展拖拽）
全部经鼠标在宠物浮窗上完成，**不设独立控制面板**（设置/退出走托盘）。

| 操作 | 手势 | 触发 | 效果 |
|---|---|---|---|
| 摸头 | 单击 | 位移<5px 且时长<300ms | +心情 |
| 喂食 | 双击 | 间隔<500ms | +饱食 |
| 洗澡 | 右键→"洗澡" | 右键菜单 | +清洁 |
| 戳一戳 | 右键→"戳一戳" | 同上 | -心情 +互动 |
| 拖拽 | 按住拖动 | 位移≥5px 或时长≥300ms | v0.3 拖拽+抛掷 |

手势消解：`mousedown` 记起点时间 → `mouseup` 算位移时长 → 位移<5px 且<300ms 判单击/双击，否则拖拽。v0.2 实现单击/双击/右键，预留拖拽 hook，v0.3 填入不重写交互层。右键菜单项（v0.2 起）：喂食、洗澡、戳一戳、──、设置、退出。

### 2.4 气泡系统（BubbleWidget）
宠物与用户沟通的**唯一 UI 通道**（聊天面板除外），Qt 原生渲染（与本体同层），非 QML。
```python
class BubbleType(Enum): INFO="info"; WARNING="warning"; CHAT="chat"
class BubbleWidget(QWidget):
    def show(self, text: str, kind: BubbleType, duration_ms: int = 5000) -> None: ...
    def hide(self) -> None: ...
```
- 位置：宠物头顶上方 20px，靠屏幕顶则翻下；最大宽 200px 自动换行；圆角矩形+三角箭头+半透明灰底黑字。
- 队列：同时最多 1 个，后来的覆盖先来的（WARNING 不被 INFO 覆盖）。
- 生命周期：`INFO` 5s 自动消失；`WARNING` 8s 自动消失（可点击提前关，不再永久停留）；`CHAT` 点击或 8s 消失。
- v0.1 骨架（能显示文字），v0.2 起接入养成/主动关怀/LLM。

### 2.5 应用生命周期
- **单实例锁**（v0.1）：`app.py` 入口获取文件锁（路径经 `platform.py` 解析，双端不同）。锁被占用 → 唤醒已有实例 → 退出，防多实例竞争存档/钩子/热键。
- **退出清理** `shutdown()`（v0.1 起，按序）：① 停 ProactiveScheduler ② 释放 EatMouseSession ③ 注销全局热键 ④ 保存 PetState+Memory ⑤ 关 QML engine ⑥ 移除托盘 ⑦ `QApplication.quit()`。托盘"退出"与快捷退出都走 `shutdown()`，无残留进程/钩子。

---

## 三、养成系统（差异化②）

### 数值
心情 / 饱食 / 清洁（0–100，随时间自然衰减）+ 年龄（随真实时间增长，`age_speed_multiplier` 默认 1.0）。**无经验、无等级、无金币**（经济系统见 §九）。

### 衰减与交互增益（进 config 默认值）
```json
"decay_per_hour": {"mood": 2, "fullness": 3, "cleanliness": 1.5},
"interaction_gain": {"pet": 5, "feed": 20, "clean": 15, "poke": -8}
```

### 长大机制
- 年龄到阈值 → 进化事件 → 阶段切换 `YOUNG → ADULT → FINAL`（默认幼 7 天→成，成 21 天→最终；multiplier 可调，开发用 fast-mode 秒级跳阶段）。
- 进化取本阶段养护平均分：`score = 0.4*mood + 0.4*fullness + 0.2*cleanliness`，`≥70 → HEALTHY`，否则 `NEGLECTED`。权重阈值进 config。
- 设计意图：时间驱动（非刷经验）+ 养护决定形态（非纯挂机）+ 分支产生可收集性。NEGLECTED 是永久形态不可逆，但可继续养护；想重来走重置。

### 重置机制
托盘→"重新开始"→二次确认（平台原生对话框）→清空 `pet_state.json`+`memory.json`→重启。

---

## 四、桌面物理行为（差异化①）

- **先做**：沿底边行走 + **全工作区随机游走（WANDER）** + 重力掉落 + 拖拽抛掷（带重力）+ 跟随鼠标 + idle 随机小动作。
- **WANDER**：FSM 行为模式——在当前工作区随机选目标点 → 行走过去 → idle 随机时长 → 选下一点，循环；尊重重力（地板/窗口顶面走，跨表面掉落）；频率受养成调制（心情高更爱游走，饱食低倾向走向食物）。
- **留后**：攀爬其他窗口边缘（需窗口枚举 + 权限，见平台文档）。
- **工作区**：用平台 sensor 给的可用工作区矩形（已排除 Dock/菜单栏/任务栏），多屏取合集。
- **窗口枚举性能**：事件驱动 + 缓存，或低频（≤2s）刷新；**绝不每帧枚举**。
- **全屏/演示模式**（v0.3 起）：检测当前空间是否有全屏窗口 → 宠物自动隐藏或移副屏、暂停 WANDER；演示模式（前台全屏演示软件）完全隐藏 + 禁吃鼠标；退出全屏自动恢复。检测实现见平台文档。

### 行为调制映射表（示例，进 config）
| 条件 | 行为变化 |
|---|---|
| `fullness < 20` | 觅食权重↑，玩耍↓ |
| `mood < 30` | 垂头丧气动画↑，主动互动↓ |
| `cleanliness < 30` | 洗澡请求气泡↑ |
| `mood > 60` | WANDER 游走频率↑ |
| `idle_time > 阈值` | 允许吃鼠标触发（见 §七） |

---

## 五、LLM 与工具层（借鉴参考仓库）

- **DS API + function calling**；system prompt 编码身份/性格/记忆/规则。
- 工具 registry（`tools_schema.py`）→ 平台工具实现（`tools_mac.py`/`tools_win.py`，命令映射见平台文档）。
- **危险操作确认**：`dispatch` 拦截 + 平台原生模态对话框（显示命令+风险），强制点击后继续。双端按钮文案与显示内容对齐。
- **Prompt 注入防护**：system prompt 限制工具范围；文件路径黑名单（禁 `/`、`~` 根、`rm -rf` 模式）；单次操作文件数上限。
- **错误降级**：`llm.py` try/except，网络/超时/额度耗尽降级为本地预设回复 + 日志，不崩。离线时聊天禁用，但行走/交互/长大正常。

---

## 六、立绘 provider 三级（差异化③）

接口见 §2.2。三级降级、同一签名：
1. **现在** `EmojiProvider`：实现 `get_static`/`get_frames`，按 `state.stage/mood/branch` 返回 emoji，零美术。
2. **前期** `AIArtProvider`：`get_static(state,skin)` 按 stage/branch/mood/skin 批量 AI 生成、缓存；一致性（512×512 + 固定 prompt + 固定 seed + `rembg` 去背）；`get_frames` 降级返回 emoji 帧；缓存 `assets/ai/{stage}_{branch}_{mood}_{skin}.png`；失败降级 emoji。
3. **后期** `CommissionArtProvider`：约稿图按命名约定入 `assets/`：`{stage}_{branch}_{mood}_{skin}.png`。

---

## 七、吃鼠标休息提醒（差异化缝合，最大风险点）

把休息提醒 + 整蛊 + 养成缝成一件。
- **触发**：久坐/休息提醒 + **`idle_time > 5 min` 双条件**。用户活动中只发温和气泡，绝不突袭。
- **活跃内容检测**（v0.7 起）：触发前额外检查前台是否视频播放器（白名单）→ 命中只气泡不吃鼠标。白名单进 config。
- **实现**：`ProactiveScheduler.eat_mouse` → `EatMouseSession.start`（平台鼠标抑制，见平台文档）；动画由 FSM 驱动——`ProactiveScheduler` 发事件切 `EAT_MOUSE` 状态，`EatMouseSession` 只管系统层抑制。
- **安全铁律**（不可妥协）：
  1. 只抑制鼠标，不碰键盘——随时可 Esc/热键逃生。
  2. 自动超时释放（≤15 s）。
  3. **强制吐出热键**：避开系统冲突的组合——mac `Cmd+Option+T`、win `Ctrl+Alt+T`（实现细节见 `平台适配与分工.md` §五/§六）；+ 托盘菜单，一键释放。
  4. DND 白名单：免打扰/重要时段不触发吃鼠标，只气泡。
  5. idle gate：`idle_time < 5 min` 只气泡不吃。
  6. **看门狗**：独立线程，主逻辑崩溃也 5 s 强制释放（防锁死）。
- **开发前置**：v0.7 前先做鼠标抑制 Spike 验证；权限未开时只提示不抑制。
- **养成回血**：吃鼠标 → +饱食 / +心情。

---

## 八、主动关怀（借鉴参考仓库）

- 链式唤醒 + follow-up（AI 自定下次唤醒 10–360 min + 空闲/深夜检测 + 隔离上下文）。
- 承载：久坐/喝水/护眼/吃饭提醒、节日/生日/纪念日祝福、早安晚安。
- 吃鼠标为休息提醒加强触发源（受 idle gate + DND 双约束）。
- 应用生命周期：应用未运行时主动关怀不触发（v0.11 自启后才完整），记为已知限制。

---

## 九、未来扩展位（只占接口/位，不实现）

- 3D Renderer + 光照（`Renderer3D` + `sun_direction`）。
- 经济系统：金币/库存/商店/换装。
- 攀爬窗口（见 §四留后）。
- 小游戏：答题（DS 出题，答对 +心情/年龄小加速）。
- 插件/脚本/API 接入。
- 性格养成：`PetState` 增 `personality_traits: dict`，随养护微调，注入 system prompt 使对话风格随养成演化。当前占位。
- Live2D：已弃，不预留。多宠互动/繁衍：不做。

---

## 十、打包与分发（v0.12，双端实现见平台文档）

- 平台各自打包（mac py2app `.app` / win PyInstaller `.exe`）；bundle 内 assets 与 QML 资源路径处理。
- 签名公证（可选）：未签名 wizard 兜底 + 手动授权；签名+公证顺滑，列可选不强制。
- 安装/卸载/首次权限设置文档。

---

## 十一、存储与密钥规划（路径经 `platform.py` 解析，双端不同）

| 内容 | 位置（mac / win 见平台文档） |
|---|---|
| 养成状态 / 记忆 / API 用量 | 应用数据目录下 |
| 日志 | 日志目录，按大小轮转 |
| 立绘缓存 | `assets/ai/`（bundle）+ 可覆盖用户目录 |
| 用户 config | 用户配置目录，权限 600 |
| **DS API key** | **平台密钥库**（mac Keychain / win 凭据管理器，经 `keyring`），**不放明文**；env `DEEPSEEK_API_KEY` 兜底；首次启动引导输入 |
| 权限状态 | 运行时检测，不持久存敏感 |

---

## 十二、权限引导设计

- 权限状态页（QML，`ui/`）：列所需权限 + ✓/✗。
- 深链系统设置：不支持应用内直接开启 → "打开系统设置"深链对应面板。
- 自动刷新：窗口重新聚焦时检测刷新。
- 权限被永久拒绝：明确提示 + 深链，**不反复弹系统对话框**。
- 各端所需权限见平台文档。

---

## 十三、Git / 版本规范

- 远端 `https://github.com/zzh4206/Desktop-Pet`；各端本地克隆路径见 `平台适配与分工.md` 路径节。
- 提交信息**中文说明 + 附版本号**；每里程碑打 tag。设计期 `v0.0.x`，实现 `v0.1+`。
- 改写已推历史前须确认对方未拉取该笔。

---

## 附：决策清单
- ✅ 渲染路线 A（sprite + 2D 物理），放弃 Live2D。
- ✅ 渲染路线 C（v0.13）：分层绑骨展示层与路线 A 并存可切（`presentation` 键），部件源自已验收立绘确定性拆件、零生成像素；非 Live2D 复辟——无外部绑骨管线，骨骼=自研 JSON 参数格式。
- ✅ UI 分层：本体 Qt 原生；应用型界面 QML，不引 Web 技术。
- ✅ 养成：心情/饱食/清洁/年龄，无经验/等级/金币；年龄进化 + 加权养护分(0.4/0.4/0.2,≥70) 定分支。
- ✅ 立绘 provider 三级，接口统一 `get_static`/`get_frames`（v0.1 冻结）。
- ✅ 桌面行为：底边走/掉落/抛掷/跟随/WANDER，攀爬留后。
- ✅ 吃鼠标：平台鼠标抑制 + 安全六铁律 + DND + idle gate + 看门狗 + FSM 驱动动画。
- ✅ LLM = DeepSeek；key 存平台密钥库。
- ✅ 持久化原子写 + version migrate + .bak + 配置 schema 校验。
- ✅ 交互入口手势消解(5px+300ms)；气泡三类；单实例锁 + shutdown()。
- ✅ 显示尺寸 YOUNG192/ADULT256/FINAL320，bottom_center 锚点。
- ✅ 全屏/演示检测；活跃内容检测；权限永久拒绝不反复弹；性格养成占位。
- ⏳ 版本切分见 `版本规划.md`；每版任务认领见 `工作表.md`；平台/协作见 `平台适配与分工.md`。
