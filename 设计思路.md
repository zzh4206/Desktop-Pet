# 桌宠 设计思路

> 本文件是设计的 single source of truth。功能版本切分随时可调，但**模块间的接口契约一旦定下，改动需在此处显式记录**，以防重复造轮子与返工。
>
> 状态：设计阶段（v0.0.x）。尚未开始实现。

---

## 一、定位与差异化

一只**会长大、会在桌面动来动去、立绘靠 AI 生成**的 Mac 桌宠。常驻轻量进程，本地零重计算（符合 Mac 严禁高 CPU 红线——桌宠只发 API 请求和调系统命令）。

对比参考仓库 `sjmsltx/deepseek-desktop-pet`：它是**会聊天的静态桌宠**（固定约稿立绘 + Live2D + LLM 工具）。本作差异化集中在参考仓库**都没有**的三件套：

| # | 我们的 | 参考仓库 | 说明 |
|---|---|---|---|
| ① | **桌面物理行为**（Shimeji 式） | 无，仅立绘呼吸/眨眼 | 行走/掉落/抛掷/跟随鼠标，桌宠的灵魂 |
| ② | **年龄驱动的成长养成** | 无任何养成 | 时间长大 + 养护决定长成什么样 |
| ③ | **可替换立绘演化** | 固定约稿，不可演化 | emoji→AI→约稿，接口统一 |

**渲染路线锁定**：2D sprite + 行为物理，**放弃 Live2D**（Live2D 需手工绑骨，无法 AI 生成、无法随成长阶段演化，与②③冲突）。3D + 太阳方位/光影为未来扩展方向，接口预留（见 §九）。

---

## 二、模块划分与接口契约

### 2.1 模块总览

```
桌宠/
├── pet/
│   ├── __init__.py
│   ├── window.py            # 透明置顶浮窗、角色绘制、气泡
│   ├── renderer.py          # Renderer 接口 + Renderer2D 实现（未来 Renderer3D）
│   ├── asset_provider.py    # AssetProvider 接口 + emoji/AI/约稿 三实现
│   ├── pet_state.py         # 养成 State（单一真相源）+ observer
│   ├── behavior.py          # 行为 FSM + 传感器接口
│   ├── llm.py               # DS 客户端 + function calling + usage 统计
│   ├── tools_schema.py      # 工具 registry JSON schema
│   ├── tools_mac.py         # macOS 系统工具实现 + 危险操作确认
│   ├── memory.py            # 长期记忆（memorize/forget/摘要）
│   ├── proactive.py         # 主动关怀 + 链式唤醒 + 吃鼠标
│   ├── hotkey.py            # pyobjc 全局热键
│   ├── tray.py              # 系统托盘菜单
│   └── config.py            # config.json 读写、角色定义
├── assets/                  # 立绘（emoji 期可为空）
├── app.py                   # 入口
├── config.example.json
├── requirements.txt
├── pyproject.toml
├── 设计思路.md               # 本文件
└── README.md
```

### 2.2 接口契约（核心，改这里 = 留痕）

#### AssetProvider —— 立绘素材来源（差异化③的接口）
```python
class Stage(Enum): YOUNG, ADULT, FINAL      # 成长阶段
class Branch(Enum): HEALTHY, NEGLECTED      # 进化分支（健康/落寞）
class Mood(Enum): HAPPY, NEUTRAL, SAD, SLEEPY, HUNGRY, ...

class AssetProvider(Protocol):
    def get_sprite(self, stage: Stage, mood: Mood, skin: str, branch: Branch) -> SpriteRef: ...
```
三个实现共用此接口，养成/物理/动画只认 `get_sprite`，不知图从哪来：
- `EmojiProvider`（MVP）：按 (stage, mood, branch) 返回 emoji。
- `AIArtProvider`（前期）：查/生成本地 AI 图，缓存到 `assets/`。
- `CommissionArtProvider`（后期）：约稿图按命名约定放 `assets/`。

#### Renderer —— 渲染层（为 3D 留缝）
```python
class Renderer(Protocol):
    def draw(self, state: PetState, screen_pos: tuple, dt: float) -> None: ...
```
- `Renderer2D`（现在）：blit sprite + 帧动画。
- `Renderer3D`（未来）：增加 `sun_direction` 光照参数。
- 养成/物理只跟 `Renderer.draw` 打交道，升 3D 只换实现。

#### PetState —— 养成单一真相源
```python
@dataclass
class PetState:
    mood: float          # 心情 0-100
    fullness: float      # 饱食 0-100
    cleanliness: float   # 清洁 0-100
    age: float           # 年龄（宠物天）
    stage: Stage
    branch: Branch

class PetStateStore:
    def get(self) -> PetState: ...
    def update(self, **deltas) -> None: ...          # 触发 observer
    def on_change(self, cb: Callable[[PetState], None]) -> None: ...
```
动画、物理行为、UI 全订阅 `on_change`，养成一改全员响应，杜绝"养成改了动画没更新"。

#### BehaviorFSM —— 桌面物理行为
```python
@dataclass
class Sensors:
    mouse_pos: tuple
    screen_edges: dict        # 屏幕边坐标
    windows: list             # 其他窗口框（进阶，攀爬用）
    idle_time: float

@dataclass
class Action:
    kind: str                 # move_to / fall / animate / speak / eat_mouse / follow_cursor
    payload: dict

class BehaviorFSM:
    def step(self, state: PetState, sensors: Sensors, dt: float) -> Action: ...
```
行为受养成数值调制（饿了找食物走动、困了打哈欠、开心跳舞）。

#### LLM 与工具
```python
class ToolSchema(TypedDict): name, description, parameters  # DS function calling schema
class ToolHandler(Protocol):
    def execute(self, args: dict, ctx: Ctx) -> ToolResult: ...

class ToolRegistry:
    def schemas(self) -> list[ToolSchema]: ...
    def dispatch(self, name: str, args: dict, ctx: Ctx) -> ToolResult: ...  # 危险操作此处拦截+确认
```
Mac 工具层实现见 §五。

#### 记忆 / 主动消息
```python
class MemoryStore:
    def memorize(self, fact: str, importance: float) -> str: ...
    def recall(self, query: str, k: int) -> list: ...
    def forget(self, mem_id: str) -> None: ...
    def summarize_session(self) -> None: ...   # 滚屏摘要

class ProactiveScheduler:
    def schedule_wake(self, delay_min: float, reason_ctx: dict) -> None: ...  # 链式唤醒
    def follow_up(self, event: str, when: float) -> None: ...
    def eat_mouse(self, duration_s: float) -> EatMouseSession: ...            # 见 §七
```

---

## 三、养成系统（差异化②）

### 数值
- **心情 / 饱食 / 清洁**（0–100，随时间自然衰减）。
- **年龄**：随真实时间增长，倍率可配（默认略加速，避免等太久）。
- **无经验、无等级、无金币**（经济系统暂不做，见 §九）。

### 怎么长大（核心机制）
- 年龄到阈值 → 触发**进化事件** → 成长阶段切换（幼 `YOUNG` → 成 `ADULT` → 最终形 `FINAL`）。
- 进化时取**本阶段养护平均分**（心情/饱食/清洁的均值）决定**进化分支**：
  - 养得好 → `HEALTHY`（健康形态）
  - 疏于照顾 → `NEGLECTED`（落寞/虚弱形态）
- 设计意图：长大是**时间驱动的（不是刷经验刷出来）**+**养护质量决定长成什么样（不是纯挂机）**，且分支形态产生**可收集性**——同宠不同养护 → 不同样，自然接上"收集"价值。

### 养护交互（同时是卖萌动画的触发源）
- 摸头（+心情）、喂食（+饱食）、洗澡（+清洁）、戳一戳（-心情 +互动）。

---

## 四、桌面物理行为（差异化①）

- **先做**：沿屏幕底边行走 + 重力掉落 + 拖拽抛掷（带重力）+ 跟随鼠标 + idle 随机小动作（喝水/伸懒腰/追光标/打滚）。
- 由 `BehaviorFSM.step` 驱动，状态由养成数值调制（见 §三）。
- **留后**：攀爬其他窗口边缘——需 Mac Accessibility API（pyobjc `AXUIElement`）枚举窗口框 + 权限，最麻烦，作为进阶。

---

## 五、LLM 与 Mac 工具层（借鉴参考仓库）

- **DS API + function calling**；system prompt 编码身份/性格/记忆/规则/待办。
- 工具 registry（`tools_schema.py`）→ `tools_mac.py` 实现。参考仓库的 PowerShell 层整体换 macOS 命令：

| 功能 | macOS 实现 |
|---|---|
| 开程序/网页 | `open -a "App"` / `open URL` |
| 锁屏 | `osascript -e 'tell app "System Events" to keystroke "q" using {control down, command down}'`（需 Accessibility）|
| 音量 | `osascript -e 'set volume output volume X'` |
| 进程管理 | `ps aux` / `pkill -x` / `killall` |
| 文件搜索 | `mdfind`（Spotlight）|
| 剪贴板 | `pbcopy` / `pbpaste` |
| 睡眠（危险）| `osascript -e 'tell app "System Events" to sleep'` |

- **危险操作（关机/睡眠/删文件）**：在 `ToolRegistry.dispatch` 拦截 + 用户确认。
- 首次启动做**权限引导向导**：辅助功能/自动化/磁盘等权限清单 + 跳系统设置。

---

## 六、立绘 provider 三级（差异化③）

`AssetProvider` 接口见 §2.2。三级降级、对外同一签名：

1. **现在**：`EmojiProvider`——按 (stage, mood, branch) 返回 emoji，逻辑全跑通、零美术依赖。
2. **前期**：`AIArtProvider`——按 (stage, mood) 批量 AI 生成、缓存 `assets/`，接口不变。
3. **后期条件允许**：`CommissionArtProvider`——约稿图按命名约定入 `assets/`，照样跑。

---

## 七、吃鼠标休息提醒（差异化缝合，参考仓库无）

把**休息提醒 + 整蛊 + 养成**缝成一件。

- **触发**：主动关怀的久坐/休息提醒时，宠物进入"吃鼠标"模式。
- **实现**：Mac `CGEventTap` 抑制鼠标输入 N 秒 + 宠物在光标位置咀嚼动画（需 Accessibility 权限）。
- **安全铁律**（防锁死，不可妥协）：
  1. **只抑制鼠标，不碰键盘**——用户随时可 Esc/热键逃生。
  2. **自动超时释放**（≤15s）。
  3. **全局热键"强制吐出"**（如 `Cmd+Option+Esc`）+ 托盘菜单，一键立即释放。
  4. **DND 白名单**：处于免打扰/重要时段时**不触发吃鼠标**，只发温和气泡提醒（避免应对重要问题时尴尬）。
- **养成回血**：吃鼠标 → +饱食 / +心情。"强迫你休息，顺便喂饱它"。
- **接口**：
```python
class EatMouseSession:
    def start(self, duration_s: float) -> None: ...
    def force_spit(self) -> None: ...            # 强制立即释放（热键/托盘触发）
    def on_dnd_active(self) -> bool: ...         # DND 命中则跳过触发
```

---

## 八、主动关怀（借鉴参考仓库）

- 链式唤醒 + follow-up（AI 自定下次唤醒 10–360 分钟 + 空闲/深夜检测 + 隔离上下文）。
- 承载：久坐/喝水/护眼/按时吃饭提醒、节日/生日/纪念日祝福、早安晚安。
- **吃鼠标**作为休息提醒的"加强触发源"之一（见 §七）。

---

## 九、未来扩展位（只占接口/位，不实现）

- **3D Renderer + 光照**（`Renderer3D` + `sun_direction`，见 §2.2）。
- **经济系统**：金币/库存/商店/换装（现 §三明确暂不做）。
- **攀爬窗口**（见 §四留后）。
- **小游戏**：答题（DS 出题，答对 +心情/小加速年龄），不做整套游戏库。
- **插件/脚本/API 接入**：留扩展点。
- **Live2D**：路线已弃（见 §一），不预留。
- **多宠互动/繁衍**：不做（单宠成长线已够重）。

---

## 十、Git / 版本规范

- 本地：`/Users/zzh4206/桌宠`
- 远端：`https://github.com/zzh4206/Desktop-Pet`
- 提交信息一律**中文说明 + 附版本号**；每里程碑打 git tag。
- 阶段：设计期 `v0.0.x`；实现里程碑 `v0.1+`（版本切分稍后定，本文件先行冻结接口）。

---

## 附：待确认 / 已确认决策

- ✅ 渲染路线 A（sprite + 2D 物理），放弃 Live2D。
- ✅ 养成数值：心情/饱食/清洁 + 年龄；无经验/等级/金币。
- ✅ 长大机制：年龄到阈值进化 + 养护分决定分支（HEALTHY/NEGLECTED）。
- ✅ 立绘 provider 三级（emoji→AI→约稿），接口统一 `get_sprite`。
- ✅ 桌面行为先做底边行走/掉落/抛掷/跟随鼠标，攀爬窗口留后。
- ✅ 吃鼠标休息提醒 + 安全铁律 + DND 白名单。
- ✅ LLM 用 DeepSeek。
- ⏳ 版本切分与里程碑：稍后定。
