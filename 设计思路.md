# 桌宠 设计思路

> 本文件是设计的 single source of truth。功能版本切分见 `版本规划.md`。**模块间接口契约一旦定下，改动需在此处显式记录**，以防重复造轮子与返工。
>
> 状态：设计阶段（v0.0.x）。尚未开始实现。
> 修订：v0.0.3 整合设计师评审反馈（P0/P1/P2）。

---

## 一、定位与差异化

一只**会长大、会在桌面动来动去、立绘靠 AI 生成**的 Mac 桌宠。常驻轻量进程，本地零重计算。

对比参考仓库 `sjmsltx/deepseek-desktop-pet`（会聊天的静态桌宠：固定约稿立绘 + Live2D + LLM 工具）。本作差异化集中在参考仓库**都没有**的三件套：

| # | 我们的 | 参考仓库 | 说明 |
|---|---|---|---|
| ① | 桌面物理行为（Shimeji 式） | 无，仅立绘呼吸/眨眼 | 行走/掉落/抛掷/跟随鼠标 |
| ② | 年龄驱动的成长养成 | 无任何养成 | 时间长大 + 养护决定长成什么样 |
| ③ | 可替换立绘演化 | 固定约稿，不可演化 | emoji→AI→约稿，接口统一 |

**渲染路线**：2D sprite + 行为物理，**放弃 Live2D**（需手工绑骨，无法 AI 生成/随阶段演化，与②③冲突）。3D + 太阳方位/光影为未来扩展（§九）。

### 性能红线（量化，开发期用 `psutil` 监控）

- 空闲平均 CPU < 1%（M 系列基线）
- 动画运行峰值 CPU < 5%
- 常驻内存 < 200 MB
- 网络请求间隔 > 5 s（主动对话除外）
- 达成手段：timer/事件驱动而非轮询；窗口枚举缓存；动画帧率限 30fps。

---

## 二、模块划分与接口契约

### 2.1 模块总览

```
桌宠/
├── pet/
│   ├── __init__.py
│   ├── window.py            # 透明置顶浮窗、气泡
│   ├── renderer.py          # Renderer 接口 + Renderer2D（未来 Renderer3D）
│   ├── asset_provider.py    # AssetProvider + emoji/AI/约稿 三实现
│   ├── pet_state.py         # 养成 State（单一真相源）+ observer + 持久化
│   ├── behavior.py          # ActionType + 行为 FSM + 传感器 + 调制表
│   ├── llm.py               # DS 客户端 + function calling + usage + 降级
│   ├── tools_schema.py      # ToolRegistry + ToolContext + 危险操作拦截
│   ├── tools_mac.py         # macOS 工具实现
│   ├── memory.py            # 长期记忆
│   ├── proactive.py        # 主动关怀 + 链式唤醒 + 吃鼠标 session
│   ├── hotkey.py            # pyobjc 全局热键
│   ├── tray.py              # 系统托盘
│   ├── permissions.py      # 权限状态页 + 深链系统设置 + 自动刷新
│   ├── config.py            # config 读写、角色定义
│   └── logging_setup.py    # 日志（~/Library/Logs/桌宠/，轮转）
├── assets/                  # 立绘
├── app.py                   # 入口（--verbose）
├── config.example.json
├── requirements.txt
├── pyproject.toml
├── 设计思路.md / 版本规划.md
└── README.md
```

### 2.2 接口契约（核心，改此 = 留痕）

#### AssetProvider —— 立绘来源（差异化③）
```python
class Stage(Enum): YOUNG, ADULT, FINAL
class Branch(Enum): HEALTHY, NEGLECTED
class Mood(Enum): HAPPY, NEUTRAL, SAD, SLEEPY, HUNGRY, ...

class AssetProvider(Protocol):
    def get_static(self, state: PetState, skin: str = "default") -> SpriteRef: ...
    def get_frames(self, state: PetState, action: ActionType, skin: str = "default") -> list[SpriteRef]: ...
```
- `get_static`：静态表情/阶段图（AI 立绘主战场）。
- `get_frames`：动作帧序列（伸懒腰/打滚/咀嚼等动画）。
- **v0.1 即定义此双方法接口**，避免 v0.10 切 AI provider 时动画崩。`EmojiProvider` 两方法都实现（emoji 无真动画→返回单帧或简单循环）；`AIArtProvider` 先实现 `get_static`，`get_frames` 可降级返回 emoji 帧。
- `skin`：可选皮肤 variant id，默认 `"default"`，供 commission/AI 出变体。
- 三个实现共用签名；养成/物理/动画只认这两个方法，不知图从哪来。
  - `EmojiProvider`（MVP）
  - `AIArtProvider`（前期，见 §六一致性要求）
  - `CommissionArtProvider`（后期，命名见 §六）

#### Renderer —— 渲染层（为 3D 留缝）
```python
class Renderer(Protocol):
    def draw(self, state: PetState, screen_pos: tuple,
             animation_state: str, facing: int, dragging: bool, dt: float) -> None: ...
```
- `animation_state`：当前动作名（选帧）；`facing`：朝向 -1/1；`dragging`：被拖拽（影响渲染优先级）。
- `Renderer2D`（现在）：blit sprite + 帧动画。
- `Renderer3D`（未来）：增 `sun_direction` 光照参数。
- 升 3D 只换实现，养成/物理不动。

#### PetState —— 养成单一真相源 + 持久化
```python
@dataclass
class PetState:
    mood: float          # 0-100
    fullness: float
    cleanliness: float
    age: float           # 宠物天
    stage: Stage
    branch: Branch

class PetStateStore:
    def get(self) -> PetState: ...
    def update(self, **deltas) -> None: ...          # 触发 observer
    def on_change(self, cb: Callable[[PetState], None]) -> None: ...
    def save(self, path: Path) -> None: ...          # v0.0.3 补：持久化
    @classmethod
    def load(cls, path: Path) -> "PetStateStore": ...# 重启不丢养成
```
- 持久化格式：JSON（轻量够用）；路径见 §十一存储规划。

#### ActionType + Action —— 行为输出（强类型）
```python
class ActionType(Enum):
    MOVE_TO = "move_to"
    FALL = "fall"
    ANIMATE = "animate"
    SPEAK = "speak"
    EAT_MOUSE = "eat_mouse"
    FOLLOW_CURSOR = "follow_cursor"

# WANDER 是 BehaviorFSM 行为模式，通过反复发 MOVE_TO（随机目标点）实现，非独立 ActionType。

@dataclass
class Action:
    type: ActionType
    params: dict          # 各 type 自有 key，文档列明
```

#### BehaviorFSM —— 桌面物理行为
```python
@dataclass
class Sensors:
    mouse_pos: tuple
    work_area: dict         # NSScreen.visibleFrame（已排除 Dock+菜单栏），多屏取合集
    windows: list           # 其他窗口框（进阶，事件驱动+缓存，不每帧枚举）
    idle_time: float        # 系统空闲秒（吃鼠标 gate 用）

class BehaviorFSM:
    def step(self, state: PetState, sensors: Sensors, dt: float) -> Action: ...
    def handle_event(self, event: str) -> None: ...   # 如 ProactiveScheduler 触发 EAT_MOUSE
```
- 行为受养成数值调制，显式映射表（见 §四）。

#### ToolContext + ToolRegistry —— LLM 工具
```python
@dataclass
class ToolContext:
    pet_state: PetState
    user_name: str
    config: dict
    window_info: Optional[dict]

class ToolHandler(Protocol):
    def execute(self, args: dict, ctx: ToolContext) -> ToolResult: ...

class ToolRegistry:
    def schemas(self) -> list[ToolSchema]: ...
    def dispatch(self, name: str, args: dict, ctx: ToolContext) -> ToolResult: ...
```
- `dispatch` 处拦截危险操作 + `NSAlert` 确认 + 参数黑名单校验（见 §五）。

#### 记忆 / 主动消息
```python
class MemoryStore:
    def memorize(self, fact: str, importance: float) -> str: ...
    def recall(self, query: str, k: int) -> list: ...
    def forget(self, mem_id: str) -> None: ...
    def summarize_session(self) -> None: ...

class ProactiveScheduler:
    def schedule_wake(self, delay_min: float, reason_ctx: dict) -> None: ...
    def follow_up(self, event: str, when: float) -> None: ...
    def eat_mouse(self, duration_s: float) -> EatMouseSession: ...   # 见 §七

class EatMouseSession:
    def start(self, duration_s: float) -> None: ...     # 仅抑制鼠标
    def force_spit(self) -> None: ...                    # 热键/托盘触发
    def on_dnd_active(self) -> bool: ...
```

---

## 三、养成系统（差异化②）

### 数值
- **心情 / 饱食 / 清洁**（0–100，随时间自然衰减）。
- **年龄**：随真实时间增长，倍率可配（`age_speed_multiplier`，默认 1.0）。
- **无经验、无等级、无金币**（经济系统见 §九）。

### 衰减与交互增益（进 config，默认值）
```json
"decay_per_hour": {"mood": 2, "fullness": 3, "cleanliness": 1.5},
"interaction_gain": {"pet": 5, "feed": 20, "clean": 15, "poke": -8}
```

### 长大机制
- 年龄到阈值 → **进化事件** → 阶段切换 `YOUNG → ADULT → FINAL`。
- 默认阶段时长：**幼年 7 天 → 成年，成年 21 天 → 最终形**（multiplier 可调；开发用 fast-mode 秒级跳阶段便于测试）。
- 进化时取**本阶段养护平均分**，按权重：
  ```
  score = 0.4*mood + 0.4*fullness + 0.2*cleanliness   # 饱食/心情为主，清洁次之
  score >= 70 → HEALTHY，否则 NEGLECTED
  ```
  权重与阈值进 config 可调。
- 设计意图：时间驱动（非刷经验）+ 养护质量决定形态（非纯挂机）+ 分支产生可收集性。

### 养护交互（卖萌动画触发源）
摸头（+心情）/喂食（+饱食）/洗澡（+清洁）/戳一戳（-心情 +互动）。

---

## 四、桌面物理行为（差异化①）

- **先做**：沿底边行走 + **全工作区随机游走（WANDER）** + 重力掉落 + 拖拽抛掷（带重力）+ 跟随鼠标 + idle 随机小动作。
- **随机游走（WANDER）**：`BehaviorFSM` 的一个行为模式——在当前工作区（`NSScreen.visibleFrame` 合集）内随机选一个目标点，用当前行走方式移动过去，到达后 idle 一段随机时长，再选下一个目标，循环。尊重重力模型（在地板/窗口顶面行走，跨表面时掉落）。是宠物"活着/自主"感的主要来源，由养成数值调制出现频率（心情高更爱游走，饱食低倾向于走向食物）。
- **留后**：攀爬其他窗口边缘（Accessibility `AXUIElement` 枚举窗口框 + 权限）。
- 由 `BehaviorFSM.step` 驱动；状态受养成数值调制。
- **工作区**：用 `NSScreen.visibleFrame`（已自动排除 Dock + 菜单栏），多显示器取所有屏 visibleFrame 合集。
- **窗口枚举性能**：事件驱动 + 缓存（监听 `NSWorkspace.didActivateApplicationNotification` 等），或低频（≤2s）刷新；**绝不每帧枚举**。

### 行为调制映射表（示例，进 config）
| 条件 | 行为变化 |
|---|---|
| `fullness < 20` | 觅食行为权重↑，玩耍↓ |
| `mood < 30` | 垂头丧气动画↑，主动互动↓ |
| `cleanliness < 30` | 洗澡请求气泡↑ |
| `mood > 60` | WANDER 游走频率↑，更爱乱逛 |
| `idle_time > 阈值` | 允许吃鼠标触发（见 §七） |

---

## 五、LLM 与 Mac 工具层（借鉴参考仓库）

- **DS API + function calling**；system prompt 编码身份/性格/记忆/规则。
- 工具 registry → `tools_mac.py`，PowerShell 换 macOS 命令：

| 功能 | macOS 实现 |
|---|---|
| 开程序/网页 | `open -a "App"` / `open URL` |
| 锁屏 | `osascript ... keystroke "q" using {control down, command down}`（需 Accessibility）|
| 音量 | `osascript -e 'set volume output volume X'` |
| 进程管理 | `ps aux` / `pkill -x` / `killall` |
| 文件搜索 | `mdfind` |
| 剪贴板 | `pbcopy` / `pbpaste` |
| 睡眠（危险）| `osascript -e 'tell app "System Events" to sleep'` |

- **危险操作确认**：`dispatch` 拦截 + `NSAlert`/`osascript display dialog`，**显示具体命令与风险**，强制点击后继续。
- **Prompt 注入防护**：system prompt 明确限制工具范围；文件路径参数黑名单（禁 `/`、`~` 根、`rm -rf` 模式）；单次操作文件数上限。
- **错误降级**：`llm.py` try/except，网络/超时/额度耗尽时降级为本地预设回复 + 日志，不崩。

---

## 六、立绘 provider 三级（差异化③）

`AssetProvider` 接口见 §2.2。三级降级、对外同一签名：

1. **现在**：`EmojiProvider`——按 `(stage, mood, branch)` 返回 emoji，零美术依赖。
2. **前期**：`AIArtProvider`——按 `(stage, mood, branch, skin)` 批量 AI 生成、缓存 `assets/`。
   - **一致性要求**：统一 512×512；固定风格 prompt（扁平卡通、纯色背景、无文字）；**固定 seed**；生成后 `rembg` 去背景；缓存路径 `assets/ai/{stage}_{branch}_{mood}_{skin}.png`。
   - 生成失败优雅降级到 emoji。
3. **后期**：`CommissionArtProvider`——约稿图按命名约定入 `assets/`：
   - **命名约定（尽早定死）**：`{stage}_{branch}_{mood}_{skin}.png`，如 `adult_healthy_happy_default.png`。

---

## 七、吃鼠标休息提醒（差异化缝合，最大风险点）

把休息提醒 + 整蛊 + 养成缝成一件。

- **触发**：主动关怀的久坐/休息提醒 + **`idle_time > 阈值`（默认 5 min）双条件**。用户正在活动时**只发温和气泡**，绝不突袭。
- **实现**：`ProactiveScheduler.eat_mouse` → `EatMouseSession.start`（Mac `CGEventTap` 抑制鼠标，需 Accessibility）；**动画由 BehaviorFSM 驱动**：`ProactiveScheduler` 向 FSM 发事件切到 `EAT_MOUSE` 状态，`EatMouseSession` 只管系统层抑制。
- **安全铁律**（不可妥协）：
  1. **只抑制鼠标，不碰键盘**——用户随时可 Esc/热键逃生。
  2. 自动超时释放（≤15 s）。
  3. **强制吐出热键 `Cmd+Option+Shift+P`**（注：原拟 `Cmd+Option+Esc` 是系统强制退出键，冲突，已弃）+ 托盘菜单，一键立即释放。
  4. **DND 白名单**：免打扰/重要时段**不触发吃鼠标**，只气泡提醒。
  5. **idle gate**：`idle_time < 5 min` 时即使到休息点也只气泡，不吃鼠标。
  6. **看门狗**：独立于主逻辑的 watchdog 线程——即使 FSM/主逻辑崩溃，5 s 后强制释放鼠标（防 bug 锁死用户）。
- **开发前置**：v0.7 前先做 CGEventTap Spike 验证；权限未开时只提示不抑制。
- **养成回血**：吃鼠标 → +饱食 / +心情。

---

## 八、主动关怀（借鉴参考仓库）

- 链式唤醒 + follow-up（AI 自定下次唤醒 10–360 min + 空闲/深夜检测 + 隔离上下文）。
- 承载：久坐/喝水/护眼/按时吃饭提醒、节日/生日/纪念日祝福、早安晚安。
- 吃鼠标为休息提醒加强触发源（见 §七，受 idle gate + DND 双约束）。

---

## 九、未来扩展位（只占接口/位，不实现）

- 3D Renderer + 光照（`Renderer3D` + `sun_direction`）。
- 经济系统：金币/库存/商店/换装。
- 攀爬窗口（见 §四留后）。
- 小游戏：答题（DS 出题，答对 +心情/年龄小加速）。
- 插件/脚本/API 接入。
- Live2D：路线已弃，不预留。
- 多宠互动/繁衍：不做。

---

## 十、打包与分发（v0.12）

- 工具：**py2app** 出 `.app` bundle（Mac 原生，Dock/菜单栏行为正常）。`assets`、权限路径处理 bundle 内引用。
- **签名与公证（可选）**：
  - 未签名：可用，但 Gatekeeper 首启拦截（右键打开绕过）；Accessibility 权限需用户手动在系统设置添加 → **`permissions.py` wizard 兜底引导**。
  - 签名 + 公证（需 Apple Developer $99/年）：首启顺滑，权限体验更好。列为可选，不强制。
- 安装：拖到 `/Applications`；卸载：删 `.app` + 清理 `~/Library/Application Support/桌宠` 与日志。
- 权限申请流程测试归 v0.12（需 .app 才能测）。

---

## 十一、存储与密钥规划

| 内容 | 位置 |
|---|---|
| 养成状态 `pet_state.json` | `~/Library/Application Support/桌宠/` |
| 记忆 `memory.json` | 同上 |
| API 用量 `api_stats.json` | 同上 |
| 日志 | `~/Library/Logs/桌宠/`（按大小轮转） |
| 立绘缓存 | `assets/ai/`（bundle 内）与可覆盖用户目录 |
| 用户 config | `~/.config/桌宠/config.json`（权限 600） |
| **DS API key** | **macOS Keychain**（经 `keyring`），**不放明文文件**；env var `DEEPSEEK_API_KEY` 兜底；首次启动引导输入 |
| 权限状态 | `permissions.py` 运行时检测，不持久存敏感信息 |

---

## 十二、权限引导设计

- **权限状态页**：列出所需权限（辅助功能/自动化/等）及当前状态（✓/✗）。
- **深链系统设置**：macOS 不支持应用内直接开启 → 提供"打开系统设置"按钮，深链到对应面板。
- **自动刷新**：权限变化后（窗口重新聚焦时）检测并刷新状态。
- 所需权限：全局热键/按键模拟/吃鼠标 → 辅助功能；控制其他 app → 自动化。

---

## 十三、Git / 版本规范

- 本地：`/Users/zzh4206/桌宠`；远端：`https://github.com/zzh4206/Desktop-Pet`。
- 提交信息**中文说明 + 附版本号**；每里程碑打 git tag。
- 阶段：设计期 `v0.0.x`；实现里程碑 `v0.1+`。

---

## 附：决策清单

- ✅ 渲染路线 A（sprite + 2D 物理），放弃 Live2D。
- ✅ 养成数值：心情/饱食/清洁/年龄；无经验/等级/金币。
- ✅ 长大：年龄到阈值进化 + 加权养护分（0.4/0.4/0.2，≥70）决定分支。
- ✅ 立绘 provider 三级（emoji→AI→约稿），接口统一 `get_sprite`。
- ✅ 桌面行为先做底边走/掉落/抛掷/跟随，攀爬窗口留后。
- ✅ 吃鼠标：CGEventTap + 安全铁律 + DND + **idle gate** + 热键改 `Cmd+Option+Shift+P` + FSM 驱动动画。
- ✅ LLM = DeepSeek；key 存 Keychain。
- ✅ 持久化 save/load；存储规划见 §十一。
- ✅ 日志系统 + 轮转 + `--verbose`。
- ✅ 权限状态页 + 深链 + 自动刷新。
- ✅ 性能红线量化（idle<1%/动画<5%/mem<200MB/网络>5s）。
- ✅ 工具层 prompt 注入防护 + 危险操作 NSAlert + 错误降级。
- ⏳ 版本切分见 `版本规划.md`。
