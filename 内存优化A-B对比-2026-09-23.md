# 内存优化 A/B 对比报告（mac 端，30min 浸泡）

> 对照组（优化前）vs 实验组（P0+P1 落地后），同脚本、同口径、同场景。
> 日期：对照 2026-09-23 08:57 / 实验 2026-09-23 10:07，各 1800s（1s × 1800 点，psutil）。

## 1. 结论

**P0+P1 生效且收益显著：加载峰值 −44%（−203MB），稳态中位 −28.6MB 已踩进 ≤200MB 红线；CPU 不变、线程 −34%、30 分钟零崩溃零异常日志。**
但稳态 P95 / 平台期（215–228MB）仍略超 200MB，要「稳定 ≤200MB」还需再攻基线或压平台期。

## 2. 实验组改动（对照组之后落的代码）

| 项 | 内容 |
| --- | --- |
| P0 | ort/tokenizers 懒 import + `enable_cpu_mem_arena=False` + `intra_op_num_threads=2`（`pet/chat_emotion.py`） |
| P0 | 冷启动只建 store、不建 engine、不 import onnxruntime；后台 QThread 预热（`app.py`） |
| P0 | 双解码消除：stat 零解码预检（`pet/rig/skinned_mesh_item.py:793`） |
| P1 | 20 层纹理 trim + UV 偏移 + mesh 重生成（`tools/trim_rig_textures.py` 等，raw RGBA 124→17MB） |

> ⚠️ `APP_VERSION` 未 bump，两组日志均打印 `v0.15.0`，光看日志无法区分对照组/实验组。

## 3. 结果对比

| 指标 | 对照组 | 实验组 | Δ |
| --- | --- | --- | --- |
| 加载峰值 RSS (MB) | 466.4 | 263.2 | **−203.2（−43.6%）** |
| 峰值时刻 (s) | 11 | 22 | +11 |
| 稳态(90s+) RSS 中位 (MB) | 204.6 | 176.0 | −28.6 |
| 稳态(90s+) RSS 均值 (MB) | 218.0 | 191.4 | −26.6 |
| 稳态(90s+) RSS P95 (MB) | 264.2 | 223.4 | −40.8 |
| 稳态(90s+) RSS 峰值 (MB) | 266.4 | 228.3 | −38.2 |
| RSS 全程最低 (MB) | 166.1 | 135.1 | −31.1 |
| CPU 全程均值（原始%） | 24.55 | 24.33 | −0.2 |
| CPU 峰值 (%) | 88.80 | 74.30 | −14.5 |
| 线程均值 / 峰值 | 17.6 / 24 | 11.6 / 16 | −6.0 / −8 |

内存曲线：

```
对照组  245.6 → 466.4(11s 峰) → 260 平台(90s) → 166.1 谷(13min) → 204.8 末
实验组  135.1 → 263.2(22s 峰) → 198→223 平台    → 147.3 谷(14min) → 175.6 末
```

## 4. 发现（诚实护栏）

1. **峰值收益最大**：−203MB 主因是 P1 trim（Apple Silicon 统一内存下 CPU 解码副本
   + GPU 纹理两份同削）+ P0 ort 冷态不 import，与方案页量级吻合。
2. **稳态中位达标、P95 未达标**：中位 176MB 踩进 200MB 红线，但平台期 215–228MB
   仍超；「稳定 ≤200MB」还需再压基线或削平台期。
3. **CPU 是渲染开销、与内存优化无关**：全程 ~24.3% 原始值（≈2.4% 整机）几乎没动。
4. **~13min 释放事件可复现**：对照 12.8min（252→166MB）、实验 13.4min（220→178MB），
   跨代码复现说明是周期性机制而非随机 GC，根因未定位（日志无对应事件），建议加 GC 探针。
5. **首末变化符号相反非泄漏**：实验 +40.6MB 是「从超低起点 135MB 爬到稳态」的自然
   过程，~810s 后稳定 169–178MB，无持续增长。

## 5. 附

- 采样明细：对照 `tools/monitor_pet_soak_30min.csv` · 实验 `tools/monitor_pet_soak_30min_exp.csv`
- 监控脚本：`tools/monitor_pet.py`
- Wiki 对照页：[wiki/实验-资源占用对照组.md](wiki/实验-资源占用对照组.md)

---
*报告人：mac 端（资源占用 A/B 专项）；数据由 psutil 1s 采样生成。*
