# 桌宠 平台适配与分工

> 合并自：原 `Windows适配计划.md`（适配部分）+ `设计思路.md` 抽出的 Mac 实现部分 + 原 `分工.md`。
> 配套：`设计思路.md`（共享核心接口，平台中立）、`版本规划.md`（里程碑验收）、`工作表.md`（每版任务认领）。
> 本文档定**双平台适配实现 + 协作规则与分工边界**，不点名到人——"端"指平台侧（mac端 / win端）。
> 状态：草稿，待双方确认。
> 修订：v0.1.1 `window.py` 归位共享（注入差异）；win 端强制吐出热键定为 `Ctrl+Alt+T`（决策点5 已决）。

---

## 一、协作方式

1. **双平台并行，非追赶**：mac端与win端并行推进同一版本号，不存在单侧追赶。
2. **三层结构**：
   - **共享核心**——双端共维护，**不 import 平台库**（pyobjc / pywin32 / ctypes），违反 = review 打回 + 在 §四登记表补行；
   - **mac shim**——mac端独有，mac端负责；
   - **win shim**——win端独有，win端负责，接口签名与 mac 对齐。
3. **接口契约是唯一同步点**：`设计思路.md` §2.2 为单一真相源。任一端改共享接口 = 同步改设计思路 + 通知对方 + 对方 review 通过才合。
4. **认领制**：改共享文件前在 issue/群声明，同一共享文件同一时间只一方在改。每版共享**任务**在 `工作表.md` 不预先指派，谁认领谁做，该模块主笔 review。
5. **版本完成 = 双端各自 Must 全绿**（见 `版本规划.md`）。Should 项各追各，不互相等；落后方补齐后一起合入，不单独跳版。
6. **里程碑评审**：每版完成后双端一起约 15 min 验收，Must 全绿再进下一版。
7. **共享文件合入前**：对方过一遍接口签名，确认无漂移。
8. **不引入 PR 流程**：两人协作直接 `main` + 认领制，暂不需要分支/review-gate；后续人数或复杂度增长再议。

---

## 二、分工（按端）

### 共享核心（双端共维护，按模块主笔分工 + 另一方评审/适配）
`pet_state` / `behavior` / `llm` / `memory` / `asset_provider` / `renderer` / `tools_schema` / `proactive` / `config` / `logging_setup` / `tray` / `ui`(QML) / `window`（PySide6 跨平台框架，透明/穿透/工作区差异经 `platform.py` 注入）

### mac端独有（mac shim）
`sensor_mac`(鼠标·空闲·工作区·窗口枚举) / `tools_mac` / `hotkey_mac` / `mouse_lock_mac`(CGEventTap 吃鼠标) / `permissions_mac` / 打包(py2app) / 自启(LaunchAgents) / 数据+日志路径(`~/Library`)

### win端独有（win shim）
`sensor_win`(`GetCursorPos`/`GetLastInputInfo`/`EnumWindows`) / `tools_win` / `hotkey_win`(`RegisterHotKey`) / `mouse_lock_win`(`WH_MOUSE_LL` 钩子) / `permissions_win` / 打包(PyInstaller) / 自启(注册表 Run) / 数据路径(`%LOCALAPPDATA%`)

> `window.py` 保持 PySide6 跨平台共享 + `platform.py` 注入差异；穿透差异过大才拆 `window_mac`/`window_win`（v0.1 W1 Spike 后定）。

### 共享适配入口
`platform.py`——按 `sys.platform` 选平台 shim 工厂，`app.py` 入口只碰它。

### 代码组织（沿用 `_mac`/`_win` 后缀 + 工厂）
```
pet/
├── platform.py                 # 按 sys.platform 选 shim
├── window.py                   # PySide6 跨平台框架（透明/穿透/工作区经 platform 注入）
├── sensor_mac.py / sensor_win.py
├── tools_mac.py / tools_win.py
├── hotkey_mac.py / hotkey_win.py
├── mouse_lock_mac.py / mouse_lock_win.py
├── permissions_mac.py / permissions_win.py
├── ui/                         # QML 应用型界面，双平台共享
└── 其余共享文件不变
```

---

## 三、共享核心主笔认领台账（按工作量粗略分配）

> 主笔 = 默认 owner + 该模块 reviewer；另一方 = 评审 + 适配本平台引用点。每版具体任务在 `工作表.md` 不预先指派、认领实现。

| 模块 | 主笔端 | 评审/适配端 |
|---|---|---|
| behavior（FSM+物理+WANDER） | mac | win |
| renderer | mac | win |
| asset_provider | mac | win |
| pet_state | mac | win |
| config | mac | win |
| logging_setup | mac | win |
| tray | mac | win |
| ui（QML 聊天/设置/记忆/权限） | win | mac |
| llm | win | mac |
| proactive | win | mac |
| memory | win | mac |
| tools_schema | win | mac |

---

## 四、平台差异登记表（活文档，双向适配对照）

任一侧在共享代码引入新平台依赖，必须在此登记一行；另一侧照表补齐。

| 模块 | macOS 实现 | 用途 | Windows 实现 |
|---|---|---|---|
| 透明置顶浮窗 | NSWindow + PySide6 | 常驻浮窗 | `WA_TranslucentBackground`+`WindowStaysOnTopHint`+`WS_EX_TRANSPARENT`/`WS_EX_LAYERED` 点击穿透 |
| 工作区(多屏) | `NSScreen.visibleFrame` | 可用区 | `QScreen.availableGeometry()` / `SPI_GETWORKAREA` |
| 鼠标/空闲传感器 | NSEvent / CGEvent | Sensors | `GetCursorPos` / `GetLastInputInfo`（无需特权） |
| 窗口枚举(后置) | `AXUIElement`(Accessibility) | 攀爬窗口 | `EnumWindows`(pywin32) |
| 图层双检查 `Sensors.solid_at` | **待补**（不填→FSM 纯几何判定，功能不缺） | 攀爬/落点二次确认（v0.3.6 增量字段） | `WindowFromPoint`+`GetAncestor(GA_ROOT)`+有效 hwnd 集 |
| 全局热键 | pyobjc 全局热键 | 唤聊天/强制吐出 | `RegisterHotKey`(ctypes) |
| 吃鼠标抑制 | `CGEventTap` | 休息提醒 | `WH_MOUSE_LL` 钩子拦截移动+钉光标（看门狗/超时逻辑共用） |
| 系统工具层 | `open -a`/`osascript`/`mdfind`/`pbcopy`/`pkill` | LLM 工具 | `os.startfile`/PowerShell 音量/Windows搜索或Everything/`clip`/`taskkill` |
| 危险操作确认 | `NSAlert` | 拦截确认 | PySide6 模态对话框（按钮文案/显示内容双端对齐） |
| 权限体系 | Accessibility / Automation | 权限页 | 基本无需特权（钩子/热键/剪贴板无需 admin），权限页改运行时自检 |
| 开机自启 | LaunchAgents plist | 常驻 | 注册表 `HKCU\...\Run` 或任务计划 |
| 打包分发 | py2app `.app` | 安装 | PyInstaller `.exe` |
| 存储/日志路径 | `~/Library/Application Support/Desktop-Pet`、`~/Library/Logs/Desktop-Pet` | 数据 | `%LOCALAPPDATA%\Desktop-Pet\`（logs 同目录 `logs\`） |
| API key | macOS Keychain (keyring) | 凭据 | Windows 凭据管理器 (keyring 后端切换，不放明文) |

---

## 五、mac 端适配细节

### 工具命令映射（tools_mac）
| 功能 | 命令 |
|---|---|
| 开程序/网页 | `open -a "App"` / `open URL` |
| 锁屏 | `osascript ... keystroke "q" using {control down, command down}`（需 Accessibility）|
| 音量 | `osascript -e 'set volume output volume X'` |
| 进程管理 | `ps aux` / `pkill -x` / `killall` |
| 文件搜索 | `mdfind` |
| 剪贴板 | `pbcopy` / `pbpaste` |
| 睡眠（危险）| `osascript -e 'tell app "System Events" to sleep'` |

### 工作区 / 窗口枚举 / 全屏检测
- 工作区：`NSScreen.visibleFrame`（已排除 Dock + 菜单栏），多屏取合集。
- 窗口枚举：`AXUIElement`（Accessibility），事件驱动 + 缓存，不每帧。
- 全屏/演示：监听 `NSWorkspace.activeSpaceDidChangeNotification` + `CGWindowListCopyWindowInfo` 查全屏窗口；前台全屏演示软件完全隐藏 + 禁吃鼠标。
- 活跃内容检测：前台应用白名单（IINA/QuickTime Player/浏览器全屏视频）；可选 `CoreAudio` 检测系统音频播放。

### 吃鼠标（mouse_lock_mac）
- `CGEventTap` 抑制鼠标事件，需 Accessibility；看门狗线程 5 s 兜底释放；权限未开只提示不抑制。
- 强制吐出热键 `Cmd+Option+T`（T=吐；弃 `Cmd+Option+Esc` 系统强制退出冲突，原 `Cmd+Option+Shift+P` 4 键难按）+ 托盘菜单。

### 权限
- Accessibility（全局热键/按键模拟/吃鼠标/锁屏）、Automation（控制其他 app）。
- 权限被永久拒绝（`kTCCServiceAccessibility` = `Denied`）→ 权限页明确提示 + 深链，不反复弹（系统只弹一次）。

### 自启 / 打包 / 路径
- 自启：`~/Library/LaunchAgents/` plist 或登录项。
- 打包：py2app `.app`；签名公证可选（未签名 wizard 兜底 + 右键开）。
- 路径：数据 `~/Library/Application Support/Desktop-Pet/`；日志 `~/Library/Logs/Desktop-Pet/`；config `~/.config/Desktop-Pet/config.json`(600)；单实例锁 `.lock` 在数据目录；本地克隆 `/Users/zzh4206/Desktop_Pet`。

---

## 六、win 端适配细节与特有风险

### 特有风险（前置 Spike，不占版本号）
| 风险 | Spike | 说明 |
|---|---|---|
| 透明点击穿透 | W1（v0.1 前）| `WS_EX_TRANSPARENT`+`WS_EX_LAYERED` 组合有坑：**穿透背景可穿透，但宠物渲染矩形不可穿透**（需响应单击/双击/拖拽）。全窗穿透无法局部豁免则拆双层窗（透明穿透背景层 + 宠物交互层），W1 结论定架构。 |
| 吃鼠标对管理员窗口失效（UIPI） | W2（v0.7 前）| `WH_MOUSE_LL` 收不到 UAC/管理员窗口输入；降级：检测到只发气泡不吃。 |
| LL 钩子被杀软误报 | W2 + v0.12 | 文档说明 + 白名单建议；打包签名可缓解。 |
| 高 DPI 多屏坐标 | v0.3 | 不同缩放率工作区计算（`GetDpiForMonitor`/Qt 逻辑坐标）。 |
| PyInstaller 报毒 | v0.12 | 签名或使用说明兜底。 |
| 打包 | S2（v0.5 后，双平台共用）| py2app + PyInstaller 最小集试跑，提前解 bundle 内 QML/assets 路径、keyring 后端、codesign。 |

### 工具映射（tools_win）
`os.startfile`（开程序/网页）/ PowerShell 音量 / Windows 搜索或 Everything（文件搜索）/ `clip`（剪贴板）/ `taskkill`（进程）/ `shutdown`（睡眠，危险；与 mac 端 `sleep` 对齐，不做关机）。

### 工作区 / 传感器 / 全屏
- 工作区：`QScreen.availableGeometry()` 或 `SPI_GETWORKAREA`，高 DPI 多屏见上。
- 传感器：`GetCursorPos`（鼠标）/ `GetLastInputInfo`（空闲，无需特权）/ `EnumWindows`（窗口枚举，pywin32）。
- 全屏：检测前台全屏应用 → 宠物隐藏/移副屏、暂停 WANDER；演示软件前台全屏 → 完全隐藏 + 禁吃鼠标。行为与 mac 端一致。

### 吃鼠标（mouse_lock_win）
- `WH_MOUSE_LL` 钩子拦截移动 + 钉住光标；看门狗 5 s 兜底（与 mac 共用逻辑，只换释放实现：取消钩子 + 恢复光标）。
- 强制吐出热键 `Ctrl+Alt+T`（T=吐，与 mac `Cmd+Option+T` 对应；避开 `Ctrl+Alt+Del` 系统冲突）+ 托盘菜单。

### 权限
- 基本无需特权（钩子/热键/剪贴板无需 admin）；权限页改运行时自检。

### 自启 / 打包 / 路径
- 自启：注册表 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` 或任务计划。
- 打包：PyInstaller `.exe`。
- 路径：数据 `%LOCALAPPDATA%\Desktop-Pet\`；日志 `logs\`；单实例锁 `.lock` 在数据目录；本地克隆 `D:\Desktop-Pet`。

---

## 七、提交规范（双向生效）

1. **登记制**：共享代码引入平台依赖，必须在 §四登记表加行 + commit message 标 `[平台]`。
2. **适配信号词**：diff 出现即触发对方适配登记——
   - mac 引用点：`NSScreen / CGEvent / osascript / NSAlert / pyobjc / AXUIElement / LaunchAgents / ~/Library / Keychain / pbcopy / pkill`
   - win 引用点：`win32 / ctypes / RegisterHotKey / WH_MOUSE_LL / GetLastInputInfo / LOCALAPPDATA / SetWindowLong / os.startfile`
3. **commit message**：中文说明 + 附版本号（设计期 `v0.0.x` / 实现 `v0.1+`）。
4. **改写已推历史前**须确认对方未拉取该笔。

### 适配流程（收到对方共享改动后）
1. 审 diff，用 §七.2 信号词定位平台引用点；
2. 对照 §四登记表：无现成实现→补本平台实现；已有→复核签名一致；
3. 跑本平台对应里程碑 Must（`python app.py` 本机验证）；
4. 合并前与对方确认接口签名无漂移。

---

## 八、待双方协商的决策点

1. 共享核心主笔：按模块划片（见 §三台账）还是按版本轮换。
2. lockstep 粒度：双端 Must 全绿即进下一版（默认）还是严格双端全部完成。
3. `window.py` 是否拆双文件（v0.1 W1 Spike 后定）。
4. win 端热键定 `Ctrl+Alt+P`(唤聊天)/`Ctrl+Alt+T`(吐出)，v0.11 RegisterHotKey 冲突检测后可调。

已决：~~是否引入分支/PR 流程~~ → 直接 `main` + 认领制（v0.0.13）；~~win 端强制吐出热键~~ → `Ctrl+Alt+T`（双端 3 键化，v0.0.12）。
