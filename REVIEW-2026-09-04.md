# 桌宠仓库全量 Review 记录（2026-09-04）

> 审查起点 HEAD `1d7ac74`（v0.14.36，含 v0.15.11 聊天情绪线合并）
> 范围：app.py + pet/ 全部 32 文件 + rig/ + ui/ QML + tools/ 10 脚本 + spikes/ 测试与 _qa 探针 + assets manifest + requirements/config.example/.gitignore + 打包配置；三路并行探索（核心运行时 / 集成安全面 / rig 产线测试）+ 重点发现逐条第一手复核。
> 上游：`REVIEW-2026-08-31.md`（其批次 0-J 修复经抽查均未回归；移交项 rL7 等见 §六）。
> 本轮重点：v0.14.31-36 新链路（部件扩充/blink/摆缝）+ v0.15.x 合并线（chat_emotion/训练产线）+ 全仓重新通读。

---

## 〇、在途工作区

- `spikes/_qa/skirt_live_check.png`：未跟踪 QA 产物（v0.14.36 摆缝验证图），随批次 0 入库。

---

## 一、Critical

无。上轮 C1 类资产事故无新增（交叠门禁实测口径抽查未见回退）。

## 二、High

**H1 🔴 存档每秒全量落盘 + fsync，24/7 永续——防抖对 1s 衰减流完全无效**
- `app.py:321-324,969-981`、`pet/pet_state.py:297-335,193-239`（第一手复核）。
- 链条：`apply_decay` 每秒产生 `age` 增量（age 无上界、永不触底）→ `update` 判"实际变化"成立 → on_change → `_save_timer.start(500)` 在下次 1s tick 前必然到期 → `_save_now` 全量 json dump + fsync + copy2(.bak) + os.replace，**每秒一次**。app.py:970-974 注释自认该问题但按"三项触底 ~27h"估算，漏算 age。另：`_periodic_save_timer`（30s）无条件调 `_save_now`，`store.save()` 无 dirty 检查，零变更也写盘。
- 影响：常驻应用持续磁盘 churn / SSD 磨损 / 笔记本磁盘无法休眠。
- 修法：变更通知按有意义粒度过滤（数值取整 0.1 比较、age 按分钟粒度）；`save()` 前置 dirty 判断，周期档只在 dirty 时写。
- 验证：v03/v05/v09 回归 + 新增"age 秒级递增不触发落盘、跨分钟粒度才触发"用例。

**H2 🔴 v2 情绪训练：验收集文本就在训练采样池内，"绝不混训"承诺结构性失效**
- `tools/build_chat_emotion_v2_data.py:19-31,66-71`（第一手复核）。
- `ACCEPTANCE` 的"我很难过"（SEEDS.sad）、"我不难过，刚才只是开玩笑"“今天没什么特别的”（SEEDS.neutral）逐字在池；`rng.sample(pool, per_label)` 无排除。文件头 25 行"绝不出现在训练集"承诺被违反；`pet/models/chat_emotion_v2/report.json` 的 acceptance macro_f1=0.911 作为唯一独立质量门禁被高估（逐字命中即污染）。
- 修法：采样前从 pool 剔除 acceptance 全集文本 + 构建时 assert 无交集，重建数据集。

**H3 🔴 v2 训练按行随机切分，变体族跨 train/valid 泄漏——validation F1=0.992 虚高**
- `tools/train_chat_emotion_encoder.py:74-89`（第一手复核）。
- 同一 seed × 9 前缀 × 8 后缀的近重复变体被行级 shuffle 拆两集（"我很难过" vs "说实在的，我很难过，"）；best checkpoint 按泄漏指标选取。0.992 与 acceptance 0.911 的差值即泄漏量。
- 修法：按 seed 分组切分（group=种子句 id，GroupShuffleSplit 语义），重训后以新 report 为准（需 GPU 服务器，见批次 D 说明）。

## 三、Medium

**M1 🟡 右键/中键也触发"摸摸头"，右键按住可拖拽宠物**
- `pet/window.py:330-380`（第一手复核）：press/release/move 三处均无 `event.button()` 过滤。快速右键（<5px、<300ms）→ `_single_shot` 在 `menu.exec()` 嵌套循环里照常触发 → 每次开右键菜单白送 mood+5 + "摸摸头～"气泡；中键同理；右键按住移动 ≥5px 进 `_dragging`。
- 修法：press 记录 button，release/move 仅 LeftButton 生效。

**M2 🟡 聊天情绪"禁用"保存后不生效——继续把用户消息写进 chat_emotion.json 直到重启**
- `app.py:570-575,611-634,793-808`（第一手复核）：设置对话框只改 `_chat_emotion_cfg` 并写盘；运行时两条路径只判 `_chat_emotion_store is not None`（启动按 enabled 定死）。明确 opt-out 后仍持续记录用户消息（自述"隐私最小化档案"）+ 60s 推理照跑。
- 修法：`_on_user_message`/`_poll_chat_emotion` 开头判 `enabled` 即返回。

**M3 🟡 `event_confidence_threshold` 是死旋钮——v0.15.9"降阈值"实际只降到 0.55 而非 0.5**
- `pet/chat_emotion.py:242-255` + `app.py:589-591`（第一手复核）：引擎内层 `confidence < 0.55` 即返 `used_fallback=True`；`is_significant` 首条件 `not used_fallback` 否决之。有效阈值恒 `max(confidence_threshold, event_confidence_threshold)`=0.55。
- 修法：`evaluate` 低置信时返回真实 label+confidence（去 used_fallback 标记或加独立字段），触发判定全部收敛到 app 层 `is_significant`；config 注释写明两阈值关系。

**M4 🟡 流式回复期间发送消息被静默丢弃，QML 已无条件清空输入框**
- `pet/ui/chat_bridge.py:193-194` + `pet/ui/main.qml:243-247`（第一手复核）：worker 在飞时 `send()` 直接 return（无排队/提示），QML `onAccepted` 无条件清空。最长 180s 窗口内消息丢失零反馈。
- 修法：`send` 返回 busy / 发信号，QML 据此保留输入文本（或排队到 done 后续发）。

**M5 🟡 `kind=blink` 豁免一切自动门禁，无面积/层序护栏**
- `tools/qa_rig_composite.py:39-40,120-125` + `pet/rig/spec.py:69`：交叠门禁与静止合成双处整体跳过 blink 件；schema 无 blink+z 组合约束（blink+under_core 永不可见也放行）。误标 blink 的任意大部件零检查、运行期照常渲染（每 4.7s 显 130ms）。
- 修法：门禁加 blink 护栏——必须 over_core、px_rect 面积上限（如 ≤全图 2%）、或对 blink 单独出 QA 图。

**M6 🟡 final/healthy_neutral 的 `qa.exclude` 恰等于整个尾件 bbox**
- `assets/rig/final/manifest.json:518-522` vs `:31-44`（第一手复核）：exclude "717,595,1024,1046" 与 `tail_healthy_neutral` px_rect 逐值相同，307×451px 全尾区失配不计入 inter_pct。尾件今后任何回归（挖除错位/PNG 损坏/重切事故）静默 PASS；test_v14 T10 常驻门禁同被掩盖。
- 修法：exclude 收敛到实测残留带（尾根接缝数像素宽），或给豁免面积设上限 + 周期复审标记；收敛前先实跑门禁量化当前失配分布。

**M7 🟡 v1 训练同款泄漏：CPED 滑窗近重复跨集**
- `tools/build_chat_emotion_data.py:54-56` + `train_chat_emotion.py:55-56`：每样本带前 4 句上下文，同对话相邻样本共享 4/5 内容；行级随机切分。修法：按 Dialogue_ID 分组切分。

**M8 🟡 部署物 int8 ONNX 从未参与评估——量化劣化无门禁**
- `tools/train_chat_emotion_encoder.py:118-120`：report 的 validation/acceptance 指标全部算在 fp32 torch embedding 上；运行时真正加载的 `encoder.int8.onnx` 只查"导出成功"。修法：导出后 onnxruntime 复算 acceptance 指标写入 report 并设与 fp32 差值阈值。

**M9 🟡 v1 特征训练/推理双份手工实现 + 分布漂移（无 parity 测试）**
- `tools/train_chat_emotion.py:20-31` vs `pet/chat_emotion.py:212-224`：n-gram blake2b 两份独立实现；推理带 exp(-age/18) 时间衰减、上下文无上限（48h 全量），训练等权 + 恒 ≤5 句；即时路径单句 vs 训练 2-5 句。v1 是 v2 损坏时的实际回退模型，单侧改动即静默 skew。
- 修法：提取共享 `ngram_hash(texts, weights)`；推理对齐训练窗口（最近 5 条等权）或训练侧模拟衰减；加 parity 测试。

**M10 🟡 test_v15 覆盖缺口**
- `spikes/test_v15_chat_emotion.py`（54 行）：v2 加载失败降级（`_load_v2` 抛异常→降级时段兜底）、五分钟到期恢复（`_reset_chat_emotion_to_neutral` 全 spikes 无引用）、阈值边界（`>=` 与 0.55/0.5 叠加关系）、v2 不被 `obvious_emotion` 短路——全部零覆盖。修法：补四组用例。

## 四、Low

**app/行为/展示**
- L1 `app.py:140-144` + `chat_emotion.py:186-196`（复核）：v2 目录存在即选中，任一文件坏 → 异常吞掉 → weights/session 全 None → 永久时段兜底，完好的 v1.npz 不被尝试。修法：v2 加载失败回退构造 v1。
- L2 `presenter.py:266-303,185-196`（复核）：`play_frames` 缺 is_file 守卫（emoji 串直接进 `QImage`/`figASrc` 垃圾 URL；`set_sprite` 有守卫，此处没有）；`_init_quick` 对 emoji 初始 sprite 隐藏 label 且不上图 → rig+emoji 启动后 ~1s 空白。修法：复刻 set_sprite 的 emoji 分流；`_init_quick` 非文件 sprite 不隐藏 label。
- L3 `app.py:1182-1189,1222-1224`：land 单帧序列无终止路径（豁免表+无 singleShot），free 模式落地蹲伏定格 5-15s、edge 模式 15-35s。修法：播后排 ~400ms singleShot 停帧。
- L4 `asset_provider.py:97-102,178-180`：emoji 行走 2 帧交替死代码（`get_frames(MOVE_TO)` 全仓零调用），行走中 emoji 宠物是静止贴图（v0.3 行为回归）。修法：`_frame_tick` 为 EmojiProvider 恢复交替播放，或删分支+docstring 对齐。
- L5 `presenter.py:375-397`（复核）：行走中 `set_walk_figure(None)` → `_walk_edge` 永早退 → `_walk_showing` 永久卡 True → `on_state_change` 只更新恢复目标不刷画面，冻结在旧覆盖图。触发：paperdoll 行走中 side/neutral 静态图缺失（半更新/误删资产）。修法：sprite None 且 `_walk_showing` 时清标志并 `set_sprite(_static_sprite)` 还原。
- L21 `app.py:1129-1153` + `presenter.py:479-480`：双眨眼系统并存——paperdoll 档帧版 blink 随机动作触发时切到烤死全帧渲染，6 sway 件+腿件微动骤停 ~1.3s + 视觉跳变（v0.14.33 已有引擎级贴片）。修法：`_play_animate` 在 paperdoll 档跳过 blink（stretch/roll 保留）。
- L22 `app.py:559-569` + `chat_bridge.py:200-204`（复核）：`_maybe_followup` 裸调在 try 外 + bridge `except: pass` 无日志，follow-up 异常无迹可查。修法：并入 try / bridge 加 debug 日志。

**config/schema**
- L6 `proactive.py:172-177,551-559` + `config.py:151-156`（复核）：quiet_hours 小数过校验（`int()` 截断判定却存原 float），`d.replace(hour=float)` 抛 TypeError（深夜唤醒重排槽内异常）；且 22.5 实际表现 23 点。修法：schema 限 integer 或存时 `tuple(int(h)...)`。
- L7 `config.py:220` vs `app.py:790`（复核）：schedule schema 正则 `^[0-2][0-9]:...` 放行 24-29 点（字符串比较永不触发，静默无效）；设置对话框正则是对的。修法：schema 对齐 `(?:[01]\d|2[0-3]):[0-5]\d`。
- L8 `config.py:194-195` + `app.py:671` + `asset_provider.py:114`：`sleepy_idle_minutes:0`（schema 允许）→ 门限 0 恒真 = 永远 SLEEPY，且无"关闭"取值。修法：0 语义定义为禁用（或 minimum 提 1）。

**LLM/proactive**
- L9 `llm.py:186-194` vs `250-251`：`_stream_once` 4xx 早退在 try 之外，`_resp` 残留、连接不显式关。修法：状态码检查挪进 try/finally。
- L10 `llm.py:364-368`：工具循环空文本+新 tool_calls 轮的 assistant 消息携带上一轮旧 content（上下文注入过时回复）。修法：append 用当轮 `text2 or ""`。
- L11 `proactive.py:282-296,422-442`：门禁异常日志写"放行不抑制"但代码继续走后续门禁可能开吃（言行不一）；DND 检测异常 fail-open（铁律4 不安全方向）；`sync_release` 异常裸吞连日志都没有。修法：统一 fail-closed 或改文案；吞异常处加 warning。

**win 工具/权限**
- L12 `tools_win.py:23,74-94`：`open_app` 无系统程序黑名单，"cmd"/"regedit" 可过 `_APP_NAME` 直接 `os.startfile`（regex 禁 `/` 无参数面，危害限开窗口）。process 有 denylist 而 open_app 无，防护不对称。修法：加 `_APP_DENYLIST`（含 .exe 形态）。
- L13 `tools_schema.py:80-81`：`_BARE_ROOT` 注释称覆盖 `~root` 但正则只匹配 `~`/`~/`/`/`。当前消费方使其不可达，共享 denylist 语义缺口。修法：`^~` 前缀整体拒绝。
- L14 `perm_bridge.py:149-158` + `app.py:423`：PermBridge 启动即实例化 → `__init__→refresh` → **每次开机后台 PowerShell 读一次剪贴板**（绕过 clipboard 工具 dangerous=True 确认语义；内容即读即弃不落日志，但属未经用户动作的剪贴板访问，且拖慢启动 1-3s）。修法：剪贴板项延迟到用户打开权限页再测（`_show_perm` 已有 refresh）。

**传感器/热键/mac**
- L15 `hotkey_win.py:163-191`：stop→start 复用陈旧 `_thread_id`，stop 可能投递失败致新线程+旧热键残留（未来运行时改键场景失效）。修法：start() 重置 `_thread_id=0`。
- L16 mac 静态可证：`hotkey_mac.py:249-281,335-356` 全失败时 handler 已装未拆、`_active` 锁外写；`mouse_lock_mac.py:316-378` CGEventTap mach 端口从不 CFRelease（每次吃鼠标会话累积）。修法：回滚 RemoveEventHandler / 移进锁内；补 CFRelease。（随实机复核项处理。）
- L17 `sensor_win.py:192-244,333-348,525-533`：`_monitor_map` 每次调用重建（EnumDisplayMonitors+screens 配对）——站窗顶时 `rect_at` 每 50ms tick 一次，20Hz 主线程 Win32 调用；`_hwnd_to_rect` 两级兜底都未命中时把物理坐标当逻辑坐标返回（混 DPI 下差 1.5×）。修法：TTL 缓存（与 2s 传感器刷新同拍）；兜底未命中返 None。

**记忆/情绪存储**
- L18 `memory.py:143-146`：`.bak` 裸 copy2（.bak 被锁时 OSError 冒泡；chat_emotion.py:113-117 同款代码有 try）。修法：对齐。
- L19 `chat_emotion.py:136-175`：`ran_slots` 注释"最近 14 天"实现"当年 1 月 1 日起全部"；`add_feedback` 全仓无调用且列表无截断。修法：按实现对齐注释；删或接线。

**产线工具/QML/打包**
- L20 产线杂项：`gen_walk_mids.py:142` `--only` 末参无值 IndexError、`:73-75` 200 非 JSON 无 try；`gen_fill.py:130-138` 掩码无 alpha 通道时 `mp[x,y][3]>0` 恒真 → 全图回贴、"区域外逐字节=原图"构造性保证静默失效（补 mode 校验）；`import_layers.py:26` 缺参 IndexError；`train_chat_emotion_encoder.py:106-117` 量化失败时 fp32 onnx 残留产物目录（补 unlink）；`split_parts.py:303-305` pivot 连线只做 /3 缩放未减部件 bbox 原点（rL2 只修一半）；`qa_rig_composite.py:77-89` `check_overlap` 死代码；exclude 串格式零校验（坏值直接崩门禁，schema 补 pattern）。
- L23 `spec.py:33-96`：figures 值允许空串/反斜杠路径；pivot 无"落在 px_rect/图内"校验；blink 与 z 无组合约束（并入 M5）。修法：补 pattern/包含关系告警。
- L24 `requirements.txt:1`（`>=6.5,<6.8`）vs `presenter.py:72-75`/`app.py:161-163` 注释"PySide6 6.10 约束"不一致。修法：核实统一。
- L25 `rig_scene.qml:146-150`：delegate 同时 anchors.fill 与显式 width/height 绑定（anchors 优先，死代码+QML 告警源）。
- L26 打包留档：23MB `encoder.int8.onnx` 直接入 git，每次模型迭代仓库 +23MB（建议外置发布物，随 v0.12 打包里程碑决策）。

---

## 五、已核对、确认无问题的区域

- **v0.14.35 set_conversation_mood-None 崩溃同类残留**：三处调用均传有效 Mood；两套 on_state_change 均有 None 守卫。无残留。
- **v0.14.31-36 修复链**：`_anim_key` 初始化+getattr 收口、set_stage 换档降级铁律、blink 门禁跳过、双槽状态机硬切不叠 B 槽——完整。
- **chat_emotion 线程模型**：store/engine 全主线程、expiry 单发 timer + 60s poll 双保险、shutdown 停表清单含两个情绪 timer、启动清残留——健康。
- **shutdown 七步 / 单实例锁 / FSM 物理边界 / 全屏抑制链 / 手势消歧（除 M1 button 问题外）/ 气泡多屏 / 托盘回滚**：无新问题。
- **安全面**：subprocess 全 argv 列表无 shell=True；PowerShell 插值有 `_ps_sq`+字符集双拦；API key 不进日志（M8 脱敏在位）；S1/M2/M5 旧修在位。
- **win 鼠标锁生命周期**（三态防孤儿钩子/watchdog/UIPI TokenElevation）、**热键线程内成对注册注销**：在位。
- **DPI 变换**（批次F `_monitor_map` szDevice 配对）：配对逻辑本身正确（性能问题见 L17）。
- **v2 训练/推理契约**（pooling/L2/max_length/tokenizer save-load）：一致（除 M8 量化盲区）。
- **config.example 与 schema 逐段对齐**；requirements 的 torch/transformers 不入运行时依赖（正确）。
- **_qa 探针**：整体有守卫（`resplit_legs_claim.py:90` 魔法回退无实害、`probe_sum.py:57` 仅慢不错）。

## 六、上轮（08-31）移交项核对

| 项 | 状态 |
|---|---|
| rL7 young/adult 尾巴部件 | 仍开放（资产工序，版本规划留档） |
| eat_mouse 12 帧零调用 | 维持功能决策留档 |
| T5/T6/T7 测试债、F28 pytest+CI | 维持留档 |
| F22 config.example 打包依赖 | 维持留档（随 v0.12 打包里程碑） |
| 非流式续轮慢滴流 | 维持留档 |
| mac 批次I 实机复核项 | 维持（本轮 L16 新增两条静态可证小项随批处理） |

## 七、修复批次安排

版本自 v0.14.37 顺 .Z 递增（当前活跃线 v0.14.x；HEAD=1d7ac74 v0.14.36）；每批独立提交 + 实跑回归，commit message 测试数字以实跑为准。

- **批次 0｜v0.14.37 留档**：本 REVIEW + `spikes/_qa/skirt_live_check.png` 入库。
- **批次 A｜v0.14.38 存档节流**：H1。回归 v03/v05/v09 + 新增 age 粒度用例。
- **批次 B｜v0.14.39 交互与聊天**：M1 + M4 + L22。回归 v02/v04。
- **批次 C｜v0.14.40 聊天情绪配置语义**：M2 + M3 + L1 + L6/L7/L8 + L19。回归 v15/v11 + 新增"禁用即时生效""v2 残缺回退 v1"用例。
- **批次 D｜v0.14.41 训练产线**：H2 + H3 + M7 + M8 + M9 + L20（量化失败 unlink）。**模型重训需 GPU 服务器**（本机不可跑）：本仓先修脚本+重建数据集+断言，report 新数字以远端实跑为准，重训可另排。
- **批次 E｜v0.14.42 rig 门禁与展示**：M5 + M6 + L2 + L3 + L5 + L21 + L23 + L25。回归 v13/v14 + qa_rig_composite 全 figure 实跑。
- **批次 F｜v0.14.43 win 工具/传感器/LLM**：L9-L15 + L17 + L18。回归 v07win/v08/v08perm/v09/冒烟。
- **批次 G｜v0.14.44 mac 静态**：L16（py_compile+静态核对，留实机复核项）。
- **批次 H｜v0.14.45 测试补强收尾**：M10 + L4 + L24 + 全量 16 套件回归 + push；本文件补"清偿执行记录"节。

## 风险与依赖

- 批次 D 的 report 数字依赖 GPU 服务器（云端直连通道）；本机交付脚本修复+数据集重建+无交集断言。
- 批次 E 的 exclude 收敛需先实跑门禁量化尾件当前失配分布；若残留带确实覆盖大半 bbox，改走"豁免面积上限+复审标记"方案并在此留痕。
- 批次 G 按惯例留 mac 实机复核项。

---

## 八、清偿执行记录（同日完成）

| 批次 | 提交 | 内容 | 实跑 |
|---|---|---|---|
| 0 | v0.14.37 | REVIEW 留档 + skirt_live_check.png 入库 | — |
| A | v0.14.38 | H1 存档节流：apply_decay 亚粒度门（数值 0.1 档/age 1 分钟档，未跨档不推 _last_update）+ PetStateStore dirty 标志 + _save_now 零变更不落盘 | v05 23/23（H1a-g）·v03 76/76·v09 38/38 |
| B | v0.14.39 | M1 鼠标事件仅左键（右键菜单不再白送摸头/右键拖拽/右键双击喂食）+ M4 send 返 bool/QML 保留输入 + L22 follow-up 兜底与钩子日志 | v02 14/14（M1）·v04 20/20（M4×2） |
| C | v0.14.40 | M2 禁用即时生效 + M3 evaluate threshold 覆写（0.528 置信实测验证明）+ L1 v2 残缺回退 v1 + L6/L7/L8 schema 三项 + L19 + **执行中新发现随修：sleepy 门限从未接线（_mood_from_state 恒用 600s 常量）** | v15 21/21（M3a-b/L1a-b/M2a-d）·v10 29/29（L8a-d）·v11 20/20·v06 32/32 |
| D | v0.14.41 | H2 验收集剔除+assert（实跑 overlap=0）/ H3 变体族分组切分（不变式 group_cross=0）/ M7 Dialogue_ID 分组 / M8 acceptance_int8 门禁 / M9 ngram_vector 单一实现+窗口对齐 / L20 unlink | v2/v1 数据构建实跑·分组不变式 0 交叉·v15 24/24（M9a-c）；**重训待 GPU** |
| E | v0.14.42 | M6 豁免预算制（实测 5403≤8100；残留散布全尾段无窄带，fallback 方案落地）+ M5 blink 护栏双端 + C3/C4 + L2/L5 presenter + L3 land 自停 + L21 双眨眼去重 + L23（figures 反斜杠禁令因 Windows 绝对路径误伤放弃，注释留痕）+ L25 | v13 48/48·v14 35/35（T10b+L2/L5）·v10 33/33·门禁 CLI PASS |
| F | v0.14.43 | L9/L10 llm + L11 门禁 fail-closed×3 + L12 open_app 黑名单 + L13 _BARE_ROOT + L14 启动零剪贴板 + L15 热键 id 重置 + L17 监视器 TTL+None 兜底 + L18 | v04 20·v07 20·v06 32·v07win 33·v08 35（L12/L13）·v08perm 5（T0）·v09 38·冒烟 9 |
| G | v0.14.44 | L16a hotkey_mac 锁内收口+全失败回滚 / L16b CGEventTap CFRelease（py_compile+静态，留实机复核） | win 侧 7 套件全绿 |
| H | v0.14.45 | M10 v15 补强（M10a-d：阈值边界/到期恢复/v2 不被 obvious 短路/v1 兜底）+ L4 emoji 行走帧交替恢复（`_play_key(interval=)` 参数名踩坑即修）+ L24 版本注释对齐 | **全量 17 项 484 通过 0 失败** |

**v0.14.45 全量实跑**（16 套件 + 冒烟，合计 484 通过 0 失败）：v02 14 · v03 76 · v03s（真桌面）13 · v04 20 · v05 23 · v06 32 · v07 20 · v07win 33 · v08 35 · v08perm 5 · v09 38 · v10 35 · v11 20 · v13 48 · v14 35 · v15 28 · 冒烟 9。静止合成门禁 7 figure（v14 T10 常驻）全 PASS，尾件豁免预算 5403≤8100。

### 执行中实测新发现（留档）
- **sleepy_idle_minutes 配置从未生效**（批次 C 执行中发现，比 L8 记录更重）：`_mood_from_state` 恒用 600s 模块常量，provider 构造存的 `_sleepy_idle_s` 从未被读取——已随 L8 接线修复并加 L8a-d 用例。
- **test_v15 due_slots 用例墙钟依赖**：23:00 前运行必挂（此前皆白天跑未暴露）——固定 23:00 时间点修复。
- 本机 Python 3.12 无 onnxruntime/tokenizers/torch：v2 引擎在本机恒走 L1 回退链（v1/时段兜底），属环境现状非缺陷；GPU 侧重训 v2 后 report 数字以服务器实跑为准。

### 本轮未清项（移交下轮）
- **v2/v1 模型重训**（依赖 GPU 服务器）：批次 D 脚本与数据集已就绪（overlap=0、分组 0 交叉实证），重训跑 `tools/build_chat_emotion_v2_data.py` → `train_chat_emotion_encoder.py`（int8 门禁已内建）、`build_chat_emotion_data.py` → `train_chat_emotion.py`，新 report 入库后数字更新。
- mac 批次 G 改动实机复核（热键全失败回滚/tap 端口释放）。
- 维持留档：rL7 young/adult 尾巴、eat_mouse 12 帧零调用、F28 pytest+CI、F22 打包依赖、非流式续轮慢滴流、T5/T6/T7 测试债、encoder.int8.onnx 仓库膨胀（建议外置发布物）。
